# PASSKEY_RP_ID / PASSKEY_ALLOWED_ORIGINS（src/index.tsのdefineString参照）。
# 本番ドメイン未確定のため、コード側のデフォルト値と同じlocalhost設定を
# 明示的にここへ置き、非対話（CI/CLI経由）デプロイでもプロンプトなしで
# 進められるようにしている。本番ドメイン確定時はこの値を更新すること。
PASSKEY_RP_ID=localhost
PASSKEY_ALLOWED_ORIGINS=http://localhost:8765
