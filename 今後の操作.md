# Google連携を本番公開するまでの手順

## 前提・現状

- Google Cloud Console / Firebase Consoleでの操作は必ず **`rhing.official@gmail.com`** で行う（個人アカウントではなくRhing名義の専用アカウントに統一する方針）
- 現在、OAuth同意画面は **「テスト」ステータス**。テストユーザーとして登録したGoogleアカウントのみがログインでき、未登録のアカウントでは「このアプリはGoogleで確認されていません」という警告が出る
- DaiDaiには性質の異なる2種類のGoogle OAuth利用箇所があり、本番公開に必要な審査の重さが異なる:
  - **ログイン本体**（`lib/repositories/auth_repository.dart`、`sign_in_screen.dart`）: スコープはデフォルト（email/profile/openid相当）のみで、Googleの分類上「非機微（non-sensitive）」スコープ
  - **Googleカレンダー連携**（`lib/services/google_calendar_auth_service.dart`、オプトイン機能）: `https://www.googleapis.com/auth/calendar.events` スコープを要求しており、Googleの分類上「機微（sensitive）」〜「制限付き（restricted）」に該当しうる

## 1. テスト運用中の当面の対応（今すぐ）

1. `rhing.official@gmail.com` でログインした状態で、Google Cloud Console「APIとサービス」→「OAuth同意画面」→「対象」タブを開く
2. 「テストユーザー」欄に、テストに使うGoogleアカウント（例: `arataurusu@gmail.com`）を追加
3. テストユーザーは **最大100人まで** という上限があるため、社内・友人テストの範囲であれば当面これで問題ない

## 2. 本番公開に必要な設定項目

OAuth同意画面の各項目を、公開に耐える内容で埋めておく:

- **アプリ情報**: アプリ名（DaiDai）、サポートメール
  - ⚠️ **ロゴをアップロードすると原則として確認審査が必須になる**。審査を避けたい/最小限にしたい場合はロゴ未設定のままにする選択肢もある
- **アプリのドメイン**: アプリホームページURL、プライバシーポリシーURL、利用規約URL
  - これらは実際にWeb上に公開されているページである必要がある（プライバシーポリシー・利用規約のページをまだ用意していない場合は先に作成・公開する）
- **承認済みドメイン**: 上記URLが属するドメインを登録
- **デベロッパーの連絡先情報**: `rhing.official@gmail.com` 等

## 3. スコープごとの審査要否

- **ログイン本体のみ（非機微スコープ）**: ロゴ未設定であれば、確認審査なしで「本番」へ公開できる場合が多い
- **Googleカレンダー連携（`calendar.events`）**: 機微〜制限付きスコープに該当する可能性が高く、Googleの確認（verification）審査が必要になる見込み
  - スコープの利用目的の説明や、アプリの動作を示す説明動画（YouTube限定公開等）の提出を求められることがある
  - スコープが「制限付き（restricted）」に分類される場合は、追加でセキュリティ評価（CASA）が必要になり、**費用・期間がさらに増える**
  - スコープの正確な分類（sensitive/restricted）は変わりうるため、OAuth同意画面の「データアクセス」設定画面上でGoogleが実際に表示する分類を都度確認すること

## 4. 公開（本番へ切り替え）の手順

1. OAuth同意画面の「公開ステータス」を「テスト」→「本番」に変更する
2. 機微/制限付きスコープ（カレンダー連携）を含めている場合、この変更時にGoogleの確認審査フローへ自動的に進む
3. 審査期間の目安: 数日〜数週間程度。提出内容の不備や制限付きスコープのCASA評価が絡む場合はさらに長くなりうる

## 5. 公開後の注意点

- 本番公開後はテストユーザーの100人上限が解除され、誰でもログインできるようになる
- 公開・審査完了後にスコープや同意画面の表示情報（アプリ名・ロゴ・ドメイン等）を変更すると、再審査が必要になることがある

## 関連メモ

- `日記.md` に、Googleカレンダー連携のOAuth設定時に発生した `origin_mismatch`（承認済みJavaScript生成元の未登録）エラーとその対応記録がある。承認済みドメイン・生成元の設定は公開前に改めて確認すること
