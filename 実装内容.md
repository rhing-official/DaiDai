# 実装内容チェックリスト

完成した処理と未実装の処理を分けるためのチェックリスト。

**運用ルール**: チェック済み（`[x]`）の項目は「実装済みで動作確認も取れている」ことを意味する。
その処理に関連するエラー・バグが発生し、コードを再び書き直すことになった場合は、
**着手した時点でチェックを外す**（`[x]` → `[ ]`）こと。直ったら再度チェックする。

---

## 認証・アカウント

- [x] Google認証（Web/モバイルでフロー分岐）
- [] Apple認証の実装（`AuthRepository.signInWithApple()`、`OAuthProvider('apple.com')`。WebはFirebase Auth JS SDKのポップアップ、Android/Windows/LinuxはsignInWithProviderの汎用OAuthフロー）。**Apple Developer Program（有料）でのServices ID/Team ID/Key ID設定が必要と判明し、2026-07-23時点でユーザーの意向により見送り。実際に使えるようになるまでログイン画面のボタンはUIから非表示にしている**（メソッド自体は実装済みなので、将来設定が済めばボタンを戻すだけで良い）
- [x] Rhing ID登録・重複チェック・小文字統一
- [ ] 2段階認証（TOTP）※フェーズ2
- [ ] パスキー ※フェーズ2
- [ ] QRコードログイン ※フェーズ2
- [ ] 秘密の質問によるパスワード復旧
- [ ] アカウント削除（3時間以内完全削除）

## 一対（DM）

- [x] 友達申請フロー（IDで検索→申請を送る→相手が承認する→一対で会話できるようになる）
- [x] 届いている申請／送った申請を一対リストの最上部にインライン表示（承認して会話が始まると通常のリストへ下がっていく）
- [x] 届いている申請の承認・拒否をその場のボタンで操作（専用画面は廃止し一対リストに統合）
- [x] 相互申請時の即時承認（お互いが同時に申請した場合は自動で友達成立）
- [x] Firestoreルールで一対作成をfriendRequests承認済みに限定（IDを知っているだけでは会話を開始できない）
- [x] 一対一覧（件数チップ表示）
- [x] 一対・広場のピン留め（右クリック／長押しメニュー、ピン留めしたものが上部に固定）
- [x] 一対・広場の通知オフ（右クリック／長押しメニュー。実際の通知抑制はFCM基盤が未実装のため表示上のみ）
- [x] メッセージ送受信（Firestoreリアルタイム購読）
- [x] 一対作成時のFirestoreルール不整合バグ修正（`resource == null`対応）
- [ ] 友達申請の新着通知（現状は一対リストへのインライン表示のみ。プッシュ通知はFCM基盤が未実装のため後続）
- [ ] 友達解除（アンフレンド）
- [x] 招待リンク・QRコードでの仲間追加（縁結び、2026-07-24追加）: 身だしなみタブの「縁結び」に自分のRhing IDを埋め込んだ招待リンク（`https://.../invite/<rhingId>`）とQRコードを表示。相手がリンクを開く、またはアプリ内スキャナーでQRコードを読み取ると`/invite/:rhingId`の確認画面が開き、既存の`FriendRepository.sendRequest`をそのまま呼び出す（新しいFirestoreコレクションは追加していない）。未ログインでリンクを開いた場合もログイン・Rhing ID登録後に自動的に確認画面へ進む（`AuthGate`を`AppGate`から切り出して共通化）。ネイティブ版（Android/Windows/Linux/iOS/macOS）はリンクを開くと本番Web版（Vercel）がブラウザで開く形（アプリ自体へのディープリンクは未対応）

## 広場（グループ）

- [x] 広場の作成（自分＋2人以上）
- [x] 広場一覧（件数チップ表示）
- [x] メッセージ送受信（Firestoreリアルタイム購読、複合インデックス対応済み）
- [x] メッセージへの送信者アイコン表示（イニシャルアバター、色分け）
- [x] 広場のピン留め・通知オフ（一対と共通の右クリック／長押しメニュー）
- [x] 広場のカスタマイズ機能（2026-07-24追加）: 語らい画面AppBarにハンバーガーメニューを追加し、「プロフィールカード（広場を代表する1枚のカード。アイコン・名前・説明を直接持つ自己完結型のモデルで、メンバー全員が編集可能）」「メンバー一覧」「招待リンク作成」「退会」を集約。招待リンク（`/join/:groupId`）は縁結びと同じくリンク表示＋コピー＋QRコードに対応し、開くと長・モデレーターの承認制の参加リクエスト（`GroupJoinRequest`、`groups/{groupId}/joinRequests/{requesterId}`）が送信される（即時参加ではない、2026-07-24ユーザー確認済みの方針）。招待リンクを非メンバーや外部SNSのリンク展開（OGP）にも見せられるよう、`groupInvites/{groupId}`（名前・アイコン・説明のみの公開読み取り専用ドキュメント）を新設。退会はオーナー不可。Firestore/Storageルールを更新（`groups/{groupId}`の更新を「プロフィールカード編集」「参加承認」「本人の退会」の3パターンのみに限定、`groupIcons/{groupId}`のStorage書き込みをFirestoreのメンバー判定と連携）。あわせて縁結び（`/invite/:rhingId`）にも同様の外部SNS向けOGP対応を追加し、`userInvites/{rhingId}`（呼び名・アイコンのみの公開プレビュー）を新設。両方のOGPは`api/link-preview.js`（Vercelサーバーレス関数、Node）でクローラー向けにog:タグを注入したindex.htmlを返す形で実装したが、**実際のデプロイ・Discord/LINE/X等での展開確認は未実施**（ローカルでは検証不可）。Firestore/Storageルールも`firebase deploy`未実施のため本番未反映
- [ ] 表組・裏組（カテゴリ機能） ※フェーズ3
- [ ] 表広場（公開広場） ※フェーズ3
- [ ] 密談（非公開チャンネル）・談論（プロジェクトボード） ※フェーズ3
- [x] 広場詳細ダイアログ（`PlazaDetailDialog`、AppBarの情報アイコンから開く）: `docs/マップ.md`の広場サイトマップ（表広場/裏広場・表組/裏組・寄合/密談・談論）を一覧表示し、現在実際に動いている構造（裏広場・単一の寄合のみ）には実装済みマークを、それ以外（表広場・表組・裏組・密談・談論）には準備中マークを付ける。談論はCLAUDE.mdの「実装しない機能」表の「Discordフォーラム機能」と概念が重なると指摘されていたが、2026-07-24にユーザー確認の上でマップ.md通りに準備中項目として追加する方針に決定（機能自体は未実装のまま）
- [ ] ロール機能：広場全体のロールとチャンネルごとのロール

## 語らいタブ（ホーム画面UI）

- [x] 一対／広場の横並びタブ切り替え
- [x] ＋ボタンを広場タブの右横にチップ（`IconButton.filled`）として配置
- [x] ＋ボタンからの中央ポップアップ（一対を始める／広場を作る）
- [x] ポップアップのポップアニメーション・外側タップでの同アニメーション解除（`Dialog`を`Material(color: transparent)`で二重ラップしていたため外側タップが効かないバグを修正。`test/add_menu_dismiss_test.dart`に回帰テストあり）
- [x] サイドバー／下部ナビを丸いチップ形式に統一（レスポンシブ切り替え）
- [x] ブラウザ・マウスの「戻る」「進む」ボタン対応（go_router導入）
- [x] モバイル下部ナビ上でのスワイプによるタブ切り替え
- [x] 広い画面（760px以上）では一対・広場一覧と会話を左右分割で同時表示（Discord風）。一覧をタップしても画面遷移せず、右隣に会話が開く。狭い画面では従来通りフルスクリーン遷移
- [ ] ブラウザ直接URL入力・リロードからの画面復元（現状`extra`渡しのため未対応。分割ビューの選択状態もURLには載らない）

## 身だしなみ（蔵システム）

- [x] アイコンのアップロード・切り替え・削除（最大2件）
- [x] 背景画像のアップロード・切り替え・削除（最大3件）
- [x] ステメ（ひとこと）の追加・切り替え・削除（最大2件）
- [x] ニックネームの追加・切り替え・削除（最大3件、最大20字）。Rhing IDの代わりに友達へ表示される呼び名で、身だしなみタブから変更する
- [x] ステメの文字数上限（最大40字）。ニックネーム・ステメともに`TextField.maxLength`で入力自体を制限し、ウィジェットテストで実際に上限を超えて入力できないことを確認済み
- [x] Firebase Storageルール（本人のみアップロード・削除可）
- [x] 身だしなみタブを「蔵」（素材の登録・管理、旧来の内容）と「工房」（旧・和合の内容）の2つにボタン（`SegmentedButton`）で切り替えられるよう再構成。工房は常に3枠を表示し、未使用の枠は「+」付きの白紙カードとして表示、どの枠をタップしても選択画面（作成・編集ダイアログ）が開く
- [x] 身だしなみタブ全体（蔵・工房双方）の表示文言を`Strings`（日本語／British English）経由にし、英語表示に対応
- [x] 素材追加不具合の調査（3回目・原因特定）: ユーザーから具体的な発生状況（アイコンは真っ白のまま表示・ニックネーム/ステメの追加ボタンとカードの保存ボタンを押しても反応がない）を確認できたことで、複数の実バグを特定・修正。(1) ニックネーム・ステメ・工房カードの必須項目が未入力のまま追加/保存を押すと、内部で無言でreturnするだけでダイアログが閉じず何も起きないように見えていた（`fieldRequiredError`によるエラー表示に変更し、ウィジェットテストで再現・修正確認済み）。(2) アイコン・背景画像のサムネイルが`DecorationImage`を使っており、読み込み中・失敗時に何も表示されず「真っ白」に見えていた（読み込み中はスピナー、失敗時は壊れた画像アイコンを表示する`Image.network`ベースの表示に変更）。(3) 実際にアップロードされていたファイルをFirebase Storageから直接確認したところ、image_picker由来の未圧縮フルサイズ画像（3〜10MB超）がそのままアップロードされており、72〜128pxのサムネイル表示のために巨大画像を読み込むことになっていたため「真っ白」に見える一因になっていた（→WebP圧縮を実装、詳細は次項）。(4) デザインテーマの`fontFamily: 'monospace'`指定がCJK（日本語）文字の字幅計算とレイアウトエンジンの推定字幅をズレさせ、「工房」等の複数文字ラベルが折り返される不具合の原因になっていたため削除（プラットフォーム既定フォントへ戻した）。CORS・Firestore/Storageルールは今回別途確認し問題なしだった
- [x] 「ニックネーム・ステメだけ追加できない」の根本原因を特定・修正（4回目）: Firestoreの実データを直接確認し、`icons`/`backgroundImages`にはデータがあるのに`nicknames`/`statusMessages`が常に空配列であることを確認。さらに`activeIconId`が存在しないicon idを指す不整合も発見し、**複数の蔵操作（アイコンのアップロード中にニックネームを追加する等）が重なると、後から完了した書き込みが先に完了した書き込みを消してしまうlost update競合**と判明（`UserRepository.updateUser()`がAppUser全体を`.set()`で丸ごと上書きしていたため）。`UserRepository`に`addToProfileList`/`removeFromProfileList`/`setProfileField`を追加し、Firestoreの`arrayUnion`/`arrayRemove`とフィールド単位の`update()`によるサーバー側での原子的なマージに変更して解消。競合を意図的に再現するウィジェットテスト（遅延ゲート付きフェイクリポジトリ）で修正を確認済み。破損していた実データ（存在しないicon idを指すactiveIconId）もFirestore上で直接修正した
- [x] 画像のWebP圧縮: `flutter_image_compress`を追加し、蔵へのアップロード時にWebPへ圧縮・リサイズ（アイコン512px/背景1080px、quality 80〜85）してから保存するよう変更。Windows/Linuxは同パッケージが未対応のため、非対応環境や圧縮失敗時は元のJPEGバイトのままアップロードするフォールバックあり
- [x] 和合改め「工房」（プロフィールカード作成）: 蔵の素材（アイコン・背景画像・ニックネーム・ステメ）を組み合わせて最大3枚のプロフィールカードを作成・編集・削除できる。独立画面（旧`/profile-creator`ルート）は廃止し、身だしなみタブ内のサブタブとして統合
- [ ] 工房カードの実際の利用場面（プロフィールURL共有時の表示、友達バーのホバーポップアップなど）は未実装。カードの作成機能のみ先行実装した
- [x] 「ニックネーム・ステメだけ追加できない」の根本原因を特定・修正（5回目・真因）: 4回目の競合修正後も「文字を打って追加を押すとダイアログは閉じるが何も起こらない」が再現するとの報告を受け再調査。ニックネーム・ステメ・工房カードのローカルid採番に使っていた`Random().nextInt(1 << 32)`が、Dart VM（`flutter test`の実行環境）では`1 << 32`が正しく4294967296になる一方、**Web（dart2js/DDC）ではビット演算が32bit符号あり整数に切り詰められるため`1 << 32`が0になり、`Random().nextInt(0)`が即座にRangeErrorを投げてsetStateの手前で処理が止まっていた**ことを`dart compile js`で実機（web相当）コンパイルして直接確認・特定。画像素材はFirestoreの`doc().id`でid採番しておりこの経路を通らないため、「画像は瞬時に成功するが文字だけ反応がない」という症状と正確に一致していた。ウィジェットテストがDart VM上で実行されるため、この差異は`flutter test`だけでは検出できない（この種のVM/Web差異は今回のフォント問題と同種のパターン）。ビット演算を経由しない10進リテラル（`Random().nextInt(4294967296)`）に置き換えて解消
- [x] ニックネーム・ステメ行のUI改良: 削除ボタンが`ListTile`のtrailingとして画面右端に配置され、テキストから大きく離れて見えていたのを、`mainAxisSize: MainAxisSize.min`のコンパクトなRow（`_RegisteredItemRow`）に変更してテキストのすぐ右にボタンが来るようにした。あわせて編集ボタンを追加し、既存のニックネーム・ステメを（同じidを保持したまま本文だけ）編集できるようにした（`_NicknameDialog`/`_StatusMessageDialog`に`initialText`パラメータを追加し、追加・編集を共用）
- [x] 工房のプロフィールカードを画面幅いっぱいに広げて表示: 以前は固定サイズ（160x200px）のカードを`Wrap`で画面左端に詰めて表示していたため、広い画面では余白ばかりが目立っていた。常に3枠固定という前提を活かし、`LayoutBuilder`で利用可能幅を3等分してカードサイズを動的に決定するよう変更。「広げるだけでなく大きく表示して」との追加指摘を受け、最大幅クランプを360→640pxに緩和し、アバター半径・パディング・文字サイズもカード幅に応じてスケールするよう調整（大きなカードの中身が寂しく見えないように）
- [x] 身だしなみタブをサイドバー構成に変更（2026-07-24）: 従来の上部`SegmentedButton`（蔵/工房の2択）を廃止し、設定タブと同じ考え方（画面幅760px以上はサイドバー＋内容ペイン、それ未満はカテゴリ一覧→内容のドリルダウン）のサイドバーに変更。蔵・工房に加えて「縁結び」（招待リンク・QRコードでの仲間追加）を3つ目の項目として追加
- [x] 設定タブのアクセントカラーにプリセット（`F08300CC`/`3D2EE0CC`/`88B04Bdd`の3色）を追加。円形スウォッチをタップするとその場で適用され、現在選択中の色にはチェックマークが付く
- [x] 蔵タブ上部のプレビューからニックネーム表示を削除（ステメのみ表示するよう変更）
- [x] 「工房」セグメントボタンの文字が縦積みに折り返される問題を再修正: 日本語（CJK）はスペースを含まないため文字間で自由に改行できてしまい、わずかに幅が足りないだけで「工」「房」のように折り返されて見えることがある。`SegmentedButton`の各ラベル`Text`に`softWrap: false`を付けて単一行描画を強制。さらに「横幅に余裕を持たせれば入らなくなることも無くなる」との指摘を受け、`SegmentedButton.styleFrom(padding:...)`でボタン自体の横幅にも余裕を持たせた。あわせて工房のアイコンを`Icons.auto_awesome_outlined`から金槌（`Icons.gavel`）に変更
- [x] 工房カードの背景画像がカード全体を確実に覆うよう構造を変更: 以前は`Ink`の`decoration`に背景画像を指定し、その`child`（パディング付きのコンテンツColumn）のサイズに実質依存する構造だったため、原理上は全面表示されるはずだが確実性に欠けていた。背景画像・グラデーション・タップ可能なコンテンツをそれぞれ独立したレイヤーとして`Stack(fit: StackFit.expand)`で重ねる構造に変更し、コンテンツ側の内容量に関係なく背景が常にカード全体（角丸の内側いっぱい）を覆うことを構造的に保証した
- [x] 設定タブの左右分割表示（画面幅760px以上）を改良: (1) 詳細パネル上部のフォルダ名ヘッダーは、一覧側で選択中の項目が既にハイライトされ冗長なため非表示に変更（狭い画面のフルスクリーン表示では「戻る」ボタンを兼ねるため引き続き表示）。(2) 詳細パネルの中身が画面幅いっぱいに間延びして見えていたのを、`Align`+`ConstrainedBox(maxWidth: 480)`で中央に幅制限するよう変更
- [x] 設定タブを左右分割レイアウトに変更: 画面幅760px以上では、語らいタブの会話一覧と同様に左に項目一覧（アカウント／デザイン／表示言語／入力／通知）・右に詳細を常時表示し、項目をクリックしても一覧は消えず詳細だけが右側に切り替わるように変更（開いているフォルダはハイライト表示）。760px未満の狭い画面では従来通り詳細をフルスクリーンで開き、戻るボタンで一覧に戻る動作を維持
- [x] 工房カード編集画面をズームイン式の直接編集UIに刷新: カードをタップするとHeroアニメーションでカードが画面中央にズームインし、アイコン・背景画像・ニックネーム・ステメの各要素をタップすると、その場に蔵の登録済み素材一覧（同じ種類のみ）がポップアップし、選んだ素材へその場で差し替えられる構成に変更（旧・ダイアログ形式の`_ProfileCardEditorDialog`を置き換え）
- [x] プロフィールカードの表示内容を整理: カード自体の名前（管理用ラベル）はカード内から外しカード下に表示し、カード内はニックネームを大きく強調・その下にステメを表示する構成に変更
- [x] 蔵/工房切り替えボタンのラベル表示を修正: `FittedBox(scaleDown)`による環境依存の文字縮小をやめ、固定フォントサイズ＋固定幅（140px、「マテリアルボックス」等の長い用語でも省略されない幅）の表示に変更
- [x] 設定タブで表示言語を切り替えると強制的に設定のトップ階層に戻ってしまう不具合を修正: ナビゲーションのパス追跡が翻訳済みタイトル文字列に依存していたため、言語切り替えで文言が変わると一致判定に失敗していたのが原因。翻訳されない安定した`id`で階層状態を管理するよう修正

## 音声通話

- [x] WebRTCによる1対1音声通話（STUNのみ、TURN未導入）
- [x] Firestoreシグナリング（offer/answer/ICE candidate交換）
- [x] 着信の自動検知・着信画面表示
- [x] ミュート・切断
- [x] Android/iOS/macOSのマイク権限設定
- [ ] 実機・複数ブラウザでの実通話の最終動作確認
- [x] 広場（グループ）通話（2026-07-24追加、メッシュ型P2P・参加上限5人）: 1対1の`Call`/`CallRepository`/`WebrtcCallController`/`CallScreen`は変更せず、広場専用に新規モデル・リポジトリ・コントローラー・画面を並行実装した。`GroupCall`（`groupCalls/{id}`、参加上限5人=`kMaxGroupCallParticipants`）・`CallParticipant`（`participants/{userId}`、在室確認ハートビート付き）・`CallPeerLink`（`peerLinks/{pairId}`、ペア単位のoffer/answer。辞書順で小さい方のuserIdが常にオファー役という決定的ルールでglareを回避）の3モデルを新設（`lib/models/group_call.dart`）。`GroupCallRepository`（`lib/repositories/group_call_repository.dart`）でN(N-1)/2組ぶんのシグナリングを扱う。`WebrtcGroupCallController`（`lib/features/call/webrtc_group_call_controller.dart`）は`Map<String, RTCPeerConnection>`で複数ピアを管理し、`participants`の差分を監視して参加者の増減に応じて接続を動的に張り直す。`GroupCallScreen`（`lib/features/call/group_call_screen.dart`）はビデオ通話時グリッドレイアウト・音声のみ通話時はアバター一覧で表示。共通の丸型コントロールボタンは`call_controls.dart`に切り出し1対1画面と共用。`GroupChatPane`のAppBarに通話/ビデオ通話ボタンを追加し、進行中の通話がなければ新規開始、あれば（満員でなければ）参加する形にした（`activeGroupCallProvider`で監視）。**TURNサーバーは自前coturn運用の方針で決定**し、`WebrtcGroupCallController`のICEサーバー設定は`String.fromEnvironment`経由のURL・認証情報を組み込み済みだが、**coturn自体のVM構築・DNS・証明書等のインフラ構築はまだ未実施**（別途対応が必要）。Firestore/Storageルールの本番反映（`firebase deploy`）も未実施。実機・複数人での実通話動作確認は未実施
- [x] ビデオ通話（720p、1対1）: `Call`モデルに`isVideo`フラグを追加し、`getUserMedia`のvideo制約（ideal 1280x720）・ローカルカメラプレビュー（`RTCVideoRenderer`をローカル/リモート2本に）・カメラのオン/オフ切替を`WebrtcCallController`に実装。`CallScreen`はビデオ通話時のみ相手の映像を画面全面に敷き、右上に自分のプレビュー、上部に名前・状態をオーバーレイする専用レイアウトに分岐（音声のみ通話の既存レイアウトは変更なし）。チャット画面のAppBarに音声通話ボタンとは別にビデオ通話ボタン（`Icons.videocam_outlined`）を追加。Android（`CAMERA`権限）・iOS/macOS（`NSCameraUsageDescription`・`com.apple.security.device.camera`）にカメラ権限設定を追加
- [x] 標準ノイズ抑制（`echoCancellation`/`noiseSuppression`/`autoGainControl`をgetUserMediaに明示指定）
- [ ] RNNoise本格統合（フェーズ3・保留）: 調査の結果flutter_webrtc/libwebrtcはマイク→エンコーダ間の生音声サンプルに介入するフックをDart層に公開しておらず、プラットフォームごと（Web=AudioWorklet+WASM、Android/iOS/Windows/Linux/macOS=ネイティブAPMカスタムプロセッサ）のネイティブ音声パイプライン改造が必要な、Dartコードのみでは完結しない大規模な別プロジェクトと判明。2026-07-23時点でユーザー確認の上、標準ノイズ抑制で当面保留とし、本格対応はフェーズ3で別途着手する方針に決定
- [ ] TURNサーバー導入（NAT越え対応、自前coturn運用で2026-07-24決定）: 対称NAT/CGNAT環境同士だとSTUNだけでは直接接続できず通話が繋がらないため、両者の間に立って中継するTURNサーバーが必要。SaaS型TURN（Twilio等）ではなく、プライバシーファースト方針との整合とコスト予測のしやすさから自前でVMにcoturnを構築し運用する方針を決定済み。ただしFirebase以外のインフラ（VM・DNS・TLS証明書等）の構築は本件は後回しとし、現時点では未着手。アプリ側は`WebrtcGroupCallController`に`String.fromEnvironment('DAIDAI_TURN_URL'/'DAIDAI_TURN_USERNAME'/'DAIDAI_TURN_CREDENTIAL')`で設定を読み込むフックのみ用意済み（値が空ならSTUNのみで動作し、対称NAT同士の接続は失敗しうる）
- [ ] 画面共有

## UIスタイル・デザイン

- [x] アクセントカラーのカラーコード自由入力（`#RRGGBB`/`#RRGGBBAA`最大8桁、SharedPreferences永続化）
- [x] カラーコード入力欄の「#」固定表示・16進数以外の文字を入力不可に制限
- [x] 標準アクセントカラーを`#F08300`に設定
- [x] 8桁カラーコード（`RRGGBBAA`）の透明度（末尾2桁）をColorのARGB整数へ誤った桁位置で渡していたバグを修正（`f08300`と`f08300ff`が別の色になっていた）。`test/color_hex_test.dart`に回帰テストあり
- [x] `ThemeData.fontFamily: 'monospace'`指定を削除（プラットフォーム既定フォントへ戻した）: CJK文字の字幅計算とレイアウトエンジンの推定字幅がズレ、「工房」など複数文字の日本語ラベルだけ折り返される不具合の原因になっていた。CLAUDE.md記載の意図する等幅フォント（BIZ UDPMincho/BIZ UDGothic）はまだアセット未取得のため、導入時は日本語グリフの字幅を要確認
- [x] ログイン画面のボタン幅を最大400pxに制限（`ElevatedButtonThemeData.minimumSize`が`Size.fromHeight`＝横幅無制限のため、外側に幅の制約がないと画面いっぱいに広がっていた）
- [x] ボタン・カードの「浮いている」elevation/shadow表現
- [x] 画面遷移のポップ・スライドアニメーション
- [x] モバイルのスワイプナビゲーション: 以前は下部の丸いチップの帯（高さ数十px）にしかスワイプ判定が無く、画面のほとんどを占める本文をスワイプしても反応しなかった。本文全体（`IndexedStack`）を覆う`GestureDetector`に変更し、画面のどこをスワイプしてもタブが切り替わるようにした


## 設定・運営

- [x] 設定タブのフォルダ階層化（アカウント／デザイン／表示・言語／入力／通知）→ 後に`docs/マップ.md`のサイトマップに合わせて再構成（下記参照）
- [x] 設定タブの横幅を最大480pxに制限し、上部余白・カード間の余白を追加（余白不足・横幅を無駄に使っている点の改善）
- [x] 表示言語の切り替え（日本語／British English、SharedPreferences永続化）
- [x] メッセージ送信キーの切り替え（Enterで送信・Shift+Enterで改行 / Enterで改行・Ctrl+Enterで送信）。実際のキー処理をウィジェットテストで検証済み（`test/chat_send_key_test.dart`）
- [x] 相手に通知せず送信（Enterで送信モード: Ctrl+Enter／Ctrl+Enterで送信モード: Ctrl+Shift+Enter／送信ボタン長押し）。`Message.silent`フラグとして保存し、送信済みメッセージには🔕アイコンを表示。実際の通知抑制はFCM基盤が未実装のため将来の接続ポイントとして先行実装
- [x] 設定タブ「入力」の送信キー選択肢に、通知せず送信のショートカット（Ctrl+Enter／Ctrl+Shift+Enter）を`subtitle`として明記（機能自体は先に実装済みだったが、設定画面のUIテキストへの反映を失念していた）
- [ ] 表示言語切り替えの全画面対応（現状はナビ・語らいタブ・設定タブなど主要文言のみ。それ以外は日本語ハードコードのまま）
- [x] 用語・表示設定（世界観重視／利便性重視の言い換え軸）: `docs/マップ.md`の対訳表（言語×用語スタイルの2×2）を`lib/l10n/vocabulary.dart`の`Vocabulary`クラスとして実装。`TerminologyStyle`（`lib/l10n/terminology_style.dart`）を表示言語（`AppLocale`）とは独立した軸としてSharedPreferences永続化し、設定タブ＞アプリケーション＞言語のチップから切り替え可能にした。蔵/工房・一対/広場等のUI文言に反映済み（`vocabularyProvider`経由）。用語スタイルによって文言の長さが変わりうる（例:「マテリアルボックス」「Terminology & display」）ため、セグメントボタンには横幅の余裕を持たせ、選択肢はWrapで折り返し可能にしている
- [x] 設定タブをマップ.mdのサイトマップに合わせて全面再構成: トップレベルを「アカウント／アプリケーション／入力／通知」の4フォルダに整理し、アカウント（Rhing ID表示・プロフィール名・セキュリティ[パスワード/2段階認証/パスキー]・QRコードによるログイン・ログアウト・アカウントの削除）、アプリケーション（色・UI・文字[フォントデザイン/フォントサイズ]・言語）という多段階層（最大3階層: 例 アカウント＞セキュリティ＞パスワード）をサポートする汎用ツリーナビゲーション（`_Node`/`_NodeListDetail`）を新設。未実装項目はすべて「準備中」を表示するリーフとして配置。バックアップコード・メールアドレスの項目は、CLAUDE.mdの明記された不採用方針（バックアップコードの代わりに秘密の質問3つを採用／プライバシー方針上メールアドレス非収集）に従い、2026-07-24にユーザー確認の上で意図的に含めていない。多段ナビゲーションの内側ListView（例: 色フォルダのアクセントカラーピッカー）が無限高さ制約でクラッシュしないことを含め`test/settings_navigation_test.dart`で検証済み
- [x] 運営タブを`docs/マップ.md`通り（ホームページのURL／お知らせ／質問フォーム）の3項目リストに再構成。正式なホームページURLがまだ確定していないため、いずれも準備中ダイアログを表示する（実在しないURLを決め打ちで埋め込むことは避けた）
- [ ] 通知タイミング・再通知間隔の設定（FCM通知基盤自体が未実装のため後続タスク）
- [x] ログアウト

## インフラ・デプロイ

- [x] Firestore/Storageセキュリティルールのデプロイ
- [x] Vercelへのデプロイ（`vercel.json`、Flutter SDKをその場でビルド）
- [x] Vercelデプロイブロック問題の解消（git commit authorをVercel連携アカウントに統一）
- [x] Firebase Storageバケットのcors設定（`storage.cors.json`）: デフォルトではバケットにcors設定が一切無く、`firebasestorage.googleapis.com`の画像ダウンロードURLに`Access-Control-Allow-Origin`が付与されないため、Flutter Web（CanvasKit）がfetchで画像バイトを読めず、蔵の画像が「真っ白」→（エラー表示追加後は）「読み込み失敗アイコン」になっていた。`gsutil cors set storage.cors.json gs://daidai-rhing.firebasestorage.app`相当の設定をGCS JSON APIで適用して解消（`storage.rules`とは別物。`firebase deploy`では管理されないため、バケットを作り直す場合は再設定が必要）
- [x] Firebase Authの認証済みドメインにVercel本番ドメインを追加
- [ ] rhing.jp（ホームページ）との統合（DaiDai完成後に着手予定）

## その他・未確認事項

- [x] スパム対策: 仲間承認制（友達申請フローで実装済み、上記「一対（DM）」参照）
- [ ] スパム対策: レート制限・メタデータ分析（未着手）
- [ ] 既読管理
- [ ] E2E暗号化（Signal Protocol） ※フェーズ2、専門家レビュー必須
- [ ] 極みプラン ※フェーズ2
- [ ] ペタピタ ※フェーズ2
- [ ] 安否確認・ロール管理 ※フェーズ3
- [ ] CLAUDE.mdと`docs/マップ.md`の未解決の食い違い3点（談論／バックアップコード／メールアドレス）の確認・解消
