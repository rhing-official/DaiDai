# ペタピタ定額課金（貼プラン）実装プロンプト

> 2026-09-07決定。実装はまだ行わず、方針とプロンプトのみをここに残す。**着手条件**（下記）を満たしたタイミングで、下部の「実装プロンプト」節をそのままAIに渡して着手すること。

## 決定事項の要約

ペタピタ（スタンプ）の定額課金モデルとして、以下の2案を検討した。

- **案A: プール型** — 単一の月額プランに登録された全ペタピタパックが使い放題になる
- **案B: クリエイター個別型** — クリエイターごとに自分で月額プランを設定（任意、月額制をしない選択も可）

調査の結果、以下が判明した:

- 企画書フェーズ4に既に「**貼プラン（ペタピタ使い放題）**」という構想があり、案Aに相当する
- ペタピタの有料販売自体が現在**凍結中**（`functions/src/index.ts`の`PAID_PACKS_ENABLED = false`、技術仕様書7.6節）。理由は特定商取引法表記・返金ポリシー未整備・Stripe本番未有効化に加え、**クリエイターへの収益分配方式（Stripe Connect等）が未決定**なこと
- 現行の決済実装は決済額が全額Rhing自身のStripeアカウントに入金される構成で、第三者クリエイターへの直接分配の仕組みはコード上どこにも存在しない
- 「極み」プラン（月額サブスクの基盤そのもの）もアプリ側は未実装（設計メモのみ、Obsidian技術仕様書7.1〜7.3節に疑似コードのみ）
- 案A（プール型）は「全額Rhingに入金→按分計算で還元」という現行構成のまま拡張できるが、案B（クリエイター個別型）はStripe Connect導入が前提になり、技術仕様書7.6節が「未解決」と明記する論点に直結する

**採用: 案A（プール型、貼プラン）**。クリエイター個別型（案B）は将来検討の対象に留め、今回のスコープには含めない。

プロンプトのスコープは**ペタピタサブスクの設計に限定**する。有料販売凍結の解除条件そのもの（特商法表記ページ等）と、極みプランのサブスク基盤構築は、別タスクとして切り離す（下記スコープ外を参照）。

## 着手条件

**まだ作らない。** クリエイター・ペタピタパックの登録数がまだプラットフォームに集まっていない段階で定額課金の実装に着手するのは時期尚早、というユーザー判断による。以下のような状態になってから着手を検討する:

- ペタピタパック・クリエイターが一定数プラットフォームに登録されている（プール型は「使い放題にする対象」がある程度無いと価値が出ないため）
- 着手前に、下記「前提となる現状のコード構成」に記載の状態（`PAID_PACKS_ENABLED`フラグ・極みプラン実装状況・有料販売凍結の解除状況）が変わっていないか再確認すること。特に極みプランのサブスク基盤が先に実装されている可能性がある

## 前提となる現状のコード構成（2026-09-07時点）

実装着手時にすぐ参照できるよう、現状のペタピタ関連実装をまとめる。

- **モデル**（`lib/models/sticker.dart`）: `StickerPack`（`packId`, `creatorId`, `name`, `price`, `stickers[]`, `category`, `tags[]`, `salesCount`, `createdAt`）と`Sticker`（`stickerId`, `name`, `imageUrl`, `roles[]`）の2階層。所有権はパック側ではなく`users/{uid}/ownedStickerPacks`サブコレクションで別管理
- **リポジトリ**（`lib/repositories/sticker_repository.dart`）: `watchPacks()`, `watchOwnedPacks()`, `uninstallPack()`, `findPackIdForSticker()`, `ownsPack()`, `watchRoles()`。購入自体はここになく、購入導線はWeb側（`daidai横丁` = `HomePage-Rhing`リポジトリ）が担当
- **Cloud Functions**（`functions/src/index.ts`）: `createStickerPack`/`updateStickerPackMeta`/`addStickersToStickerPack`（画像は追加専用）/`deleteStickerPack`/`createStickerPackReport`。購入付与フローは`grantOwnership()`（内部関数）/`uninstallStickerPack`/`createCheckoutSession`（無料パックは即時付与、有料パックはStripe Checkout Session作成）/`stripeWebhook`（`checkout.session.completed`受信で`grantOwnership`呼び出し）
- **`PAID_PACKS_ENABLED`フラグ**（`functions/src/index.ts`、現在`false`）: 有料パックの出品・価格変更・購入を一律禁止。再開には技術仕様書7.6節の4項目（特商法表記・返金ポリシー・収益分配方式決定・Stripe本番アカウント有効化）が必要
- **極みプラン・`Purchase`モデル**: アプリ側は未実装（`lib/models/`・`lib/repositories/`・`functions/src/index.ts`いずれにも該当フィールド無し）。Obsidian技術仕様書7.1〜7.3節にStripe Webhookの疑似コード（`activateSubscription`/`deactivateSubscription`、`users/{uid}.subscriptionPlan='kiwami'`）のみ存在
- **クリエイターへの収益分配**: 未実装。決済は全額Rhing自身のStripeアカウントに入金される構成（Stripe Connect等の第三者分配の仕組みは無し）
- 競合調査.mdには、スタンプ課金モデル自体を他社比較した調査は**まだ無い**（2026-09-05エントリはサジェスト機能の調査で、課金モデルは副次的な一文のみ）

## 実装プロンプト（着手時にそのまま使う）

> 以下を次回セッションのAIにそのまま渡して着手する。

```
DaiDaiに「貼プラン」（ペタピタ定額使い放題サブスク）を実装したい。

## 決定済み方針
- プール型: 単一の月額プランに加入すると、そのプランに「登録された」ペタピタパックが使い放題になる
- クリエイターは自分のパックを貼プランに登録するかどうか任意で選べる（登録＝オプトイン、しない選択も可）。「使える」の範囲は、クリエイターが選んで貼プランに登録したパックのみ（未登録のパックは対象外で、従来通りの無料/買い切り販売と併存できる）
- 収益分配は全額Rhing運営のStripeアカウントに入金される現行構成を維持し、クリエイターへの還元はプール収益の按分計算で行う（Stripe Connect等によるクリエイターへの直接分配は本タスクのスコープ外）

## スコープ外（この実装では扱わない）
- クリエイター個別課金（クリエイターごとの独自サブスク、Stripe Connect導入）は将来の拡張候補として企画書・技術仕様書に書き添えるに留め、実装しない
- 有料ペタピタ販売の凍結解除そのもの（特定商取引法に基づく表記ページ・返金ポリシー等）は前提条件の確認事項に留め、このタスクでは扱わない。ただし貼プラン自体を有効化するには`PAID_PACKS_ENABLED`とは別に、貼プラン加入者への「使い放題」提供が実質的な有料コンテンツ提供にあたるため、着手前に運営（ユーザー）へ凍結解除状況を確認すること

## 検討・設計してほしい論点
1. データモデル拡張: `StickerPack`への登録フラグ追加（例: `poolPlanEnrolled`）か、別コレクション（例: `poolPlanEnrollments`）で管理するか
2. 還元計算の按分基準: プール収益をクリエイター間でどう按分するか。現状`salesCount`（買い切り販売数）はあるが、「使い放題プラン内での利用」をどう計測するか自体が未設計（送信回数・ダウンロード回数等の候補を検討し、計測の実装コストとのバランスで決める）
3. 月額課金のStripe実装: 既存の買い切り用`createCheckoutSession`（`functions/src/index.ts`）とは別に、Stripeの定期支払い（Subscription）フローが必要。極みプラン（未実装、¥300/月）とインフラを共有できるか、独立させるかも検討する
4. UI: ペタピタピッカー・daidai横丁双方での「貼プラン対象パック」の見せ方、クリエイター向け管理画面（`HomePage-Rhing`リポジトリ側の`/daidai-yokocho/creator`）への貼プラン登録・解除導線
5. 解約・日割り・無料体験の扱い: 極みプランで検討されている「1ヶ月無料体験」との整合（技術仕様書7.3節参照）

## 実装前に行うこと
- `market-research`サブエージェントで、他社のスタンプ使い放題プラン（LINEスタンプ使い放題等）の課金モデル・還元設計を調査し、`競合調査.md`に追記する（競合調査.mdにはこの観点の調査がまだ無いことを2026-09-07時点で確認済み）
- 着手時点で`functions/src/index.ts`の`PAID_PACKS_ENABLED`フラグ・極みプランの実装状況を再確認し、この実装プロンプトの前提が変わっていないか確認する

## 検証観点
- `flutter analyze`・`flutter test`
- Stripeサンドボックスでのend-to-end確認（既存の買い切りパック検証手順に準ずる）
- クリエイターの貼プランへのオプトイン/オプトアウトが即座に反映されること
- 貼プラン未加入ユーザーには対象外パックが従来通り（無料/買い切りのまま）扱われること
```
