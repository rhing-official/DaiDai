# TODO
- [ ] `firebase deploy --only functions`で今回のCloud Functions変更（通知ペイロードへの`conversationId`/`roomId`追加、未読件数加算`incrementUnreadCounts`）を本番反映する
- [ ] 次回`flutter build web`デプロイ後、`web/firebase-messaging-sw.js`の通知タップ時ディープリンクが本番Webで動作するか確認する
- [ ] 通知タップで該当の語らいまで開く動作を実機（Android・Web）で確認する
- [ ] 語らい一覧の未読バッジ・最新メッセージプレビュー・時刻表示（今日→時刻/日付が変わると日付表示に切替）を実機で確認する。特に日付をまたぐ表示切替は端末時刻変更等での確認がまだ
- [ ] ガラスUIの各種ポップアップ（19箇所修正）が実際に透明化せず表示されるか目視確認する
- [ ] モバイル幅・寄合複数モードの広場でハンバーガーメニューから「広場自体の設定」を開けるか実機確認する

---

## 2026-09-02

### 実装内容
- **モバイル操作性の改善**（`lib/widgets/swipe_gestures.dart`・`lib/features/chat/chat_panes.dart`・`lib/features/chat/chat_screen.dart`ほか）: 画像のピンチズームの不具合修正、メッセージ・語らい一覧の長押しドラッグ選択メニューの改善を実施
- **語らい一覧の並べ替え機能を新規追加**: `lib/models/conversation_sort_order.dart`（新規）・`lib/providers/conversation_sort_order_provider.dart`（新規）・`lib/utils/kana_sort.dart`（新規、五十音順ソート）で「最近会話した順／五十音順／未読優先」を切り替え可能にし、`app_user_preferences.dart`/`user_preferences_sync.dart`で設定を永続化・複数端末同期
- **ガラスUIポップアップの透明化バグ修正**: `GlassAlertDialog`でラップされず生の`AlertDialog`を直接使っていた19箇所（`group_leave_dialog.dart`・`group_delete_dialog.dart`・`two_factor_setup_dialog.dart`・`group_invite_dialog.dart`・`severance_dialog.dart`・`qr_login_dialog.dart`・`group_member_list_screen.dart`・`conversation_profile_card_dialog.dart`・`group_profile_card_screen.dart`・`album_detail_screen.dart`・`album_picker_sheet.dart`・`album_popup_content.dart`・`owned_sticker_packs_popup.dart`・`admin_panel_screen.dart`・`chat_screen.dart`）を`isGlass`分岐＋`GlassAlertDialog`に修正
- **プッシュ通知タップで該当の語らいまで開く機能を実装**: Cloud Functions（`functions/src/index.ts`）の通知ペイロードに`conversationId`/`roomId`/`isDm`を追加。`go_router`にID単体で解決する新ルート`/chat/dm/:dmId`・`/chat/group/:groupId`（新規`lib/features/chat/notification_chat_opener.dart`の`NotificationChatOpener`が寄合・メンバーシップを解決）を追加。Android側は`flutter_local_notifications`のタップコールバック・cold start時の`getNotificationAppLaunchDetails()`、Web側は`web/firebase-messaging-sw.js`の`notificationclick`ハンドラでディープリンクを実装
- **モバイル・寄合複数モードの広場で語らい設定を開くボタンが無い不具合を修正**: `GroupSettingsPopup`を開く処理を`showGroupSettingsDialog`関数として切り出し（`group_settings_popup.dart`）、ハンバーガーメニュー（`chat_panes.dart`の`_GroupMenuButton`）に「広場自体の設定」項目を追加
- **語らい一覧タイルに未読件数・最新メッセージプレビュー・最終送信時刻を追加、広場の人数表示は削除**: `ConversationPrefs.unreadCount`を新設し、Cloud Functions（`onDmMessageCreated`/`onGroupMessageCreated`から呼ぶ新規`incrementUnreadCounts`）が受信者の未読件数を加算、`ConversationPrefsRepository.setLastRead`が既読時に0リセット。`DirectMessage`/`Group`に`lastMessageContentType`/`lastMessagePreview`を追加し、全メッセージ送信メソッド（DM・広場ともテキスト/添付/スタンプ/通話サマリー）で更新。`lib/utils/message_time.dart`に`formatConversationListTime`（今日なら時刻のみ、それ以外は月/日のみ）を新設。`talks_tab.dart`の`_DirectMessageTile`/`_GroupTile`を再構成し、`_ConversationIndicators`を`_ConversationTrailing`に改称して時刻・未読バッジを追加、広場の`'${人数}人'`表示は3スタイルとも削除（ユーザーとの相談の上、情報過多を避けるため一覧からは外す方針で決定）

### 詰まったところ
- ガラスUI修正で`AlertDialog`の引数をローカル変数に切り出す際、閉じ括弧の数を繰り返し数え間違え、特に`two_factor_setup_dialog.dart`で8回前後の修正失敗（プレースホルダーの誤挿入も含む）が発生。ブロック全体を一括で正しい形に書き換えてから`flutter analyze`で検証する方式に切り替えて解決
- 語らい一覧タイルの書き換えで、`Edit`ツールのold_stringに半角括弧`()`を使ったが実ファイルは全角括弧`（）`のコメントだったため置換が繰り返し失敗。該当行を`Read`で再確認し全角に修正して解決

### 次にやること
- `firebase deploy --only functions`で今回のCloud Functions変更（通知ペイロード拡張・未読件数加算）を本番反映する
- 次回`flutter build web`デプロイ後、通知タップのディープリンクがWeb本番で動作するか確認する
- 通知タップの語らいオープンをAndroid・Web実機で確認する
- 語らい一覧の未読バッジ・最新メッセージプレビュー・時刻表示（日付をまたいだ際の表示切替含む）を実機確認する
- ガラスUIの各種ポップアップが透明化せず表示されるか目視確認する
- モバイル幅・寄合複数モードの広場でハンバーガーメニューから「広場自体の設定」を開けるか実機確認する

## 2026-09-01

### 実装内容
- **Node.js 20→24への更新**: `functions/package.json`の`engines.node`・`firebase.json`の`functions[0].runtime`を`nodejs20`→`nodejs24`に変更。ローカルのnvmデフォルトも`nvm install 24`→`nvm alias default 24`でNode 24.20.0に切り替え。Google Cloud側のNode 20ランタイムが2026-04-30に非推奨化済み・2026-10-30に廃止予定と判明したための対応（Node 22 GA/Node 24 GAの両選択肢からユーザー判断で最新LTSのNode 24を選択）
- **firebase-functions 6.3.1→7.3.2への更新**: GitHubのv7.0.0リリースノートを確認し、破壊的変更（`functions.config()`廃止・Node16未満サポート終了・v1 `Event`→`LegacyEvent`改名等）はいずれも`functions/src/index.ts`の実装（v2 API・`defineSecret`のみ使用）に該当しないことを確認した上で実施。コード変更不要でビルド・デプロイとも成功
- **安全なマイナー/パッチ更新一式**: functions側`stripe`を22.5.0→22.6.0（`npm update`）、Flutter側は`flutter pub upgrade`で`pubspec.yaml`の制約内に収まる56パッケージ（Firebase SDK群`cloud_firestore`/`firebase_auth`/`firebase_storage`等、`flutter_riverpod`/`riverpod`、`flutter_webrtc`、`video_player`等）を更新。更新前後で同じ4件のテスト失敗（既存の問題、今回とは無関係）であることを`git stash`で切り分けて確認済み
- **メジャーバージョン更新4件**（事前にExploreサブエージェント3件を並列起動して実コード上の影響範囲を調査し、各パッケージのCHANGELOGで破壊的変更を裏付けた上で着手）:
  - `google_fonts` 6.3.3→8.2.1: 使用箇所は`lib/main.dart`（`GoogleFonts.config.allowRuntimeFetching = false`）と`lib/theme/gekiga/gekiga_theme.dart`（`GoogleFonts.anton()`、バンドル済みローカルフォント使用）の2箇所のみ。`Config`クラスは8.2.0で`GoogleFontsConfig`に改名されたが旧名は非推奨のまま存続しておりコード変更不要
  - `go_router` 17.3.0→18.0.0: `lib/router/app_router.dart`は`ShellRoute`/`redirect`等の高度機能を使わない単純な構成で、18.0.0の破壊的変更（Flutter 3.44/Dart 3.12以上が必須）は既に満たしておりコード変更不要
  - `file_picker` 3.0.4→12.1.2（**要コード変更**）: v11.0.0で`pickFiles()`の返り値が`FilePickerResult`から`List<PlatformFile>`/`PlatformFile?`に変更されキャンセル時の挙動も`null`から空リストに変化、v12.0.0で`withData`パラメータが非推奨化され`PlatformFile.readAsBytes()`方式に変更。唯一の呼び出し箇所`lib/features/chat/attachment_popup_button.dart`の`_pickFile()`を、pub-cache内の実際のパッケージソースでAPIを確認した上で`FilePicker.platform.pickFiles(withData: true)` + `result?.files.single.bytes`方式から`FilePicker.pickFile()` + `await file.readAsBytes()`方式に書き換え
  - `firebase-admin` 12.7.0→14.3.0・`typescript` 5.7.2→7.0.2: `functions/src/index.ts`（全機能がこの1ファイル）は既に`admin.*`旧名前空間を一切使わずモジュラーAPIのみで記述、`enum`/`decorator`/`any`も未使用の手堅い書き方だったためコード変更不要。`firebase-functions@7.3.2`のpeerDependencyも`firebase-admin@^14.0.0`を許可済み
- 各段階で`flutter analyze`/`flutter test`/`npm run build`を実行し、`npx firebase-tools deploy --only functions`で本番の全21関数を都度デプロイ（Node24化時・firebase-functions v7化時・firebase-admin v14化時の計3回）。ユーザーによるWeb版実機確認（ファイル送信・劇画UI表示）でも問題なしと確認済み
- **`functions/`の`npm audit`moderate脆弱性7件は対応不要と判断**: `uuid <11.1.1`のバッファ境界チェック不備が`gaxios`/`teeny-request`経由で`firebase-admin`の奥深くに埋まっている構造で、`npm audit fix --force`は`firebase-admin`を10.3.0へダウングレードする提案になる。今日Node 24対応のために14.3.0へ上げたばかりの内容を巻き戻すことになり明らかに割に合わない上、severityもmoderate・実際の攻撃条件（非暗号用途でのuuid生成時のバッファ処理）も限定的なため、現状のまま許容する
- **ルートの`package.json`（`api/`配下、Vercel Functions＝OGP画像生成）の依存更新**: `node_modules`未インストールだったため`npm install`してから`npm outdated`を実施。`sharp` 0.33.5→0.35.4（High severityのlibvips由来脆弱性CVE-2026-33327ほか4件を解消）、`@vercel/og` 0.6.8→**0.11.1**（`npm outdated`上の最新1.0.2ではなくあえて1つ前の系統）に更新。理由: 実際に両バージョンをローカルで動かして検証したところ、1.0.2はSatori 0.33.0で追加されたHarfBuzzテキストシェイピング機能（`harfbuzzjs`）のNode.js Runtimeビルドが`Error: Dynamic require of "fs" is not supported`でモジュール読み込み時点でクラッシュすることが判明した（esbuildバンドル時にCJSの`require`がESM用シムに置き換わり、動的requireを弾く典型的な不具合とみられる）。`api/og-image.mjs`はNext.js等のビルドツールを介さず素の`.mjs`としてVercelにデプロイされる構成のため、この不具合はそのまま本番でも再現するはずと判断し見送った。0.11.1は`ImageResponse`のコンストラクタ・オプション（`width`/`height`/`fonts`）が0.6.8と型・挙動とも変わっておらず、`og-image.mjs`の非JSXプレーンオブジェクトツリーもそのまま動作することを、型定義diffと実際にNode.jsで動かすテストの両方で確認した。また0.11.1は`sharp`を`optionalDependencies`（`^0.34.5`、内部でのPNGラスタライズに任意で使用）として持ち、指定と噛み合わないままだと`node_modules/@vercel/og/node_modules/sharp`に脆弱性のある0.34.5が別途インストールされてしまうため、`package.json`に`overrides.sharp: "^0.35.4"`を追加して単一バージョンに強制。最終的に`npm audit`は0件になった
- `api/`配下のコード（`og-image.mjs`/`link-preview.js`/`url-preview.js`）自体の変更は無し（API互換のため）。ローカルで`sharp`のPNG変換と`@vercel/og`の`ImageResponse`生成を`og-image.mjs`と同じ非JSXツリー形式で疎通確認済みだが、Vercel実デプロイでの確認はまだ（下記「次にやること」）
- **Googleカレンダー同期機能のUI刷新**: 設定＞カレンダー同期・設定＞通知の行を、`ListTile`+`onTap`から`SwitchListTile`（劇画は既存の`_DraftSyncFolder`と同じ`GekigaJointedTileList`パターン）のトグル方式に変更。カレンダーアイコンから開く画面自体も、予定追加ポップアップだけの簡易UIから、予定のある日を塗り潰す月表示グリッド（`_MonthGrid`/`_DayCell`）に刷新し、日付タップで「予定が無ければ即追加・あれば近くに浮かせるポップアップで一覧表示」という挙動にした。当初「全画面表示」で実装したが、`Navigator.push`が単一ルートNavigatorの上に乗って友達一覧・寄合一覧のサイドバーごと覆ってしまう問題をユーザーに指摘され、既存の`EmbeddedCallPane`（通話画面用のローカル`bool`トグルでペイン内容を差し替える設計）と同じパターンに作り直し、`CalendarPaneView`として`DmChatPane`/`GroupChatPane`のメッセージ表示領域内にのみ収まるよう修正した
- **カレンダーUIの追加調整**: 日程一覧をボトムシートから、タップした日付近くに浮かせる`showMenu`+`RelativeRect`ベースのポップアップに変更（既存のアルバムポップアップ等と同じ「ポップアップ」慣習に合わせた）。右上の＋ボタンを削除、`LogicalKeyboardKey.arrowLeft`/`arrowRight`と横スワイプ（横方向のみの`GestureDetector`を別レイヤーに分離、下スワイプでのメッセージ画面復帰は既存の`SwipeDownToDismiss`のまま）での月移動を追加
- **Googleカレンダー連携のOAuth設定**: `GoogleCalendarAuthService`にkIsWebかつWeb Client ID未設定時専用の`GoogleCalendarNotConfiguredException`を追加し、生の`google_sign_in_web`アサーションエラーの代わりに案内文言を表示するよう修正。ユーザーがFirebase Console（Authentication＞Sign-in method＞Google＞ウェブSDK構成）から取得したWeb Client IDを`_webClientId`に設定した
- **仲間→友達 表記統一**: `docs/マップ.md`の独自用語表に無い一般表記と確認した上で、`strings.dart`の5箇所（縁結び招待リンク・QRコード説明文、招待確認画面のタイトル・本文・送信ボタン）と関連ドキュメントコメント3箇所・CLAUDE.md記述2箇所を「友達」に統一
- **絶縁後も「すでに友達です」と出るバグの修正**: ユーザーから両側で絶縁完了済み（提案→相手が「同意する」まで確認済み）の相手なのに新規申請時に「すでに友達です」と出る報告を受け調査。友達判定ロジック自体（`isFriend`が`friends`サブコレクションの実在ベース）は既に健全だったが、`DirectMessageRepository.acceptSeverance`が「時間のかかるメッセージ・寄合の削除ループ」の後に「友達関係を実際に終わらせるcascadeBatch（friends・friendRequests削除）」を実行する順序になっており、削除ループ途中でタブを閉じる等の中断があると会話は消えて絶縁完了に見えても友達データが残る欠陥を特定。firestore.rulesを確認し、friends/friendRequestsの削除条件がメッセージ削除完了に依存しないことを確認した上で、cascadeBatchを最優先・最初に実行する順序に入れ替えて修正した

### 詰まったところ
- file_pickerのメジャー更新後、起動済みの`flutter run -d web-server`プロセスにホットリスタート（`kill -SIGUSR2`）をかけたところ、Web用プラグイン登録ファイル（`web_plugin_registrant.dart`）が旧file_picker内部パス（`file_picker_web.dart`）を参照したままで`Error when reading ... No such file or directory`が発生。ホットリスタートでは解決せず、プロセスを完全に終了して`flutter run`をやり直す必要があった
- `@vercel/og`の最新版（1.0.2）へ素直に更新すると、ローカル検証で初めて発覚したHarfBuzz関連のESM/Node.js Runtime非互換でモジュール読み込み時点からクラッシュする不具合があった（詳細は上記）。`npm outdated`の表示だけを見て最新版へ更新すると本番のOGP画像生成が全滅していたはずで、必ず実際に動かして確認する必要があった
- カレンダーUIの「全画面」実装で、単一ルートNavigatorへの`push`がサイドバーごと画面全体を覆ってしまう設計ミスをユーザーに指摘され作り直した（「カレンダーは全画面とは言ったが、メッセージ画面の範囲にとどめてほしい」）。複数回のウィジェットツリー再構成の過程で、括弧の対応崩れによる一時的な構文エラーが繰り返し発生し、都度該当箇所を読み直して修正した
- Googleカレンダー連携で、Web Client ID設定後にGoogleのOAuth同意画面から`origin_mismatch`（エラー400）が返された。原因はGCP ConsoleのOAuthクライアントID側で「承認済みの JavaScript 生成元」に開発中のオリジン（`http://localhost:8765`等）が未登録なことによるもので、これはコード側の問題ではなくユーザー側でのGCP Console一回限りの追加設定が必要（未解消、下記「次にやること」）

### 次にやること
- ルートの`package.json`（`api/`配下、Vercel Functions＝OGP画像生成）の`@vercel/og`/`sharp`更新後、実際のVercel本番デプロイ後にOGP画像生成（招待リンク・広場招待リンクのog:image）が壊れていないか実機確認する（ローカルでは`api/og-image.mjs`相当のコードをNode.jsで直接実行して疎通確認済みだが、Vercelの実デプロイ環境での確認はまだ）
- Googleカレンダー連携のOAuth `origin_mismatch`エラーを解消する（GCP Consoleで承認済みJavaScript生成元にオリジンを追加）
- Googleカレンダー同期機能一式（月表示カレンダー・予定追加/一覧ポップアップ・矢印キー/スワイプでの月移動・トグル化した同期/通知設定）を実機確認する
- 絶縁バグ修正後、既存の詰まっていた相手ペアで再度「絶縁」フローをやり直し、友達関係が正しく解消されるか確認する
- 「友達申請」関連画面で「仲間」→「友達」表記が正しく反映されているか目視確認する

## 2026-08-31

### 実装内容
- **危険操作系ラベルを「赤文字」から「赤ピル＋白文字」に変更**: ハンバーガーメニュー（`chat_panes.dart`の`_MenuTile`）・広場設定ポップアップの「広場を削除」（`group_settings_popup.dart`）・設定＞アカウントの「アカウントを削除」行・アカウント削除の選択ポップアップの「今すぐ削除」カード（`settings_tab.dart`）で、`Colors.red`/`colorScheme.error`の文字色のみだった危険操作ラベルを、共通ウィジェット`DestructiveLabel`（新規`lib/widgets/destructive_label.dart`、角丸の`Colors.red.shade700`背景＋白文字）に置き換えた。フォームの検証エラー文（`Text(_errorMessage!, ...)`）はタップ不可の表示であり対象外とした
- **showMenu独自ポップアップ（ピン留め/アルバム/ハンバーガーメニュー）の背景コントラスト改善**: フラットUIはポップアップ背景（`popupCardBackground`、以前は純白固定）が画面背景（ライトは`colorScheme.surface`、ほぼ白）とほぼ同色で視認性が無く、ガラスUIも`GlassSurface`の塗り色が`colorScheme.surface`（＝Scaffold背景と同値）を半透明ブレンドしているだけで色差が実質ゼロだった不具合を修正。新設`lib/theme/popup_surface_colors.dart`の`popupCardBackground`/`popupCardForeground`/`popupCardBorder`を`AppUiStyle`も受け取るよう拡張し、フラット/ガラス共通の新しい中立グレー（ライト`#E7E7EC`/ダーク`#2C2C34`）＋半透明の枠線に統一（ガラスは`GlassSurface(variant: floating)`を使うのをやめ、フラットと同じ不透明`Container`に一本化）。劇画は「色だけ揃えてほしい」というユーザー要望に応え、既存の黒パネル＋白文字パレット（`GekigaColors.panel`/`onPanel`）を使う専用分岐を追加（枠の手描き風接合ボックス等の構造は今回持ち込まない）。対象3ファイル（`chat_panes.dart`の`_MenuPanel`、`chat_screen.dart`の`_PinnedMessagesPopupContentState`、`album_popup_content.dart`の`_AlbumPopupContentState`）とも同じ形に統一
- **プッシュ通知（FCM）を新規実装（Web+Androidのみ、iOS/macOSはApple Developer Program未加入、Windows/Linuxはfirebase_messaging非対応のため対象外）**:
  - データモデル: `AppUser.fcmTokens`（`FcmTokenEntry{token, platform}`の配列、新規`lib/models/fcm_token_entry.dart`）、`MessageFileMetadata.thumbnailUrl`（動画のみ）を追加
  - クライアント: `lib/repositories/push_notification_repository.dart`（権限リクエスト・トークン登録、永続化は既存の`UserRepository.addToProfileList`/`removeFromProfileList`を再利用）、`lib/push_notifications.dart`（Androidのバックグラウンドハンドラ・`flutter_local_notifications`によるリッチ通知組み立て、Cloud Functions側がdata-onlyで送るリモート画像URLを`http`でダウンロードして表示）、`lib/features/push_notification_bootstrap.dart`（`AppGate`でログイン直後に自動登録・トークン更新追従・フォアグラウンド受信時のローカル通知表示）。設定＞通知フォルダ（従来`_ComingSoonFolder`のプレースホルダー）を実装し再リクエスト用ボタンを設置。ログアウト時は`unregisterCurrentToken`でトークン削除
  - Web: 新規`web/firebase-messaging-sw.js`（Service Worker、`flutter build web`が`web/`直下をそのまま`build/web/`にコピーするためVercelにも追加設定無しでデプロイされる）
  - Android: `AndroidManifest.xml`に`POST_NOTIFICATIONS`権限追加
  - Cloud Functions（`functions/src/index.ts`に追加、既存の単一ファイル構成を踏襲）: `onDmMessageCreated`/`onGroupMessageCreated`（`onDocumentCreated`、新規メッセージ作成時に通知送信。`Message.silent`・`conversationPrefs`のミュート設定を尊重、DMは送信者の`effectiveNicknameFor`と同じロジックをTypeScript側に移植した呼び名、広場は`groupInvites/{groupId}`の名前・アイコンを使用、contentType別のプレビュー文言・画像を組み立ててAndroid宛はdata-only、Web宛は`webpush.notification`で`sendEachForMulticast`）、`generateVideoThumbnail`（`onObjectFinalized`、動画アップロード完了時に`ffmpeg-static`+`fluent-ffmpeg`で1フレーム抽出しStorageへ保存、Admin SDKに`getDownloadURL()`相当が無いため`firebaseStorageDownloadTokens`を自前発行して既存の添付ファイルURLと同じ形式のURLを組み立て、`fileMetadata.thumbnailUrl`を更新）
  - Firestoreルール: `users/{userId}`の`allow update`に`fcmTokens.size() <= 10`の上限チェックを追加
  - `npx firebase-tools`（ログイン済み: `rhing.official@gmail.com`）で`functions`・`firestore:rules`を本番デプロイ済み
  - スコープを縮小した点: 通知タップでの特定会話への自動ジャンプは未実装（`go_router`の`/chat/dm`・`/chat/group`がURLではなくメモリ上の`extra`オブジェクトでしか会話を渡せない設計のため、実装中に発覚。タップすると今はアプリのホーム画面が開くところまで）
- （このセッションの会話には含まれない別作業として）通話UI（`call_screen.dart`等）で相手の呼び名表示が`@rhingId`固定だったのを、新設`CallParticipantNameLabel`（`lib/features/call/call_avatar.dart`）で`AppUser.effectiveNicknameFor`を反映するよう修正する変更が同時に加わっていた

### 詰まったところ
- プッシュ通知の動画サムネイルAdmin SDK生成で、Storageの保存パスが`dmFiles/{dmId}/{messageId}.ext`のようにroom単位ではなくdmId/groupId単位でフラットなため、アップロードイベント単体からはroomIdが分からずメッセージドキュメントを直接特定できないと判明。`directMessages/{dmId}/rooms`配下を全件走査して`messages/{messageId}`の存在を順に確認する方式（`findMessageRef`）で対応した（会話1件あたりのroom数は少数のため許容）
- 通知タップ時に特定の会話へ直接遷移させる計画で着手したが、`go_router`の`/chat/dm`・`/chat/group`が`state.extra`にDart側で組み立てた`DmChatArgs`/`GroupChatArgs`（DirectMessage/Groupオブジェクト本体を含む）を要求する設計だとコーディング中に判明し、URL文字列やService Worker側からは会話を復元できないことが分かった。実装済みのFCM送信ペイロード自体は`conversationId`/`roomId`を含めたまま、クライアント側の自動ディープリンクは見送り、素直にアプリを開くだけの動作に留めた
- `flutter_local_notifications`を22系で新規導入した際、`initialize()`/`show()`が想定より新しいAPI（named parameters必須）に変わっており`flutter analyze`でエラーになった。パッケージの実際のソース（pub-cache配下）を直接確認して正しいシグネチャに修正した

### 次にやること
- プッシュ通知（FCM、Web+Androidのみ実装）を実機で確認する: Android実ビルドでのアプリ完全終了状態からの通知到達、Web本番（Vercel）デプロイ後にタブを閉じた状態での通知到達
- ペタピタ・動画メッセージ送信時、通知にプレビュー画像（動画は`generateVideoThumbnail`が生成するサムネイル）が表示されるか確認する
- Android通知バー用の専用モノクロアイコンを用意する（現状`@mipmap/ic_launcher`＝ランチャーアイコンを暫定流用）
- 危険操作系ラベルの赤ピル表示（ハンバーガーメニューの寄合削除・広場を削除・アカウントを削除・今すぐ削除カード）を実機確認する
- showMenu独自ポップアップ（ピン留め/アルバム/ハンバーガーメニュー）の背景コントラスト改善を、フラット/ガラス/劇画×ライト/ダークの各組み合わせで実機確認する
- 通話UIの呼び名表示修正（`@rhingId`固定表示から`effectiveNicknameFor`反映へ）を実機確認する

## 2026-08-30

### 実装内容
- **ガラスUI（Apple Liquid Glass風）を新規実装**: `AppUiStyle.glass`を`flat`/`gekiga`と並ぶ3つ目の選択式UIスタイルとして追加。中核部品`GlassSurface`（すりガラスの塗り＋縁の光彩、`GlassVariant.chrome`/`floating`/`card`で`BackdropFilter`の要否を切り替え）と、それを使う`GlassAppBar`・`GlassAlertDialog`・`showGlassModalBottomSheet`・`GlassIconBadge`/`GlassIconButton`を新設し、ホーム・設定・身だしなみ・語らい（サイドバー・寄合タブバー・寄合一覧・チャット吹き出し・入力欄・日付区切り・通話/ハンバーガーボタン）までほぼ全画面に適用。複数回のフィードバックを経て以下を修正: (1) マテリアルに影を一切使わない方針に統一（`enableOuterGlow`の仕組み自体を削除、`GlassThemeExtras.edgeGlowShadow`も削除）した上で、ハンバーガーメニューのアイコンのみユーザー指示により`GlassIconBadge.shadow`で個別に影を復活、(2) `BorderRadius.zero`の全面パネル（入力欄・寄合タブバー・寄合一覧サイドバー）が同色背景に溶け込んで見えなくなる問題を`GlassVariant.card`→`chrome`（ぼかし付き）に変更して解消、(3) 縁ストロークが直線状の全面パネルで不要な線に見える問題を`enableEdgeStroke`個別トグルで解消、(4) `ListTile.selected: true`時は`textColor`/`iconColor`ではなく`selectedColor`を見るというFlutter仕様の盲点により選択中タイルの文字色がアクセントカラーのままになっていた不具合を`selectedColor`明示指定で修正、(5) 入力欄の`SafeArea`が画面最下部にも関わらず不要な上端セーフエリア分の余白を確保していた不具合を`top: false`で修正、(6) 入力欄`TextField`が独自の縁取り・塗りを常に描画し外側のすりガラスと二重の箱に見えていたのをガラス時のみ`InputBorder.none`に統一、(7) `colorScheme.onSurface`/`onSurfaceVariant`をアクセントカラー由来の`ColorScheme.fromSeed`任せにせず、ライト/ダーク各1色の固定色（`GlassColors.lightForeground`/`darkForeground`）に上書きして視認性を確保、(8) 便り（公式アカウント）のメッセージにアイコン・呼び名が表示されない問題を、`ChatScreen.forceShowSenderInfo`を新設し便りの一対のみ`sideBySide`レイアウトでも常時表示するよう修正、(9) 寄合名・通話ボタン・ハンバーガーメニューのみ`GlassSurface.opaque`でメッセージが透けない高不透明度に固定、(10) メッセージ画面の寄合名をガラスのピル型ボックスで表示
- **劇画UIの下書き同期トグルの配色修正**: 設定＞語らい＞詳細設定＞「入力中のメッセージを他の端末と同期」のON/OFFで、以前はタイル全体（背景・文字色）が反転していたのを、タイルの見た目は固定のまま、スイッチのボール・トラックの色だけを白黒反転させるよう変更（`Switch`の`activeThumbColor`等を直接指定）
- **アクセントカラーの文字色コントラスト修正**: `app_theme.dart`・`glass_theme.dart`の`colorScheme.onPrimary`・`elevatedButtonTheme`/`floatingActionButtonTheme`の`foregroundColor`が`Colors.white`決め打ちで、アクセントカラーを白系にするとボタン等の文字が見えなくなっていた不具合を、ライトモード=黒・ダークモード=白の固定ルールに変更して修正（アクセントカラー自体の明度からの動的計算ではなく、テーマのbrightnessのみで決まる単純なルール）
- **アクセントカラーのカスタム登録機能を追加**: 設定＞アプリケーション＞デザインのカラーコード入力欄の右端（適用チェックマークの隣）に「＋」ボタンを追加し、押すと現在の色を最大5色まで登録・一覧表示（長押しで削除確認→削除）できるようにした。`customAccentColorsProvider`（`accentColorProvider`と同じSharedPreferences＋Firestore二重保存パターン）を新設し、`AppUserPreferences.customAccentColorsArgb`を追加（コード生成無しの手書きクラスのためフィールド追加のみで対応）
- **メッセージのピン留め機能を追加**: 語らい（一対・広場）のAppBarに、通話ボタンとハンバーガーメニューの間にピンアイコンを追加。タップするとボタン直下にポップアップでピン留め済みメッセージ一覧を表示し（無ければ「ピン留めされたメッセージはありません」）、選択すると既存の返信ジャンプ機構（`_jumpToMessage`、ハイライトフラッシュ込み）を再利用してジャンプする。長押しメニューにも状態に応じて「ピン留め」/「ピン留め解除」を追加。データは`DmRoom`/`Room`に`pinnedMessageIds`（配列末尾が最新）を追加し、`DirectMessageRepository`/`GroupRepository`に`pinMessage`/`unpinMessage`（`FieldValue.arrayUnion`/`arrayRemove`）を実装。送信取り消し・参加者/メンバー全員削除による物理削除時には、同じbatch内でピン参照も自動的に取り除く。権限は一対・広場とも全参加者/全メンバーが操作可能とし、firestore.rulesの広場`rooms`更新ルールに`pinnedMessageIds`のみの変更を無条件許可する分岐を追加（DM側は既存ルールが元々全フィールド更新可のため変更不要）。あわせて、DM側の寄合一覧（`DmRoom`）購読が従来「横スクロールタブバー表示時のみ」に限定されていたのを、ピン留めデータを常に取得できるよう常時購読に変更（タブバー表示の可否は別の`showRoomTabBar`変数で判定するよう分離）
- **語らい一覧に検索機能を追加**: 一対/広場切り替えタブの直下に検索欄を追加。入力中は`_category`の選択（一対/広場タブ）に関わらず、一対・広場を横断して名前が一致する語らいを一覧表示する（`_buildSearchResults`、劇画UIは`GekigaJointedTileList`、それ以外は通常の`ListView`）。フレンド申請・広場参加リクエストは「語らい」そのものではないため検索対象外
- **共有アルバム機能を追加**: 寄合（一対・広場）単位で参加者全員が使える「アルバム」を新規実装。`Album`/`AlbumItem`モデルと`AlbumRepository`/`FirestoreAlbumRepository`を追加し、`.../rooms/{roomId}/albums/{albumId}/items/{itemId}`というFirestore構造で管理。メッセージ長押しメニューの「アルバムに登録」→`album_picker_sheet.dart`のボトムシートでアルバムを選択・新規作成でき、チャットAppBarの`_AlbumButton`から`album_popup_content.dart`のポップアップで一覧・作成・リネーム・削除、`album_detail_screen.dart`でアイテム閲覧・削除、`album_media_viewer_screen.dart`で画像/動画の全画面表示ができる。CLAUDE.mdの「アルバム永久保存」ポリシーの実装で、登録した画像・動画は`album_storage.dart`経由で`albumFiles/dm|group/...`という独立Storageパスにコピーし、通常メッセージの保存期限・削除ルールから切り離す。firestore.rules・storage.rulesにアルバム/アイテム用の権限（寄合の参加者・メンバーなら全員対等に読み書き可、広場のロール権限とは非連動）を追加
- **Discord風の「重要度が下がるほど文字・アイコンが薄くなる」階調（プロミネンススケール）を実装**: discordを参考にした提案で、フラット・ガラスUI向けに3段階（Primary/Secondary/Tertiary）の固定色（新規`lib/theme/text_prominence_colors.dart`の`TextProminence`）を新設し、各テーマの`onSurface`/`onSurfaceVariant`/新設`textTertiary`に反映
- **語らい名・寄合タブの不要な省略表示を修正**: PCサイドバー向けの文字数ベース省略（`truncateName`）をモバイルの語らい一覧・寄合タブバーでもそのまま使っていたため、実際には収まる横幅でも一律で省略されていた不具合を、幅ベースの`TextOverflow.ellipsis`に置き換えて解消（PC専用の固定幅サイドバー・工房カードでの使用はそのまま維持）
- **ホバー時のテキスト表示（ツールチップ）を全面的に削除**: 以前から繰り返し指摘していたにも関わらず別箇所で再発していたため、残っていた`tooltip:`文言・生の`Tooltip()`ウィジェット・`GlassIconButton`の未使用`tooltip`パラメータを洗い出して全て削除
- **通話画面の一連の不具合修正・ガラスUI対応**: (1) ミニ通話ウィンドウ（`PinnedCallOverlay`）が劇画UIで斜めカット形状によりボタンが隠れていた不具合、映像通話時に縦長になっていた不具合を、固定140×200サイズ＋矩形のモノクロボックスに戻して修正、(2) 自撮り映像が背面カメラでも左右反転していた不具合を、`isFrontCamera`状態を通話コントローラー2種に追加し前面カメラの時だけ反転するよう修正、(3) ビデオ通話画面左上の寄合名表示を削除、(4) ガラスUIの通話コントロール3アイコン・ミニ通話ウィンドウに`GlassSurface`ベースの専用デザイン（円形パネル・floatingバリアントの縁取り）を追加、(5) 通話画面の「映像表示領域」は常に固定グレー・「それ以外（背景・ヘッダー）」は3スタイル共通でテーマ背景色、というルールに統一（以前はガラスのみの対応・音声通話の扱いも不統一だった）、(6) ガラスUIの通話ボタンの背景が映像の色を透けさせてアイコンとのコントラストを損なっていた不具合を`GlassSurface.opaque`で修正、さらにダークモードでグレー/赤のアイコン色自体のコントラストも不足していたため新設`GlassColors.adaptiveIconColor`で明暗ごとに色を調整、(7) PC埋め込み通話画面（`EmbeddedCallPane`）で、画面上のどこでも上スクロール/下スワイプすると左上の「メッセージに戻る」ボタンと同じ動作をするように追加（`chat_screen.dart`のメディアビューアと同じ`PointerScrollEvent`＋デバウンス＋`SwipeDownToDismiss`のパターンを再利用）

### 詰まったところ
- ガラスUIの`talks_tab.dart`改修中、複数回のEdit操作でカッコの対応が崩れ、誤ってコードブロックの重複＋`★DUPLICATE_MARKER★`という架空のプレースホルダーコメントが実コードとして混入する事故が発生。行ごとの括弧スタックを手動でトレースして特定・修正した
- ガラスUIの「選択中タイルの文字色がアクセントカラーのまま」というバグは、既に`iconColor`/`textColor`を意図した固定色に指定していたにも関わらず再発していた。原因は`ListTile`が`selected: true`の場合`textColor`/`iconColor`ではなく別プロパティ`selectedColor`（未指定時は`colorScheme.primary`にフォールバック）を使うというFlutter側の仕様で、コードレビューだけでは見落としやすかった
- `customAccentColorsProvider`追加後、`flutter test`で`settings_navigation_test.dart`の4件が新規に失敗。原因は同テストの`ProviderScope`の`overrides`に新設した`initialCustomAccentColorsProvider`の上書きを追加し忘れていたこと（`build()`内で`throw UnimplementedError`する設計のため）で、テスト側にも同じ上書きを追加して解消した
- ピン留め機能では、DM側の寄合（`DmRoom`）一覧購読が既存実装で「横スクロールタブバーが必要な場合のみ」に限定されていたため、そのままではピン留めデータが取得できない画面（広い画面のサイドバー表示・単一モード）が生じることに実装中に気づいた。購読条件とタブバー表示可否の判定を分離することで対応した。それ以外は既存の引用（返信）機能のジャンプ・プレビュー用ヘルパーをそのまま再利用できたため、大きな詰まりは無かった
- `flutter run -d web-server`のホットリロード/リスタートが、Floorpブラウザでは動作確認に必要なDart Debug Chrome拡張が使えないため実際には反映されておらず、複数の修正（ピン留めミニ通話サイズ・劇画の切り欠き・ツールチップ削除等）が「反映されていない」と繰り返し報告される事態になった。ユーザーから「ホットリロードしたのに反映されていない、こちらのせいにするな」と明確な指摘を受け、原因を認めて謝罪し、以後はコード変更のたびにプロセスを完全に再起動する方式に統一した
- ホバー時のツールチップ表示は、過去に一度削除を指示されていたにも関わらず別箇所で再発しており、「言ったよね？」と再度強い指摘を受けた。今後の再発を避けるため、この方針をメモリ（feedback）として保存した
- ガラスUIの通話コントロールボタンの視認性問題は1回の修正では収まらず、(1)背景の半透明フィルが背後の映像の色を透かしてアイコンとのコントラストを損なう、(2)ダークモードではアイコン自体の色（グレー・赤）も背景とのコントラストが不足、という2段階の別々の原因が重なっていたため、都度ユーザーからの指摘を受けて順に切り分けて修正した
- 劇画UIのミニ通話ウィンドウは、映像通話時に画面下端まで伸ばす仕様（8/21実装）が横長のWebカメラ映像には不自然だったことと、既定の非対称な斜めカット形状がボタン行と干渉することの2点が重なっており、「縦長になった」「斜めカットでアイコンが見えない」という2つの独立した指摘を受けてそれぞれ原因を切り分けて修正した

### 次にやること
- メッセージのピン留め機能（AppBarのピンアイコン・ポップアップ・長押しメニュー）をブラウザ実機で確認する
- 語らい一覧の検索機能（一対/広場切り替えタブ直下の検索欄）をブラウザ実機で確認する
- ガラスUI一式（新スタイルとして追加、ほぼ全画面に適用）をブラウザ実機で最終確認する
- 劇画UIの下書き同期トグルの配色修正（ボックス固定・スイッチのみ白黒反転）を実機確認する
- アクセントカラーの文字色コントラスト修正（ライト=黒/ダーク=白固定）を、白系アクセントカラーで実機確認する
- アクセントカラーのカスタム登録機能（上限5色、登録・削除・Firestore同期）を実機確認する
- 共有アルバム機能（作成・アルバムに登録・詳細閲覧・全画面ビューア・削除）を実機確認する
- Discord風のプロミネンス階調（文字・アイコンの重要度による濃淡）を実機確認する
- 語らい名・寄合タブの省略表示修正（幅ベースのellipsis）を実機確認する
- 通話UIの一連の修正（カメラミラー・劇画ミニ通話ウィンドウの切り欠き/縦長化修正・ホバーツールチップ削除・ガラスUIの通話コントロール/ミニウィンドウの見た目とアイコン視認性・背景色ルールの3スタイル共通化・PC埋め込み通話画面でのスクロール/スワイプ最小化）を実機確認する

## 2026-08-29

### 実装内容
- **運営向け管理画面（`/admin`）のモノクロUI化**: ユーザー設定のアクセントカラーに左右されないよう、`AppTheme.admin()`（黒・白・グレーのみのColorScheme）を新設し管理画面全体に適用
- **便りのアイコン・呼び名編集が`permission-denied`で失敗する不具合の修正**: `users/{userId}`の更新自体は管理者向け例外込みで成功していたが、更新のたびに呼ばれる`syncInvitePreview`が書き込む`userInvites/{rhingId}`のfirestore.rulesに管理者例外が無く、そちらだけが失敗していた。`users`と同じ管理者例外を`userInvites`にも追加し本番デプロイ
- **管理画面のレイアウト不具合修正**: `AppTheme.admin()`の`filledButtonTheme`が`minimumSize: Size.fromHeight(48)`（＝幅無限大）を指定しており、Row内の「保存」ボタンが本物のFlutter RenderFlexオーバーフローを起こしていたのを`Size(64, 48)`に修正。あわせて便りのアイコン編集UIを蔵と同じ「丸に+」方式に、サイドバーのラベル非表示、呼び名編集をEnter確定の自動保存（保存ボタン廃止）に変更
- **チャット画像ビューアーのピンチズーム不具合修正**: `InteractiveViewer`が子（`Image`）の実サイズに合わせて自身の大きさ・クリップ領域を決めていたため、ズームしても元のレターボックスされた矩形からはみ出せなかった。`SizedBox.expand`で画面いっぱいに広げ、`Image`側に`fit: BoxFit.contain`を指定して修正
- **ホーム画面下部ナビのアイコン大きさ統一**: 「身だしなみ」「設定」の`Icons.face_outlined`/`Icons.settings_outlined`が「語らい」の`Icons.forum_outlined`よりグリフの密度が低く小さく見える問題を、アイコンごとの表示サイズ指定（24→28）で解消
- **モノクロUI（`AppUiStyle.dessin`）をフラットUIへ統合し廃止**: フラットUIのアクセントカラープリセットに黒（`#000000`）を追加。チャット画面の吹き出し・既読バッジ・送信者アイコンのフラット分岐を、旧デッサンスタイルと同じ「地色＋枠線の角丸ボックス、自分/相手を色で区別しない」構造に統合（色は`DessinColors`固定値ではなく`colorScheme`＝アクセントカラー由来に変更）。`AppUiStyle.dessin`をenumから削除し、`main.dart`・`settings_tab.dart`・`strings.dart`の関連分岐を除去、`lib/theme/dessin/`・`lib/widgets/dessin/`配下の4ファイルを削除
- **語らい一覧の配色調整**: フォールバックアバター（人物アイコン等）・日付セパレーターの背景色を、Material3のデフォルト配色（薄いピンク寄りの色）から`colorScheme.primary`/`onPrimary`（アクセントカラー由来）に変更
- **語らい一覧の一対/広場カテゴリ切り替えレイアウト修正**: 英語UIで"Private"/"Plaza"チップの合計幅が収まらず縦に折り返される不具合を修正。最終的には`_CategoryTab`のパディング（14→8）・フォントサイズ（16→13）・チップ間隔を縮め、横スクロール無しの通常`Row`で常に横並びに収まるようにした
- **劇画UIモバイルの設定・身だしなみカテゴリ一覧を拡大**: 共有部品`GekigaTileContent`にオプトインの`contentPadding`/`titleFontSize`パラメータを追加（省略時は既存動作のまま、他画面には無影響）し、狭い画面時の設定・身だしなみのカテゴリ一覧だけパディング・フォントサイズ・アイコンを拡大
- **Firestoreルールに自己フォロー・自己お気に入り防止を追加**: daidai横丁（別リポジトリHomePage-Rhing）で「自分自身をフォロー/自分の出品パックをお気に入り登録できてしまう」バグが見つかり、クライアント側（followCreator/addFavoritePack、FollowButton/FavoriteButton）は修正済みだったが、真のセキュリティ境界であるFirestoreルール側にチェックが無く、開発者ツールから直接Firestore SDKを叩けば依然として可能な状態だった。`users/{userId}/follows/{creatorId}`は`creatorId != userId`を条件に追加。`users/{userId}/favoritePacks/{packId}`はpackIdがユーザーIDではなくstickerPacksのドキュメントIDのため直接比較できず、新設した`isOwnStickerPack(packId, userId)`ヘルパー関数（`hasGroupPermission`と同じget()パターン）で`stickerPacks/{packId}`をget()し`creatorId`を比較、`allow create`のみを`read/update/delete`から分離して適用。firestore.rulesの他の未コミット変更（userInvites関連）には触れず該当2ブロック+新規関数のみを変更し、rhing.official@gmail.comでのfirebase CLI（npx firebase-tools、ローカル未インストールのため一時取得）でfirestore:rulesのみ本番（daidai-rhing）にデプロイ済み

### 詰まったところ
- 便りのアイコン/呼び名編集の不具合は、`users`ドキュメント自体の更新は成功しており、副次的に呼ばれる`userInvites`同期処理だけが`permission-denied`になっていたため、エラーメッセージだけを見ると原因の切り分けが紛らわしかった
- 管理画面のオーバーフロー警告（黄黒のハザードストライプ）を「劇画UIの演出と混ざっている」と誤解されたが、調査の結果DaiDai独自の劇画演出とは無関係な、Flutter標準のRenderFlexオーバーフロー警告だった。真因は`AppTheme.admin()`の`FilledButtonThemeData`が`Size.fromHeight`（＝幅無限大）を使っていたこと
- 語らい一覧の一対/広場切り替えは、`Wrap`（英語表示で縦に折り返される）→横スクロール（ユーザーから「スクロールで収めるのではなく縮めてほしい」と修正依頼）→パディング・フォントサイズ縮小、と3段階の試行錯誤になった
- セッション途中で動作確認用の`flutter run -d web-server`プロセスが原因不明のまま停止しており、画面が真っ黒になる不具合として報告された。プロセスを完全に再起動して復旧したが、停止自体の根本原因は特定できていない
- ユーザーの言う「モノクロUI」が実際には`AppUiStyle.dessin`（設定画面の表示ラベルが「モノクロ」、劇画UIとは別軸の3つ目のスタイル）を指しており、「劇画UIのモノクロ演出」と紛らわしかったため、対象の特定に複数回の調査が必要だった

### 次にやること
- 今回の一連の変更（管理画面のモノクロ化・不具合修正、チャット画像ビューアーのズーム修正、ホームナビアイコンの大きさ統一、モノクロUI＝デッサンのフラットUIへの統合・廃止、語らい一覧の配色・レイアウト調整、劇画UIモバイルの設定/身だしなみ画面拡大）をブラウザ実機で最終確認する
- `flutter run`プロセスがセッション中に原因不明のまま停止し画面が真っ黒になった件の根本原因を調査する（発生時はプロセス完全再起動で復旧可能）
- Firestoreルールに追加したfollows/favoritePacksの自己フォロー・自己お気に入り防止を、HomePage-Rhing側（daidai横丁）から実機確認する（ルールのユニットテストが無いため）

## 2026-08-25

### 実装内容
- **メッセージ削除時のFirebase Storage添付ファイル実体削除**: 参加者/メンバー全員がメッセージを削除し終えてFirestore上の`messages`ドキュメントを物理削除する際、そのメッセージが画像・動画・ファイルの添付（`Message.fileMetadata`）を持っていた場合にFirebase Storage上のファイル実体も削除するよう`DirectMessageRepository.hideMessagesForMe`・`GroupRepository.hideRoomMessagesForMe`に追加（`_storage.refFromURL(url).delete()`、失敗はtry/catchで無視）。既存の`hiddenFor`蓄積→全員一致で物理削除、というロジック自体は2026-07-27に実装済みだったが、添付ファイルのStorage実体だけ削除されずに残る漏れがあった
- **デッサンUI（`AppUiStyle.dessin`）の新規追加**: 既存の`flat`/`gekiga`と並ぶ3つ目のUIスタイルとして「手描き・陰影・モノクロ・紙の質感」をコンセプトに実装。決定的シード付き乱数によるベジェの手描き線・鉛筆ハッチング・紙目ノイズをCustomPainterで描画する設計（`lib/theme/dessin/dessin_shapes.dart`・`lib/widgets/dessin/sketchy_box_painter.dart`）で、チャット画面の吹き出し・アバター・設定タブの選択肢まで実装した
- **デッサンUIをミニマルなモノクロUIに作り直し**: 実機確認したユーザーから「鉛筆画に見えない、ただのモノクロの線」というフィードバックを受け、上記の手描き風CustomPainter一式（`dessin_shapes.dart`・`sketchy_box_painter.dart`）を削除し、`BoxDecoration(border: Border.all(...))`による細い実線の枠で囲むだけのミニマルな実装に作り直した。自分/相手の吹き出しは色反転させず位置（左右）のみで区別する方針にした。`AppUiStyle.dessin`というenum値・3択の骨格・`DessinColors`のモノクロパレット・`DessinTheme`自体は変更していない

### 詰まったところ
- デッサンUIの初回実装（CustomPainterによる手描き風装飾）は、紙目ノイズの密度不足・鉛筆ハッチングの適用範囲の限定・輪郭線のジッター量不足により、実機では「単なる2重の細い線」にしか見えなかった。パラメータ強化の余地はあったが、Flutterの`CustomPainter`によるベクター線・点描画だけで本物の鉛筆デッサンの質感（グラファイトの粒状感・濃淡のムラ）まで再現するのは技術的に限界があると判断し、ユーザーに確認の上でミニマルな枠線ベースのUIへ方針転換した
- `dart format`をプロジェクト全体に対して実行すると、今回の変更と無関係な既存ファイル（`lib/features/call/`配下等）や、`chat_screen.dart`内の意図しない既存3箇所（`HardwareKeyboard`の折り返し等）までフォーマッタのバージョン差異で再整形されてしまう現象が2回発生し、その都度手動で元のフォーマットに戻した。以降このプロジェクトで`dart format`をかける際は、変更したファイルだけを明示的に指定する必要がある
- 動作確認用に起動していた`flutter run -d web-server`プロセスが、セッションの途中で（原因不明のまま）停止しており、ホットリロード用のシグナル送信が失敗した。プロセスを完全に再起動して復旧した

### 次にやること
- デッサンUI（枠線ベースのミニマルなモノクロUIに作り直した版）をブラウザ実機で最終確認する
- メッセージ削除（範囲選択→参加者全員削除で物理削除）時のFirebase Storage添付ファイル削除を実機確認する

## 2026-08-24

### 実装内容
- **部分コピーモード（劇画UI）の黒背景・吹き出し変形バグ修正**: 長押しメニューの「部分コピー」で本文選択用の読み取り専用`TextField`（`LinkifiedEditingController`、前回セッションで`SelectionArea`から置き換え済み）に切り替わった瞬間、劇画UIでのみ吹き出しより大きい黒い矩形が出現し、テキストが隠れて選択不能になる不具合を修正。原因は(1) `TextField`の`decoration`が`filled`/`fillColor`を明示しておらず、劇画テーマの`inputDecorationTheme`（`filled: true, fillColor: GekigaColors.panel`＝黒）を継承していたこと、(2) `TextField`は`Text`と異なり緩い制約下では最大幅まで広がろうとし、`_GekigaBubble`（`CustomPaint`が子に追従してサイズを決める）が本文サイズを超えて膨張していたことの2点。`decoration`を`InputDecoration.collapsed(border: InputBorder.none)`に変更し（`filled`が既定`false`のため黒塗りを継承しない）、`TextField`を`IntrinsicWidth`でラップして本文サイズにシュリンクラップさせて解消（`chat_screen.dart`の`_MessageRow`）
- **選択ハイライト色の固定**: `TextSelectionThemeData.selectionColor`が`isMe`・`colorScheme.primary`に応じて分岐していたが、劇画テーマは`colorScheme.primary`を白にremapしているため劇画UIでは常に白（自分の白い吹き出しではほぼ見えない）になっていた。バブル所有者・テーマに依存しない固定の青（`Colors.blue.withValues(alpha: 0.4)`）に統一
- **部分コピーモードがCtrl+C/Ctrl+Xで終了しない不具合の修正**: 選択範囲をコピーした時点で`_copyPartialSelectionAndExit`によりモードを自動終了する設計（前回セッションで実装済み）だったが、実際にはコピーしてもモードが持続し何度でも選択・コピーできてしまう不具合があった。実装は`TextField`の`CopySelectionTextIntent`を外側の`Actions`で上書きする方式（Flutter SDKの`Action.overridable`、`TextField`のコピー動作を上書きする目的で公式にサポートされた正規の手段）だったが、Web環境では実際には発火していなかった。上書きに頼らず生のキー入力を`Focus.onKeyEvent`で直接検知してCtrl+C/Ctrl+X時に`onCopyPartialSelection`を呼ぶ実装に変更（既存の`CallbackAction`はデスクトップ/モバイル向けの保険としてそのまま残置）

### 詰まったところ
- 黒背景修正の1回目の報告で「変化無し」と言われたが、原因は動作確認用に起動していた`flutter run -d web-server`プロセスへホットリロードを送り忘れていたこと。CLAUDE.mdに手順があるにもかかわらず、コード修正後にホットリロードを実行し忘れた
- 2回目は、ユーザー自身がFlutterのホットリロード・ブラウザのハードリロード双方を行った後でも症状が変わらないと報告された。ここで初めて「原因診断自体が的外れ」ではなく「ホットリロードでは構造的なウィジェットツリー変更（`InputDecoration`のコンストラクタ変更・`IntrinsicWidth`の追加）が正しく反映されない場合がある」という可能性に思い至り、`flutter run`プロセスを完全に終了・再起動して初めて修正が実際に反映されていることを確認できた（2026-08-21の日記にも同種の「ホットリスタートを繰り返した後の不具合」の記録があり、`web-server`デバイスのホットリロード/リスタートの信頼性には注意が必要）
- Ctrl+Cでモードが終了しない不具合は、コード上は`Action.overridable`というFlutter公式ドキュメント・dartpad例にもある正規の上書き手段を使っており、Flutter SDKソース（`editable_text.dart`）を確認しても理屈上は動作するはずだったが、実際にはWeb環境で機能していなかった。「公式にサポートされた実装パターン」であることは動くことの保証にならず、実機（実ブラウザ）での確認が必要だと再認識した

### 次にやること
- 部分コピーモードのCtrl+C/Ctrl+Xによる自動終了修正（`Focus.onKeyEvent`での実装、ホットリロード済み）を実機確認する
- 白画面不具合の再発有無を確認する（`flutter run`プロセスの完全再起動で復旧済み、根本原因は未特定）
- リアクション・編集の反映不具合修正、削除確認ポップアップのスタイル変更、長押しメニュー各モードのEscキー終了、送信取り消しポップアップのサイズ変更を実機確認する
- 引用元メッセージへのジャンプ機能（`scrollable_positioned_list`移行）を、大量にメッセージが間に挟まった状態・未ロードの古い日への遷移・通常スクロール・無限スクロール（過去日の自動読み込み）・ファイル添付ブロックの展開/折りたたみ時のスクロール補正の観点でブラウザ実機確認する（`flutter analyze`・`flutter test`は通過済み）
- メッセージ読み込みの1日単位ページネーション・下書き同期の保存不具合修正・語らい切り替えラグ解消（IndexedStack化）・ピン留めミニ通話表示の映像時拡張を実機確認する
- 通話中央アイコン修正・通話UI再設計（PC埋め込み表示/右上ピン留めミニ表示/モバイルのドック化）・動画ビューア拡充（シークバードラッグ・10秒送り戻し・fキーFullscreen・トラックパッド閉じ）をブラウザで実機確認する（`flutter analyze`は通過済みだが未検証）
- 手描きのアプリアイコン・起動スプラッシュ・イメージキャラクター素材が用意され次第、`assets/branding/app_icon_source.png`に配置し`dart run flutter_launcher_icons`・`dart run flutter_native_splash:create`を実行する（pubspec.yamlの設定基盤は維持済み。AI生成分は不採用が確定し`assets/branding/`以下に参考として残置のみ）
- 広場作成の不具合修正（`GroupRepository.createGroup`のroom作成バッチ分離＋基準ロールルール修正）は`firestore.rules`をdaidai-rhingへデプロイ済みだが、ユーザーによる動作確認はまだ得られていない
- daidai横丁専用の呼び名・アイコン機能（`functions/src/index.ts`・`storage.rules`）が本番Firebaseへデプロイ済みかどうか未確認
- 「メッセージ画面に戻るボタンが効かない」不具合は対症療法で対応したが、正確な原因は未特定。再発する場合は追加調査が必要
- ペタピタのLINE型送信方式を実機で動作確認する
- daidai横丁再開に向け、macOSの配布方式を決定する
- Stripeに登録する銀行口座を決定する
- E2E暗号化が未完了のままアプリをリリースするかどうかを判断する
- daidai横丁の法務整備は`競合調査.md`に記録済みだが未着手

## 2026-08-21

### 実装内容
- **引用元メッセージへのジャンプ機能の恒久修正**: 「メッセージが一定数以上間に挟まると引用元へジャンプできなくなる」という報告を受けて調査。データモデル側（`Message.replyToMessageId`）は元々Firestoreドキュメント ID直接参照で位置に依存しない設計であり、`getMessagesAround`/`getRoomMessagesAround`もID直接取得のためロード範囲に非依存だった。真因はUI側で、`_jumpToMessage`（`chat_screen.dart`）が対象行の`GlobalKey.currentContext`が付くのを待って`Scrollable.ensureVisible`する方式のため、`ListView`の`cacheExtent`（3000px、「ロード済みは最新50件程度」という前提で設定）を超えて対象が離れるとジャンプ不能になっていた。この前提は当日実装済みの1日単位ページネーション（`_olderMessages`が無制限に蓄積）で崩れていた。対症療法（cacheExtent拡大・件数上限）ではなく`scrollable_positioned_list`パッケージへ置き換える根本修正を選択し、`ListView`→`ScrollablePositionedList.builder`、`GlobalKey`ベースの`_messageKeys`→`ItemScrollController.jumpTo(index:)`＋`messageId→index`のマップ、`ScrollController`→`ItemPositionsListener`（無限スクロールの閾値判定に流用）に置き換えた。`flutter analyze`・`flutter test`は通過（既存の無関係な失敗3件は変更前から存在することを確認済み）
- （前回までのセッションで実装され未コミットのままだった分、今回まとめて確認・整理）**メッセージ読み込みの1日単位ページネーション**: 新規`DayMessagesPage`モデル、`DirectMessageRepository`/`GroupRepository`に`watchLatestDay*Messages`（直近暦日をライブ購読）・`loadOlderDay*Messages`（それより古い直近の暦日を1回だけ静的取得）を追加。`chat_panes.dart`で当日分（ライブ）と過去日分（蓄積）を結合、`chat_screen.dart`に`onLoadOlderMessages`/`isLoadingOlderMessages`/`hasMoreHistory`を配線し、末尾に終端表示（`chatNoMoreHistory`）を追加
- **下書き同期の保存不具合修正**: `ConversationPrefsRepository.setDraft`が`set({'draftByRoom.$roomId': ...}, merge: true)`という書き方をしていたが、`set()`はドット区切りキーをネストしたフィールドパスとして解釈しない（`update()`と異なる）ため、下書きが実際には一切保存されていなかった。ネストしたマップリテラルに直して解消
- **語らい切り替えラグの解消**: 分割表示で一度開いた会話（一対・広場）を`IndexedStack`で裏側に保持し続ける方式に変更（`talks_tab.dart`、`_visitedConversationKeys`）。以前は選択中idが変わるたびに寄合一覧ストリームをキャッシュし直す方式だったが、会話一覧の再描画のたびにChatScreenごと作り直され（Firestore購読のやり直し）表示が定着しない不具合があった
- **ピン留めミニ通話表示の拡張**: 映像が表示されている間（`pickCallRenderer`が非null）は右上ミニ表示を画面下端まで伸ばすように変更（`pinned_call_overlay.dart`）
- **リアクション・編集が反映されない不具合の修正**: 「リアクションがつかない」「メッセージの編集ができない」という報告を調査した結果、原因は1日単位ページネーションの`_olderMessages`（過去日、`loadOlderDayMessages`による1回だけの静的取得）にあった。今日以外の日のメッセージへの編集・リアクション・既読・削除・送信取り消し・アカウント削除通知への「いいえ」は、Firestoreへの書き込み自体は成功してもローカルの`_olderMessages`が更新されず画面に反映されなかった（`_liveTailMessages`＝当日分はライブ購読のため元々問題無い）。`DirectMessageRepository.getMessage`/`GroupRepository.getRoomMessage`（単一メッセージの再取得）を新設し、`chat_panes.dart`に`_afterMutation`ヘルパーを追加。`onMarkRead`/`onHideMessages`/`onEditMessage`/`onUnsendMessage`/`onSetReaction`/`onDeclineAccountDeletionNotice`全てをラップし、対象が過去日にあれば操作後に取得し直してローカル反映するようにした
- **メッセージ削除・送信取り消し確認ポップアップのスタイル変更**: `_confirmDeleteSelected`（メッセージ削除）に`constraints: BoxConstraints(maxWidth: 400)`（`group_delete_dialog.dart`と同じパターン）を追加、ボタン文言を「削除する」→「削除」、色を`colorScheme.error`（薄い赤）から`Colors.red.shade700`+白文字（濃い赤）に変更。同じ色の問題があった`_confirmUnsend`（送信取り消し）も色とサイズ制約を同様に修正
- **部分コピーモードの視覚表現**: 長押しメニューの「部分コピー」使用中、当初はメッセージ吹き出し全体を青い枠線で囲む実装にしたが、ユーザーからNotionのスクリーンショットを添えて「テキスト自体を青いラベル（ハイライト）で囲んでほしい」とフィードバックを受け、`SelectionArea`の子を`DefaultSelectionStyle(selectionColor: colorScheme.primary.withValues(alpha: 0.35))`でラップする実装に作り直した（Flutter標準の選択色指定用InheritedWidget、テキストの選択範囲自体がハイライトされる）。全文選択（`selectAll()`）自体は既存のまま
- **長押しメニュー各モードのEscキー対応**: `_ChatScreenState`の`Scaffold`全体を`CallbackShortcuts`+`Focus(autofocus: true)`で包み、Escキーで現在有効なモード（削除選択・スクリーンショット選択・部分コピー・編集/返信中断）を1つ終了する`_handleEscapeKey`を追加。部分コピーは元々×ボタン自体が無かったため、Escが唯一のキャンセル手段になる
- Web版で白画面になり何も表示されない不具合が発生。複数回のホットリスタート（`kill -SIGUSR2`）を重ねた後に発生し、ログにはエラーが出ていなかった。`flutter run`プロセスを完全に終了し再起動することで復旧した（根本原因は未特定、`web-server`デバイスでのホットリスタート信頼性の限界の可能性）

### 詰まったところ
- ユーザーから「送信時にメッセージへ番号を振ってIDで追跡すれば、間にいくつメッセージが挟まっても・編集されても追跡できるはず」という設計上の指摘があったが、調査の結果その設計（Firestoreドキュメント ID参照）は既に実装済みだった。問題の所在をデータモデルではなくUI側のレンダリング（`cacheExtent`とGlobalKeyのビルド待ち）に正しく切り分けられたのは、Explore→Planサブエージェントによる2段階の詳細調査のおかげ
- 部分コピーモードの視覚表現は、最初の実装（吹き出し全体を枠線で囲む）がユーザーの意図と異なっていた。「青いラベルで囲む」という指示だけでは枠線／テキストハイライトのどちらを指すか判別できず、実装後にフィードバックを受けて作り直す形になった。次回同種の要望では、実装前に見た目のイメージ（枠か、テキスト自体のハイライトか）を確認した方がよい
- Web版の白画面は、ログ上エラーが一切出ないまま発生したため原因の特定はできなかった。ホットリスタートの繰り返しが引き金になった可能性はあるが未検証

### 次にやること
- 白画面不具合の再発有無を確認する（`flutter run`プロセスの完全再起動で復旧済み、根本原因は未特定）
- リアクション・編集の反映不具合修正、削除確認ポップアップのスタイル変更、部分コピーのテキストハイライト、長押しメニュー各モードのEscキー終了、送信取り消しポップアップのサイズ変更を実機確認する
- 引用元メッセージへのジャンプ機能（`scrollable_positioned_list`移行）を、大量にメッセージが間に挟まった状態・未ロードの古い日への遷移・通常スクロール・無限スクロール（過去日の自動読み込み）・ファイル添付ブロックの展開/折りたたみ時のスクロール補正の観点でブラウザ実機確認する（`flutter analyze`・`flutter test`は通過済み）
- メッセージ読み込みの1日単位ページネーション・下書き同期の保存不具合修正・語らい切り替えラグ解消（IndexedStack化）・ピン留めミニ通話表示の映像時拡張を実機確認する
- 通話中央アイコン修正・通話UI再設計（PC埋め込み表示/右上ピン留めミニ表示/モバイルのドック化）・動画ビューア拡充（シークバードラッグ・10秒送り戻し・fキーFullscreen・トラックパッド閉じ）をブラウザで実機確認する（`flutter analyze`は通過済みだが未検証）
- 手描きのアプリアイコン・起動スプラッシュ・イメージキャラクター素材が用意され次第、`assets/branding/app_icon_source.png`に配置し`dart run flutter_launcher_icons`・`dart run flutter_native_splash:create`を実行する（pubspec.yamlの設定基盤は維持済み。AI生成分は不採用が確定し`assets/branding/`以下に参考として残置のみ）
- 広場作成の不具合修正（`GroupRepository.createGroup`のroom作成バッチ分離＋基準ロールルール修正）は`firestore.rules`をdaidai-rhingへデプロイ済みだが、ユーザーによる動作確認はまだ得られていない
- daidai横丁専用の呼び名・アイコン機能（`functions/src/index.ts`・`storage.rules`）が本番Firebaseへデプロイ済みかどうか未確認
- 「メッセージ画面に戻るボタンが効かない」不具合は対症療法で対応したが、正確な原因は未特定。再発する場合は追加調査が必要
- ペタピタのLINE型送信方式を実機で動作確認する
- daidai横丁再開に向け、macOSの配布方式を決定する
- Stripeに登録する銀行口座を決定する
- E2E暗号化が未完了のままアプリをリリースするかどうかを判断する
- daidai横丁の法務整備は`競合調査.md`に記録済みだが未着手

## 2026-08-18

### 実装内容
- **通話中央アイコン不具合修正**: 音声通話（および相手の映像が届くまでのビデオ通話プレースホルダー）の中央アバターが、蔵/工房で設定した会話ごとのアイコン（`AppUser.effectiveIconFor`）を無視し、Rhing ID頭文字だけの色付きイニシャルアバターに固定表示されていた不具合を修正。`chat_screen.dart`の`_SenderAvatar`と同じ「`watchedUserProvider`で`AppUser`取得→`effectiveIconFor(会話id)?.url`があれば`NetworkImage`、無ければイニシャル」というロジックを共有ウィジェット`CallParticipantAvatar`（新規`lib/features/call/call_avatar.dart`）に切り出し、`Call.otherUserId`を新規追加した上で、1対1全画面（`CallScreen`）・広場全画面（`GroupCallScreen`）・PC埋め込み表示（`EmbeddedCallPane`）・右上ピン留めミニ表示（`PinnedCallOverlay`）の計5箇所全てで置き換えた
- **通話UI再設計**（前回までのセッションで実装され未コミットのままだった分、今回まとめて内容を確認・整理）: 通話コントローラーの所有権を画面のマウント状態から新設の`activeCallSessionProvider`（`active_call_session.dart`）へ移し、画面を閉じても通話が継続するように変更（`CallScreen`/`GroupCallScreen`はセッションへの再アタッチのみを行う）。PC/Web向けにメッセージ画面内へ通話UIを直接埋め込み表示する`EmbeddedCallPane`、右上に常時表示できるミニ表示`PinnedCallOverlay`（音声のみの通話も対象）を新設。モバイルは映像ありの通話中に下スワイプでドック（ミニ化）可能に。1対1・広場で重複していた通話コントロールボタン列を`CallControlBar`に統合。カメラの有無判定（`camera_availability.dart`）を追加しカメラ無し端末でビデオ通話の選択肢を隠すように変更、従来の「カメラ一時停止」機能（`toggleCamera`/`CallParticipant.cameraOff`）は削除
- **動画ビューアの機能拡充**（同じく前回セッション分の未コミット整理）: 中央に再生/一時停止＋前後10秒送り/戻しの3ボタンを追加しシークバーをドラッグ操作可能に変更、モバイルはダブルタップでの10秒送り/戻しに対応、`f`キーでブラウザ本来のFullscreen API（タブ・アドレスバーごと隠す）をトグルする機能を追加（`lib/utils/fullscreen/`新設）、トラックパッド/マウスホイールの上スクロールでメディアビューアを閉じる機能を追加
- アプリアイコン・起動スプラッシュ・イメージキャラクター「凛空（りんく）」をHiggsfieldで生成し（`assets/branding/`）、`flutter_launcher_icons`/`flutter_native_splash`の設定基盤をpubspec.yamlに追加したが、橙の皮剥き動画への具体的指摘（動作の因果関係・剥く向きと皮の向きの不一致・陰影/線のコントラスト不足）を受けて`orange_peel_v2`として再生成した後も「まだまだ品質が低い、自分で1から描く」とユーザーが最終的に不採用を確定。AI生成によるアイコン・スプラッシュ・キャラクター制作は終了し、手描きで制作する方針になったため、Flutter側の反映（アイコン・起動画面）は`git checkout`でデフォルトへ戻した（生成物自体は参考として`assets/branding/`以下に残置）

### 詰まったところ
- `PopScope`の`canPop`が再構築を経てからしか反映されないため、ドック化の`setState`直後に同期的に`Navigator.pop`するとブロックされる問題があり、`addPostFrameCallback`で次フレーム後にpopするよう対処した
- `CallControlBar`でボタンの表示/非表示によって`spaceEvenly`が他ボタンの位置までずらしてしまう不具合があり、常に同じスロット数を保持し`IgnorePointer`+`Opacity(0)`で見た目とタップ判定だけを消す方式に変更した
- `flutter_webrtc`の`Helper.cameras`がWebで権限許可前にカメラ台数を過少報告する挙動があり、「無し」と「不明」を区別せず判定するとビデオ通話の選択肢を誤って隠してしまっていた
- トラックパッド/ホイールでのメディアビューアpop実装で、2本指スワイプ等が1ジェスチャーで大量の`PointerScrollEvent`を発火するため即座に`pop()`すると残りイベントが背後のメッセージ一覧に漏れて意図しないスクロールが起きる不具合があり、150msのデバウンスで新規イベントが止まってからpopする方式で解消した
- Web（`video_player_web`）は`<video>`要素をDOMに直接描画するため動画本体のクリックがFlutterのジェスチャー検出まで届かず、透明なオーバーレイの`GestureDetector`を重ねて捕捉し直すことで対応した

### 次にやること
- 通話中央アイコン修正・通話UI再設計（PC埋め込み表示/右上ピン留めミニ表示/モバイルのドック化）・動画ビューア拡充（シークバードラッグ・10秒送り戻し・fキーFullscreen・トラックパッド閉じ）をブラウザで実機確認する（`flutter analyze`は通過済みだが未検証）
- 手描きのアプリアイコン・起動スプラッシュ・イメージキャラクター素材が用意され次第、`assets/branding/app_icon_source.png`に配置し`dart run flutter_launcher_icons`・`dart run flutter_native_splash:create`を実行する（pubspec.yamlの設定基盤は維持済み。AI生成分は不採用が確定し`assets/branding/`以下に参考として残置のみ）
- 広場作成の不具合修正（`GroupRepository.createGroup`のroom作成バッチ分離＋基準ロールルール修正）は`firestore.rules`をdaidai-rhingへデプロイ済みだが、ユーザーによる動作確認はまだ得られていない
- daidai横丁専用の呼び名・アイコン機能（`functions/src/index.ts`・`storage.rules`）が本番Firebaseへデプロイ済みかどうか未確認
- 「メッセージ画面に戻るボタンが効かない」不具合は対症療法で対応したが、正確な原因は未特定。再発する場合は追加調査が必要
- 返信ジャンプ機能・ペタピタのLINE型送信方式・入力欄の複数端末下書き同期機能を実機で動作確認する
- daidai横丁再開に向け、macOSの配布方式を決定する
- Stripeに登録する銀行口座を決定する
- E2E暗号化が未完了のままアプリをリリースするかどうかを判断する
- daidai横丁の法務整備は`競合調査.md`に記録済みだが未着手

## 2026-08-13

### 実装内容
- 「シンプルUI」を「フラットUI」に全面改名（`AppUiStyle`のenum値・l10nフィールド名・CLAUDE.mdの該当箇所）
- フラットUIの一対/広場切り替えチップを、劇画UIと同じ単一`Text`によるコンパクト表示に変更し`Wrap`で横並びに修正（`talks_tab.dart`）。切り替え行と語らい一覧の間の余白を両UIスタイル共通で確保し重なりを解消
- チャット画面AppBarの背景色がスクロール時に本文と異なって見える不具合を`surfaceTintColor: Colors.transparent`・`scrolledUnderElevation: 0`で修正（`app_theme.dart`、劇画UIは前日修正済みだったパターンを踏襲）
- 設定・寄合一覧の選択中リストタイルが区切り線に重なる不具合を、`tileColor`/`selectedTileColor`の使い分けと`ListView`パディング追加で修正
- 返信メッセージのジャンプ機能を実装。当初は吹き出し全体にタップ判定を付けたためジェスチャー競合で画像/動画/URLリンクにタップを奪われる不具合があり、返信引用ボックス単体にタップ判定を限定して解消。その後も「ジャンプしない」報告が続き、ビューポート内可視性の直接検証・再試行ロジックを追加、最終的に`Scrollable.ensureVisible`のアニメーション時間を`Duration.zero`にして`_markUnreadMessages`のFirestoreエコーバックとの競合を根本的に解消した
- 画像・動画プレビューの角丸が場所によって不揃いだった不具合を、共通ウィジェット`lib/widgets/media_preview_frame.dart`への切り出しで解消（`chat_screen.dart`・`link_preview_card.dart`の重複実装を統合）。劇画UIの写真マットも角カットの面取りをやめて直角＋黒枠のデザインに変更
- 承認待ちの一対にも他の語らいと同様に寄合一覧ペイン（`RoomListPane`）を適用
- メッセージ画面の長押しによる自動スクロール機能を廃止（ミドルクリックによる自動スクロールは維持）
- 広場設定ポップアップの高さを「自分のプロフィールカード」表示の有無で2種類に分岐、広場削除確認ポップアップを縦長・横幅制限ありに変更しボタンを固定の濃い赤（`Colors.red.shade700`）に統一
- アカウント削除通知の「はい」ボタンをUIスタイル別に配色分岐（フラット: 濃い赤、劇画: 白地黒文字）。寄合削除確認ポップアップのボタンも「削除する」→「削除」に変更し濃い赤に統一
- CLAUDE.mdに「薄い地色に白文字のボタンの組み合わせを禁止」するデザインルールを追記（`colorScheme.error`がダークテーマで薄いサーモンピンクになる問題への対処を恒久ルール化）
- ペタピタ送信方式選択機能を新規実装。LINE型（1回タップで拡大プレビュー、再タップで送信、標準）とDiscord型（1タップ即送信）を切り替え可能にし、拡大プレビューはルート`Overlay`への`OverlayEntry`挿入方式で実装（`sticker_picker_content.dart`）
- 設定タブに「詳細設定」という折りたたみアコーディオンUIを新設（`SettingsAccordionSection`、`AnimatedSize`によるスライド開閉）。設定＞語らい＞詳細設定にペタピタ送信方式設定を配置
- メッセージ入力欄の下書きを複数端末で同期する機能を実装（標準オン、設定＞語らい＞詳細設定に配置）。会話を開いた時に`ConversationPrefs.draftByRoom`から復元し、入力中は600msデバウンスでFirestoreへ保存、送信・編集時は適切にクリア/抑制する
- daidai横丁のGoogle Play/App Store外部決済リンク規約をサブエージェントで3回に分けて調査し、`競合調査.md`に「4段階の配信方針（日本限定配信→複数プラットフォーム配信＋URL配置→ストア版のみ手数料上乗せ→将来の地域判別ロジック導入）」として記録
- 用語・表示設定（世界観重視／利便性重視の切り替え）を廃止し、世界観重視の用語へ一本化した。言語・用語設定の組み合わせによって画面上のブロックの大きさが変わるため、全てに対応するのが困難になったのが理由。`TerminologyStyle`・`terminologyStyleProvider`・設定タブの該当セクションを削除し、`Vocabulary`は表示言語（日本語／English）のみの1軸に縮小した
- 一対/広場切り替えタブのサイドバー幅（`talks_tab.dart`のRow）が240pxのままだと、English表示時（"Private N"/"Plaza N"）にタブ＋追加ボタンが収まりきらない不具合があったため300pxへ拡張。言語間でブロックの大きさを変えない方針のため日本語表示時も同じ幅にした
- daidai横丁のペタピタについて、他人が送信済みの所持パックをタップした際にそのパックへ絞り込んだ送信ピッカーを開き、未所持なら横丁ストアページ（rhing.jp、別リポジトリ）をブラウザの別タブで開く機能を実装（`StickerRepository.findPackIdForSticker`/`ownsPack`追加、`StickerPickerContent`/`StickerPickerSheet`に`initialPackId`追加）
- 上記ポップアップの表示位置を送信アイコンと完全に一致させるよう修正。送信アイコンの`GlobalKey`（`_stickerButtonKey`）を`_MessageRow`へ渡して共有し、`showStickerPickerPopup`を`anchorKey`ではなく呼び出し側計算済みの`anchorRect`を受け取る形に汎用化した
- 語らい一覧・身だしなみ・設定の3画面のサイドバー幅を、区切り線のx座標を揃える目的で複数回調整（300→240→劇画UIでPlaza切り替えタブが「＋」ボタン手前でクリップされる不具合発覚→260→250→260、最終的に3画面とも260pxで統一）。劇画UIの原因は、一対/広場タブ（`GekigaJointedPair`）が横スクロール式の`SingleChildScrollView`内にあり折り返せない設計だったため、サイドバー幅を縮めると初期表示でタブの右端がクリップされて見えなくなっていたことだった
- 画像/動画の拡大表示に、同じ語らい内の前後の画像/動画への横スワイプ・矢印キーナビゲーションを追加。`_ImageViewerScreen`/`_VideoViewerScreen`を、共通のギャラリー画面`_MediaViewerScreen`（`PageView`）と中身のみの`_ImageViewerPage`/`_VideoViewerPage`に再編。動画は直接プレビューをタップして開いた場合のみ自動再生し、スワイプ/矢印キーで切り替えた先の動画は中央の再生ボタンをタップするまで一時停止のままにした
- 動画再生にスペースキーでの再生/一時停止トグル、動画の座標上（レターボックス含む）どこをクリックしても再生/一時停止できる操作、10秒送り/戻しボタンを追加。あわせて、スワイプで動画ページへ遷移した直後は再生ボタンを押しても再生できない不具合を、「動画タップでトグル」と「動画以外のタップで閉じる」の入れ子になっていたジェスチャー判定を単一のトグル用`GestureDetector`に一本化することで解消し、メディアビューアーのAppBar戻るボタンも自動生成任せから明示的な実装に変更した
- QRコード経由の「仲間申請」画面・語らいの「友達を追加」ダイアログで「送信」/「追加」ボタンが表示されない不具合を修正。原因はテーマの`ElevatedButtonThemeData.minimumSize`（横幅`double.infinity`）を持つ`ElevatedButton`を、キャンセルボタンと横並びの`Row`へ直接（`Expanded`等で囲まずに）置いていたため幅の制約が衝突してレイアウトが破綻し、破綻した描画が背景色に紛れて実質見えなくなっていたこと。両箇所とも`Expanded`で囲んで解消した
- 「使うプロフィールカード（省略可）」ラベルから「（省略可）」を削除。あわせて共通ウィジェット`ProfileCardPicker`（友達申請3系統・広場参加・設定タブ計5箇所で使用）を、`ChoiceChip`＋`Wrap`から、アプリの他の単一選択UI（`RadioGroup`+`RadioListTile`、劇画UI時は`GekigaJointedTileList`+`GekigaTileContent`）に統一した
- （別途、本日中に着手済みの未コミット作業）daidai横丁のフォロー・お気に入り機能に向け、`users/{uid}/follows`・`users/{uid}/favoritePacks`サブコレクションのfirestore.rulesと、出品者の公開プロフィールミラー`creatorProfiles/{userId}`（Cloud Functions経由のみ書き込み可、`syncCreatorProfile`トリガーで`users`ドキュメント更新に追随）を追加
- 広場作成時に`[cloud_firestore/permission-denied]`エラーが発生する不具合を2件特定・修正。(1) `GroupRepository.createGroup`が広場ドキュメントとデフォルト寄合を同一batchで作成していたが、寄合のcreateルール`hasGroupPermission`が`groups/{groupId}`を`get()`参照するため、同一バッチ内では未コミットの広場を見つけられずdenyされていた（`groupInvites`/`roles`は既に別書き込みに分離済みだったが`roomRef`だけ対応漏れだった）。(2) 基準ロール（`isEveryone: true`の「全員」ロール）の作成が、`roles`サブコレクションのcreateルールで`isEveryone == false`を無条件に要求していたため常に拒否されていた。既存広場は一度きりのCloud Function移行（Admin SDK、ルール迂回）で基準ロールを作成済みだったため、これまで新規広場作成時にしか顕在化していなかった。両方を修正し、`firestore.rules`をdaidai-rhingプロジェクトへデプロイした

### 詰まったところ
- 返信ジャンプ機能は3回連続で「直っていない」という報告を受けた。1回目はジェスチャー競合の解消のみで不十分、2回目はビューポート可視性検証を追加したが依然不十分、3回目でようやく`_markUnreadMessages`の既読書き込みがFirestoreストリームへエコーバックしメッセージ一覧の再構築がアニメーション中に競合を起こしていたことに気づき、アニメーション自体を無くして解消した。この修正はブラウザ環境での動作確認手段が無いため未検証のまま完了報告した点は反省点
- `_draftSyncActive`のgetterで`ref.read(draftSyncEnabledProvider)`を条件式の先頭に置いてしまい、`roomId`未指定（下書き同期非対象）の既存テストでもプロバイダが評価されて`ProviderScope`未overrideの`UnimplementedError`が発生。nullチェックを先に評価する順序に直して解消した
- 新規追加した`_DraftSyncFolder`が設定タブに常時マウントされる（アコーディオンの折りたたみは見た目だけで子ウィジェット自体は破棄されない）ため、`settings_navigation_test.dart`が新規プロバイダ未overrideで失敗。テスト側にも`initialStickerSendModeProvider`/`initialDraftSyncEnabledProvider`のoverrideを追加して解消した
- 劇画UIの写真マットを直角デザインへ作り直す過程で、共通ウィジェット切り出し時に`gekiga_shapes.dart`のimportを誤って削除し、無関係な`_GekigaBubblePainter`等が壊れる事故があったが、エラーメッセージからすぐに気づき復元した
- 所持ペタピタタップ時のポップアップ座標は、1回目の修正（タップ位置基準）では「送信アイコンをクリックした時と同じ座標で開いてほしい」というユーザーの意図と異なっており、指摘を受けて送信アイコンの`GlobalKey`をそのまま共有する方式に作り直した
- サイドバー幅を設定画面に揃えるため一旦240pxへ縮める際、事前に「劇画UIでPlazaタブが欠ける可能性がある」とユーザーに警告した上で了承を得て変更したところ、実際にその通りの不具合が発生した。原因調査自体は正確に行えたが、修正方針（`FittedBox`による縮小表示）を提案した後、ユーザーは代わりに3画面ともサイドバー幅を広げる方針を選び、その後も260→250→260と数値の微調整が続いた
- 動画ギャラリーで「スワイプ後に再生ボタンが効かない」不具合は、正確な発生メカニズム（Flutterのジェスチャーアリーナの詳細な挙動）を完全には特定できておらず、「動画タップのトグルと動画以外タップの閉じるが入れ子になっている構造自体を無くす」という対症的なアプローチで解消した
- 「メッセージ画面に戻るボタンの座標をクリックしても戻れない」報告も、正確な原因は再現・特定できておらず、AppBarの戻るボタンをFlutterの自動生成（`automaticallyImplyLeading`）任せから明示的な`onPressed`実装に置き換える対症的な修正に留めた
- 仲間申請/友達追加の送信・追加ボタンが表示されない不具合は、ボタン自体はコードに存在し`onPressed`もnullではなかったため当初原因が分かりにくかったが、サブエージェントに調査を委任し、テーマの`ElevatedButtonThemeData.minimumSize`（横幅`double.infinity`）と`Row`直置きの組み合わせによるレイアウト制約の衝突が原因と特定できた
- 広場作成の不具合は1回目の修正（roomRefのバッチ分離）だけでは直らず、「また同じエラー発生」という報告を受けてから2つ目の原因（rolesのisEveryoneルール）に気づいた。firestore.rulesの修正はローカルファイルの変更だけでは反映されず、実際のFirebaseプロジェクトへのデプロイが必要な点も、ユーザーに確認を取った上で`npx firebase-tools deploy --only firestore:rules`を実行して初めて有効化した

### 次にやること
- 広場作成の不具合修正（2件）は`firestore.rules`をdaidai-rhingへデプロイ済みだが、ユーザーによる実際の動作確認（広場作成が成功するか）はまだ得られていない
- 今回の一連の修正（ペタピタ横丁遷移・ポップアップ座標、語らい/身だしなみ/設定サイドバー幅、画像/動画ギャラリーのスワイプ・矢印キー・スペースキー・10秒送り戻し、仲間申請・友達追加ボタン表示、プロフィールカード選択UI刷新）をブラウザで実機確認する（`flutter analyze`/`flutter test`は通過済みだが未検証）
- 「メッセージ画面に戻るボタンが効かない」不具合は対症療法（AppBar戻るボタンの明示化）で対応したが、正確な原因は未特定。再発する場合は追加調査が必要
- 返信ジャンプ機能（`Duration.zero`修正）が実際に直ったか、ブラウザで確認する（3回連続で「直っていない」と報告された経緯があるため特に注意）
- ペタピタのLINE型送信方式・入力欄の複数端末下書き同期機能を実機で動作確認する
- daidai横丁再開に向け、macOSの配布方式（Mac App Store経由か直接配布か）を決定する
- Stripeに登録する銀行口座を決定する（ゆうちょ／みんなの銀行／PayPay銀行で検討中）
- E2E暗号化が未完了のままアプリをリリースするかどうかを判断する
- daidai横丁の法務整備（Microsoft Store・Flathubの規約詳細調査、地域判別ロジックの実装）は`競合調査.md`に記録済みだが未着手

## 2026-08-16

### 実装内容
- daidai横丁専用の呼び名・アイコン（`users/{uid}.daidaiNickname`/`daidaiIconUrl`）を追加。DaiDai本体の身だしなみ（アクティブなニックネーム/アイコン）とは独立した任意設定で、設定されていればそちらを優先し、無ければ従来通りDaiDaiのアクティブ素材にフォールバックする（`functions/src/index.ts`の`buildCreatorProfileFields`）
- 上記アイコンのアップロード先として`storage.rules`に`daidaiYokochoProfile/{userId}/`を追加（読み取りは誰でも可、書き込みは本人のみ）

### 詰まったところ
- 特になし（このセッションでは直接実装しておらず、詳細な経緯は不明）

### 次にやること
- 広場作成の不具合修正（`GroupRepository.createGroup`のroom作成バッチ分離＋基準ロールルール修正）は`firestore.rules`をdaidai-rhingへデプロイ済みだが、ユーザーによる動作確認はまだ得られていない
- daidai横丁専用の呼び名・アイコン機能（`functions/src/index.ts`・`storage.rules`）が本番Firebaseへデプロイ済みかどうか未確認
- 直近の一連の修正（ペタピタ横丁遷移・ポップアップ座標、語らい/身だしなみ/設定サイドバー幅、画像/動画ギャラリーのスワイプ・矢印キー・スペースキー・10秒送り戻し、仲間申請・友達追加ボタン表示、プロフィールカード選択UI刷新）をブラウザで実機確認する
- 「メッセージ画面に戻るボタンが効かない」不具合は対症療法で対応したが、正確な原因は未特定。再発する場合は追加調査が必要
- 返信ジャンプ機能・ペタピタのLINE型送信方式・入力欄の複数端末下書き同期機能を実機で動作確認する
- daidai横丁再開に向け、macOSの配布方式を決定する
- Stripeに登録する銀行口座を決定する
- E2E暗号化が未完了のままアプリをリリースするかどうかを判断する
- daidai横丁の法務整備は`競合調査.md`に記録済みだが未着手

## 2026-08-12

### 実装内容
- 劇画UI入力欄のペタピタ送信アイコンの固定位置を修正。前回の`Stack`＋`Positioned(top:0)`方式が機能しなかったため`Row`＋`CrossAxisAlignment`方式へ作り直したが、その際`.start`（上揃え）としたのが誤りだった。入力欄コンテナは画面下端に`Positioned(bottom:0)`で固定され行数が増えると上方向にしか伸びないため、`.end`（下揃え）に修正し、行数によらずアイコンの絶対位置が変化しないようにした
- 劇画UIの吹き出し・入力欄で、枠取り（`MonochromeBoxPainter`の`thicknessBase`）が箱の短辺（`size.shortestSide`）に比例して際限なく太くなり、縦長の吹き出しや複数行の入力欄で文字と枠が重なる不具合を修正。ユーザーと相談の上、枠の太さを箱のサイズに関わらず固定値（40.0）にする方針で合意し実装した
- 上記の残存する重なりへの対応として、吹き出し・入力欄それぞれの余白を追加調整（吹き出し: 横28→30px/縦14→18px、入力欄: 横20→24px/縦10→16px）。入力欄TextFieldの`contentPadding`が縦方向のみ指定され水平方向が意図せず0pxになっていた副作用も修正（横4pxを追加）。入力欄が1行のときペタピタアイコン（48px固定）とテキストフィールドの高さが揃わず文字が下寄りに見えていた問題は、TextFieldに`ConstrainedBox(minHeight: 48)`を追加して解消
- Markdown添付プレビュー（`_FileAttachmentBlock`）を3点改善: (1) メッセージ一覧が`ListView`でも画面外に出るとStateごと破棄され再度取得し直していた再読み込み問題を、`AutomaticKeepAliveClientMixin`とURLキーの静的キャッシュで解消。(2) footerにコピーアイコンを追加（`Clipboard.setData`のみ、確認スナックバーは出さない方針に途中で変更）。(3) 全表示を一度`showDialog`のポップアップ方式で実装したが、「メッセージ画面のままで拡大表示してほしい」という指摘を受けて撤回し、固定高さ（140px⇔360px）でスクロール可能なインライン拡大方式に変更した
- コピー・ダウンロードアイコンの半透明の黒丸背景を、footer内では削除（周囲のプレーンなアイコンと統一）。ホバー時に浮かぶダウンロードボタン（画像等の上に重なる用途）は丸背景を維持
- Markdown添付の拡大表示が下端を起点に上方向へ伸びてしまう不具合を修正。メッセージ一覧が`ListView(reverse: true)`のため、Sliverの座標系上「配列上の開始位置＝画面下端」が固定されアイテムの高さ変化は上端側だけに反映される仕組みだったことが原因。高さ変化時に`Scrollable.maybeOf(context)`で取得した祖先のスクロール位置を変化量（220px）分だけ補正し、上端を起点に下方向へ伸びるように変更した
- `/diary`スキルを更新し、日記.mdの最上部に常設の`# TODO`ブロックを追加する運用を追加（実行のたびに「次にやること」の内容で丸ごと置き換える）
- 管理画面（`/admin`）を`npx firebase-tools`（グローバル`firebase` CLI未導入のため）でCloud Functionsをデプロイして稼働確認。`/#/admin`ではなく`/admin`（`usePathUrlStrategy()`採用のためハッシュ無し）でアクセスする必要があると判明
- 管理画面の配信画面・住人側のお知らせ閲覧画面を、専用の簡易UIから通常の語らいと同じ`ChatScreen`ベースに統一。新規`lib/features/chat/announcement_screen.dart`（`AnnouncementScreen`）を追加し、住人側は設定＞運営から`/announcements`ルートで直接開く
- 管理画面を5点整理: 「住人一覧」「お知らせ」を左サイドバー（`NavigationRail`）化、「配信画面」「便り」の呼称を「お知らせ」に統一、住人向け設定の「お知らせ」項目が「準備中」のままだった不具合を`_openAnnouncements`実装で解消、管理画面から送っても「まだメッセージはありません」と表示される不具合を修正、赤帯の注意書きを削除。上記のメッセージ不具合は`users`ドキュメントの`accountStatus`フィールドが2026-07-28以前作成のアカウントに存在せず、Firestoreの等価クエリ（`where("accountStatus","==","active")`）がフィールド欠落ドキュメントを暗黙に除外していたことが原因と判明し、一度きりの移行Cloud Function（`backfillAccountStatusOnce`）で補修した（実行後の後始末は未確認のため上記TODO参照）
- モバイルUIの通知用ボトムシート（SnackBar）を全廃し、画面上部に3秒間自動表示されるバナー（`lib/utils/auto_dismiss_banner.dart`の`showAutoDismissBanner`）に統一。成功通知は完全に廃止し、エラー系のみバナーで残す方針をユーザーと確認した上で全19箇所（`chat_screen.dart`・`chat_panes.dart`・`settings_tab.dart`・`profile_tab.dart`等）を置き換え
- UI/UX微調整6件を実施: ボタン押下時の半透明リップルエフェクトを`ThemeData.splashFactory`で全廃、返信/編集モード開始時にテキスト欄へ自動フォーカス、ホバー時のツールチップを全廃（`IconButton.tooltip`削除・`Tooltip`ラップ解除・`GekigaIconButton`から`tooltip`引数自体を削除）、モバイルでメッセージ送信後もソフトウェアキーボードが閉じないよう送信ボタンに`canRequestFocus: false`＋送信後の明示的`requestFocus()`を追加、ペタピタ選択シート上部の見出しテキストを削除、承認待ちの語らいを開くと右スワイプで戻れない不具合を`SwipeBackDetector`でのラップにより修正
- 設定＞運営を整理: 未実装だった「ホームページのURL」「質問フォーム」の行を削除、便り（公式アカウント）との一対が語らい一覧に混ざって表示されていたのを`talks_tab.dart`側でフィルタして除外、「運営」タップで中間ページを経由せず直接お知らせ画面が開くようにし、広い画面では語らいの分割表示と同じく設定サイドバーを残したまま内容ペインにお知らせを表示（狭い画面のみ`/announcements`へフルスクリーンpush）。`ChatScreen.onSend`をnullable化し、入力欄自体を出さない読み取り専用モードを追加
- 管理画面に「身だしなみ」タブを新設し、便り（`official-tayori`）の呼び名・アイコンを管理者が編集できるようにした。便りはFirebase Authの実アカウントを持たないため、firestore.rules/storage.rulesに「管理者クレームかつ対象uidが`official-tayori`の場合のみ」書き込みを許可する限定的な例外（`isAdmin()`関数）を追加
- 管理画面での便りの呼び名・アイコン編集がユーザー側お知らせ画面に反映されない件を調査。原因はキャッシュ不具合ではなく「既定のレイアウトスタイル（`ChatLayoutStyle.sideBySide`）では一対の吹き出しに相手のアイコン・呼び名を表示せず、AppBarタイトルも寄合名（固定文字列「メイン」）であって相手の呼び名ではないため、便りの身だしなみを表示する場所がそもそも画面内に存在しなかった」ことと判明。一度`_OfficialAccountHeader`（アイコン＋呼び名のライブ表示、`watchedUserProvider`購読）を追加したが、ユーザー判断で不要と分かり撤去した
- お知らせ画面の質問フォーム導線を、AppBarの小さいアイコンボタンから`ChatScreen.banner`を使った右寄せの縦長ブロックに変更。劇画UIでは`GekigaJointedTileList`+`GekigaButton`（単体枠付きボタンとして再利用する明記済みパターン）、シンプルUIでは`colorScheme.primaryContainer`ベースの配色にして、現在のUIスタイルに応じた見た目にした
- 通知バナー（`showAutoDismissBanner`）を`ScaffoldMessenger`の永続バナー領域を使う実装から`Overlay`を使う実装に変更。画面のコンテンツを下に押しやらず、画面上部にカードとして浮かぶ形にした。関数シグネチャは変更せず全呼び出し元は無改修、独自アクションボタンから直接`ScaffoldMessenger.hideCurrentMaterialBanner()`を呼んでいた4箇所のみ新設の`dismissAutoDismissBanner()`に置き換えた

### 詰まったところ
- ペタピタアイコンの固定位置修正は2回連続で意図と逆の実装をしてしまった。1回目は`Stack`+`Positioned(top:0)`が機能せず、2回目に`Row`+`CrossAxisAlignment.start`（上揃え）へ変更したが、これも実際には逆方向（入力欄が画面下端固定のため、上揃えだと行が増えるほどアイコンが上へ動いてしまう）の誤りだった。ユーザーからの「まだ直っていない」という報告を2回受けてから、入力欄コンテナ自体がbottom固定である点を見落としていたことに気づき`.end`に修正した
- 枠の太さが際限なく太くなる問題は、`MonochromeBoxPainter`の`insetPolygon`がマイター結合（隣接辺の交点計算）を使っているため、傾きで歪んだ四隅では平らな辺より内側への食い込みが大きくなるという幾何学的な特性の理解に調査が必要だった
- Markdown添付の拡大表示の伸びる向きの問題は、`reverse: true`のListViewにおけるSliverのスクロール座標系（アイテムごとの「配列上の開始位置」と「画面上の位置」の対応関係）を数式で導出しないと、なぜ下端が固定されるのか・どう補正すれば上端を固定できるのかが分からなかった
- `IconButton.tooltip`（省略時null＝非表示）と`PopupMenuButton.tooltip`（省略時「メニューを表示」等の非null文言＝表示される）とでデフォルト値の仕様が違う点を見落とさないよう、削除対象を一律`tooltip:`の行削除ではなく個別に判定する必要があった
- 承認待ちの語らいの右スワイプ不具合は、当初`onSwipeBack`を`ChatScreen`に渡すだけで直ると想定していたが、実際には対象画面のメッセージが常に空でメッセージ吹き出し自体が無いため、吹き出し単位のスワイプ検知（`_MessageInteractions`）が機能する余地が無いことが根本原因だった。画面全体を包む外側の`SwipeBackDetector`でラップする方式に切り替えて解決した
- お知らせ画面の身だしなみ未反映は、`watchedUserProvider`がFirestoreの真のライブストリームであることを確認して「同期不具合」の可能性を除外した上で、`chat_screen.dart`の`showAvatarAndName`判定・`ChatLayoutStyle`・`DmChatPane`のタイトルが実は「寄合名」であって「相手の呼び名」ではないことをコードから辿るまで、そもそも表示箇所が存在しないというシンプルな原因に気づけなかった
- firestore.rulesの編集で、既存コメント中の全角括弧（（）と半角括弧の混同によりEdit toolの文字列一致が失敗した。正確な文字を再度Readで確認してから修正した
- 通知バナーをOverlayベースに変更するにあたり、独自アクションボタンの`onPressed`内で直接`ScaffoldMessenger.of(context).hideCurrentMaterialBanner()`を呼んでいた4箇所（`chat_screen.dart`3箇所・`settings_tab.dart`1箇所）を見落とすと動作しなくなるため、`grep`で全呼び出し元を洗い出してから漏れなく`dismissAutoDismissBanner()`に置き換えた

### 次にやること
- 本日の一連の修正はいずれも`flutter analyze`/`flutter test`（既存の無関係な3件を除き）は通過済みだが、ブラウザでの実機確認はユーザー側で継続中
- Markdown添付の拡大表示のスクロール位置補正は、方向が逆に感じられた場合は`delta`の符号を反転するだけで直せる想定だが、まだユーザーからの確認待ち
- diaryスキルの新しい最上部TODOブロック運用は今回が初回適用のため、次回以降の`/diary`実行で正しく機能するか確認する
- `backfillAccountStatusOnce`の冪等性確認（2回目実行で`backfilled: 0`になること）が未完了。確認後、一時的な補修コード（ボタン・repositoryメソッド・Cloud Function・テストfake）と`grantFirstAdminOnce`をまとめて後始末する
- モバイル実機・DevToolsのモバイルエミュレーションでのソフトウェアキーボード維持確認が未完了
- お知らせ画面の質問・要望ブロックの見た目・Overlayベースの新しい通知バナーの見え方は、直近の変更でホットリスタートまでは確認済みだがユーザー本人によるブラウザ確認は未了
- firestore.rules/storage.rulesに追加した便り身だしなみ編集用の管理者例外が、本番Firebaseへデプロイ済みかどうか未確認

## 2026-08-11

### 実装内容
- シンプルUIのURLリンクプレビューを、ユーザー提示のDiscordスクリーンショットを参考に再構築（`lib/widgets/link_preview_card.dart`全面書き換え）。アクセントカラーの縦バー・ホスト名・色付きタイトル・説明文・サムネイル（動画は再生アイコン重畳）という構成にした。劇画UIにも同様の情報構成を適用しつつ、意匠は劇画UIらしいモノクロの`GekigaJointedTileList`ベースに独自実装（アクセントカラーは劇画UI選択中は使わない既存方針を踏襲）
- 劇画UIのリンクプレビュー画質調整: サムネイル画像を黒いモノクロボックスに直接埋め込む方式から、他の劇画要素と同じ「白マットに写真を余白付きで収める」構成に変更（動画の場合の黒レターボックスが背景と同化して見えていた問題への対応）。あわせてカード横幅を300→340に拡大、内側パディングを10→14に拡大し、プレーンなURLテキストメッセージの吹き出しがボックスからはみ出す別不具合（`_GekigaBubblePainter`の傾き量が幅の10%のまま頭打ち無しだったため、長いURLで傾きがpadding 28pxを超えていた）も発見し、傾き量を20px頭打ち＋padding28pxに修正
- ファイル添付のダウンロード操作を「ブロックをクリックで即ダウンロード」から「ポインターを乗せると右上にダウンロードアイコン表示（モバイルは常時表示）」に変更（`_FileAttachmentBlock`新設、`MouseRegion`+`defaultTargetPlatform`判定）。加えてMarkdown（.md/.markdown、1MB以下）ファイルにプレビュー機能を追加: `flutter_markdown_plus`パッケージを新規導入し、初期は高さ140pxで省略表示、シェブロンタップで全文表示に展開、常時右下にダウンロードボタンを表示する構成にした
- マークダウンプレビューで発生した3件の不具合を修正: (1) 表（GFMテーブル、`Table`+`FlexColumnWidth`）を含むファイルで折り畳み時に`ConstrainedBox(maxHeight)`による強制圧縮が制約違反を起こし赤いエラー画面になる不具合→`SizedBox`+`SingleChildScrollView`（スクロール無効）方式に変更。(2) 劇画UIのプレビュー文字色に`ThemeData.dark()`を単体生成して使っていたが、`MaterialApp`を経由しないと`textTheme.bodyMedium.fontSize`が未解決のままで`MarkdownStyleSheet.fromTheme`のassertに失敗する不具合→実際にアプリで使われている`Theme.of(context)`をベースに劇画UI用の文字色だけ`copyWith`で上書きする方式に変更。(3) 日本語本文が文字化けする不具合→`http`パッケージの`response.body`がFirebase Storageのcharset未指定レスポンスをlatin1フォールバックで誤復号していたのが原因と判明し、`utf8.decode(response.bodyBytes)`に変更
- マークダウンカードの横幅を340→480に拡大（狭さゆえに全表示時の折り返しが増え縦に長大化する不具合への対応）
- 劇画UIの画像メッセージに残る黒枠の不具合を複数回に渡り修正。当初`_GekigaBubblePainter`の傾き（skew）のみを0にする対応をしたが、`distortedParallelogramVertices`が傾きとは別に四隅をランダムにジッターさせる処理を常に行っており、`MonochromeBoxPainter`の内側リングのinset量とズレて黒い隙間が再発。最終的に画像コンテンツについては`_GekigaBubble`のモノクロボックス装飾（`CustomPaint`）自体を纏わせない方式に変更して解消。同じ理由で、二重枠になっていたマークダウンプレビューカード（`_FileAttachmentBlock`が自前で`GekigaJointedTileList`枠を持つ）についても外側の`_GekigaBubble`枠を纏わせないようにした（`isImageContent`を`skipFrame`にリネームし、`_FileAttachmentBlock.isPreviewableMarkdown`を静的メソッドとして共通化）
- 劇画UIの画像メッセージに白いマット枠を追加（`_GekigaPhotoMat`新設）。当初、送信者アイコン枠と同じ`monochromeBoxVertices`を流用したが、この関数は右辺・下辺を最大10〜15%内側に凹ませる設計（小さい正方形アイコン向けの調整）のため、矩形いっぱいの写真に使うと右・下方向に写真がマットからはみ出す不具合になった。矩形の4頂点に`cutCorners`（角カットのみ）を適用する方式に変更して解消（辺の中央部分は必ず矩形の端まで届くため、アスペクト比に関わらずはみ出さない）
- 画像添付の表示幅を220→280に拡大
- 画像タップ時のフルスクリーン表示（`_ImageViewerScreen`）に、Escキーおよび画像以外の領域タップで一覧へ戻る操作を追加（`Focus`+`onKeyEvent`、背景の`GestureDetector`＋画像側で空の`onTap`によるタップ吸収）
- マークダウンプレビューの外側の黒白パラレログラム枠（`_GekigaBubble`）が、カード自身の枠（`GekigaJointedTileList`）と二重表示になっていた不具合を修正。画像で使っていた`skipFrame`判定（旧`isImageContent`）にマークダウンプレビュー添付も含めるよう拡張し、`_FileAttachmentBlock.isPreviewableMarkdown`を`_MessageRow`からも呼べる形に整理した
- 劇画UIの画像メッセージに「カクカクした」白マット枠（`_GekigaPhotoMat`）を追加。当初、送信者アイコン枠と同じ`monochromeBoxVertices`を流用したところ、この関数は右辺・下辺を最大10〜15%内側に凹ませる設計（小さい正方形アイコン向け）のため矩形いっぱいの写真がマットからはみ出す不具合になり、矩形4頂点への`cutCorners`（角カットのみ）方式に変更して解消した
- 画像フルスクリーン表示のEscキー・外側タップでの復帰機能を追加（`_ImageViewerScreen`）
- 動画添付のアプリ内再生・プレビュー機能を新規実装。`video_player`パッケージを導入し、対応プラットフォーム（iOS/Android/Web/macOS）ではタップでフルスクリーン再生画面（自動再生、タップで一時停止/再開、`VideoProgressIndicator`によるシーク、Esc/動画外タップで閉じる）を開き、非対応のLinux/Windowsでは外部アプリで開く形にフォールバックする設計にした（技術仕様書5.8に決定事項を記録）。インラインプレビューは当初280px幅・16:9の暗いプレースホルダー＋再生アイコンのみだったが、ユーザー要望を受けて`_VideoThumbnail`を新設し、`video_thumbnail`等の専用パッケージを使わず`VideoPlayerController`を初期化した直後（再生せず先頭フレームで一時停止した状態）の`VideoPlayer`をそのまま縮小表示する方式に変更し、画像と同様に実際の最初のコマを表示するようにした
- 動画再生画面（`_VideoViewerScreen`）に再生/一時停止ボタン・現在位置/合計時間（mm:ss）・白色のプログレスバーを追加。`VideoPlayerController`自体が`ValueNotifier`であることを利用し`ValueListenableBuilder`で再生位置の変化に追従するコントロールバー（`_buildControlBar`）を新設した
- 動画メッセージにも上記`skipFrame`（旧`isImageContent`）の適用漏れがあり、画像・マークダウンと違って外側の黒いモノクロボックス枠が二重に被さっていた不具合を修正（`skipFrame`の条件に`contentType == 'video'`を追加）
- 動画フルスクリーン再生画面で、縦長（ポートレート）動画を再生すると画面の高さを大きく超えるレイアウトオーバーフロー（709px）が発生する不具合を修正。原因は`AspectRatio`を`Column`の直下（`mainAxisSize: MainAxisSize.min`）に置くと縦方向の制約が無制限になり、縦長動画で高さが際限なく計算されてしまうことだった。`Expanded`で明示的に高さを画面の残り分に制限する構成に変更して解消
- 設定・身だしなみタブで、同じナビゲーションチップを再タップするとカテゴリ一覧トップへ戻る機能（2026-08-10追加）をユーザー指示により廃止し、タブを切り替えても直前に見ていた項目を記憶して同じ場所に戻る、以前の仕様に戻した。調査の結果、この「記憶」機能自体は`HomeScreen`が`AnimatedIndexedStack`（`Offstage`で非表示タブも保持し続ける）でタブを切り替えているため`SettingsTab`/`ProfileTab`の`State`が元々破棄されず自然に保持されており、2026-08-10に追加した`_tabResetSignal`とその購読処理（`_handleResetSignal`等）を除去するだけで復元できた

### 詰まったところ
- 開発用`flutter run -d web-server`のホットリロード（`kill -SIGUSR1`）が、シグナル自体は送信できているにも関わらずコンパイル成果物（incremental dillファイル）のタイムスタンプが更新されず、実際には全く反映されていないという事態が発生した。ユーザーからのハードリロード（Ctrl+Shift+R）でも変化がないとの報告を受け、ビルド成果物のファイルタイムスタンプを直接確認することで「シグナルが握りつぶされている」ことに気づき、プロセスを完全に再起動して解決した。原因（なぜSIGUSR1が効かなくなったか）自体は特定できていない
- マークダウンプレビューの黒画面（赤いネストした矩形が表示されテキストが出ない不具合）は、`flutter_markdown_plus`パッケージのソース（`builder.dart`/`style_sheet.dart`）を直接読み、`Table`が`FlexColumnWidth`を使うため単純な高さ圧縮に対応していないことを突き止めるまで原因不明だった
- 劇画UIの画像黒枠は、`skewOverride`だけを0にする対応では四隅のジッターという別の要因を見落としており、ユーザーから2回連続で「まだ残っている」と指摘を受けてから、モノクロボックス装飾自体を纏わせないという根本対応に切り替えた。白マット追加時も同様に、`monochromeBoxVertices`の右下大幅凹み仕様を見落として画像はみ出しを引き起こし、ユーザー指摘後に`cutCorners`ベースへ変更した
- 動画添付のskipFrame適用漏れ（外側の黒枠が二重に残る不具合）、動画ビューアーの縦長動画オーバーフロー不具合とも、実装直後には気づけず、ユーザーのスクリーンショット付き指摘で判明した。特に後者は`Column`内での`AspectRatio`が主軸方向に無制限の高さを受け取るというFlex layoutの基本的な挙動の見落としが原因だった
- 動画プレビューを実フレーム表示に変更したことで、メッセージ一覧の動画の数だけ`VideoPlayerController`（実ネットワーク接続）が生成されるようになった。パフォーマンス上の懸念はあるが、実機での問題確認は未了のため対応は保留し、技術仕様書に留意点として記録するに留めた

### 次にやること
- 本日の一連の修正（URLプレビュー再構築、ファイル添付ダウンロードUI、マークダウンプレビュー、劇画UI画像の白マット・拡大、画像ビューアーのEsc/外側クリック対応、動画添付のアプリ内再生・実フレームプレビュー・再生コントロール、ナビゲーションチップの記憶機能復元）は`flutter analyze`/`flutter test`（既存の無関係な3件を除き）は通過済みだが、ブラウザでの実機確認はまだユーザー側で完了していない
- 開発用`flutter run`プロセスのSIGUSR1ホットリロードが機能しなくなった根本原因は未調査のまま（今回はプロセス再起動で回避）
- 動画プレビューの`VideoPlayerController`大量生成によるパフォーマンス懸念は、実機で問題が確認された場合にスクロール外動画のアンロード等を別タスクとして検討する
- Linux/Windowsでのアプリ内動画再生（`media_kit`等への移行）は技術仕様書5.8に「将来の検討事項」として記録済みだが未着手
- Sudachi（P2P、2GB超のファイル転送）は引き続き未実装

## 2026-08-10

### 実装内容
- QRコードログインのスキャン開始前に、「別の端末でQRコードが表示されている」という前提を説明する確認ダイアログを追加（`settings_tab.dart`の`_QrLoginRow._scan`から`_confirmStartScan`を新設して呼び出し）。1台の端末だけで試すと「読み取るべきものが無いのにカメラだけ起動する」ように見えて分かりにくいとの指摘を受けた対応。あわせてCLAUDE.mdの独自用語表の誤記（「和合」→正しくは「工房」）を修正
- ファイル・画像・動画の送信機能について、ユーザーと仕様を対話形式で詰めた上で実装。仕様確定内容: 拡張子ポリシー（ファイルは実行ファイル・スクリプト系のみ拒否するブロックリスト方式、画像・動画は形式チェックのみ）、容量上限（1件2GB、既存のSudachi誘導方針を踏襲）、保存期間（ファイル・動画は画像の標準保存期間と同じ2週間）、送信失敗時の挙動（インジケーター＋手動再送のみ、自動リトライ・レジュームは無し）、＋ボタンのUI（テキストボックス右端、押すと即座にポップアップでファイル/画像/動画/撮影を選択、長押しメニューと同じドラッグ選択にも対応）、撮影画面（アプリ内カメラで横スワイプにより写真/録画モード切替、シャッター色分け＝白は写真・赤は録画、タップで録画開始/終了）。決定内容は`docs/DaiDai-技術仕様書.md`5章に反映
- 実装: `pubspec.yaml`に`file_picker`・`camera`追加。`lib/utils/file_extension_policy.dart`（拡張子ブロックリスト・容量上限）、`lib/utils/attachment_upload.dart`（Storageアップロード共通処理、画像はWebP圧縮）を新規作成。`Message`モデルに`fileMetadata`（`MessageFileMetadata`）を追加。`DirectMessageRepository`/`GroupRepository`に`sendAttachmentMessage`を追加。`storage.rules`に`dmFiles`/`groupFiles`の権限ルールを追加。メッセージ長押しメニューのドラッグ選択ジオメトリ（`_DragMenuGeometry`）を`lib/utils/drag_menu_geometry.dart`の公開`DragMenuGeometry`として切り出し、`chat_screen.dart`側もこれを使うようリネーム。新規`lib/features/chat/attachment_popup_button.dart`（＋ボタン、タップ即開閉＋ドラッグ選択＋個別タップ選択の3方式に対応する独自のPointer処理）と`lib/features/chat/camera_capture_screen.dart`（`camera`パッケージによる自前プレビュー画面）を実装し、`chat_screen.dart`の入力欄・`chat_panes.dart`のDM/広場配線・メッセージバブル（画像サムネイル/ファイル・動画のアイコン表示）に接続

- ホーム画面（語らい/身だしなみ/設定タブ）・設定/身だしなみのドリルダウンに、スライド＋ポップ演出（`lib/theme/motion.dart`の`buildPopSlideTransition`、フェード＋わずかな下方向オフセット＋拡大）を統一適用。画面遷移全体は`PopSlidePageTransitionsBuilder`（`app_theme.dart`）で、ホームタブ切り替えは新規`AnimatedIndexedStack`（`lib/widgets/animated_indexed_stack.dart`、状態を保持したまま切り替え対象だけポップ演出させる`IndexedStack`代替）で対応。設定・身だしなみでドリルダウン中に同じメニューチップを再タップすると、カテゴリ一覧のトップへジャンプする機能を追加（`HomeScreen`の`_tabResetSignal`を`SettingsTab`/`ProfileTab`が購読）
- 添付送信機能（前回エントリ参照）の実機確認で判明した4件の不具合・要望を修正: (1) モバイルで＋ボタンを押すとソフトウェアキーボードが開きポップアップが実質見えなくなる不具合を、`AttachmentPopupButton`を`TextField`の`suffixIcon`からRow内の兄弟要素へ移動し、`_onPointerDown`冒頭で`FocusScope.unfocus()`する対応で修正。(2) 画像・ファイル送信/撮影の失敗は、`storage.rules`の`dmFiles`/`groupFiles`ルールを本番Firebaseへデプロイし忘れていたことが原因と判明し、ユーザー許可の上`firebase deploy --only storage --project daidai-rhing`を実行して解消。(3) カメラ画面（`camera_capture_screen.dart`）に前面/背面カメラの切り替えボタンと、写真/動画モードを一目で判別できるアイコンインジケーター（タップでも切替可）を追加。(4) 添付送信失敗時の通知を、消えないように見えていたSnackBarから画面上部のMaterialBanner＋3秒タイマーでの自動消去に変更
- モバイルの寄合タブバー（`RoomTabBar`）で、指でなぞって寄合を切り替える操作中に一時停止すると以降切り替えられなくなる不具合を修正。原因は、指が乗ったチップへその都度即座に`pushReplacement`（`RoomTabBar`自身を含む画面全体の作り直し）していたため、遷移アニメーション完了と同時にドラッグ検出自体が消滅していたこと。ドラッグ中はハイライトのみ追従させ、指を離した時点で一度だけ実際に切り替える方式に変更（ユーザー確認の上、ホーム画面のメニューチップと全く同じ「なぞった瞬間に即座切替」は今回のスコープ外と判断）
- 劇画UIの入力欄・＋ボタンをモノクロボックス化。入力欄は自分のメッセージ吹き出し（`_GekigaBubble`）と同じ「歪な平行四辺形＋黒白反転のモノクロボックス」意匠を新規`_GekigaComposerField`で適用（seedは固定値）、＋ボタンは既存の`GekigaIconBadge`（寄合追加ボタン等と同意匠）に差し替え
- ホーム画面が真っ赤になり何も表示・操作できなくなる重大な不具合を修正。原因は上記`AnimatedIndexedStack`（`lib/widgets/animated_indexed_stack.dart`）の`_buildChild`で、表示すべき子・直前まで表示していた子のどちらにも`Offstage(offstage: false, ...)`を明示しておらず、`Offstage`のデフォルト値`offstage: true`が全分岐に適用されてタブの中身が常に非表示・非ヒットテスト対象になっていたため（劇画UIの`scaffoldBackgroundColor`である赤だけが背後に見えていた）。両分岐に`offstage: false`を明示して修正
- ユーザー要望を受け、劇画UIの入力欄・＋ボタンをさらに調整: ＋ボタンを入力欄の右から左端（テキスト欄より前）へ移動。`distortedParallelogramVertices`（`gekiga_shapes.dart`）に`skewOverride`引数を追加し、入力欄のような「横幅に対して縦が極端に短い箱」では、既定の「横幅の10%」ではなく縦幅基準の値（最終的に`size.height * 0.12`）を渡すよう`_GekigaComposerFieldPainter`を変更。文字がはみ出さない程度に傾きを抑えつつ、＋ボタン側の角の食い込みを減らしてテキスト欄がより左まで伸びるようにした

### 詰まったところ
- ＋ボタンのキーボード問題は、Flutter SDKのソース（`text_field.dart`/`text_selection.dart`）を読み、`Listener`ベースの独自実装がジェスチャーアリーナに参加しないため`TextField`自身のタップ検出と独立に競合していることを確認するまで原因を特定できなかった
- 添付送信失敗の原因究明は直接「本番デプロイ済みルールの中身を取得する」手段が無いため、2026-08-06のfirestore.rules未デプロイ事例とのパターン一致（日記.mdの記録上、同日実装したQRログインはデプロイに言及があるのに添付機能側の`storage.rules`変更には言及が無い）を状況証拠として推測し、デプロイ後に解消したことで確認する形になった
- 寄合タブバーのドラッグ不具合は、Exploreサブエージェントで`RoomTabBar`と`_NavChip`（メニューチップ）双方の実装を比較調査させ、メニューチップが機能する理由（タブの中身を`AnimatedIndexedStack`で常時同時マウントしたまま表示だけ切替、ドラッグ検出ウィジェット自体は不変）と寄合タブバーがそれをそのまま踏襲できない理由（寄合切替は意図的に`ChatScreen`自体を作り直す設計のため、ドラッグ検出ウィジェットごと破棄される）を明確化してから、ユーザーに「メニューチップと全く同じ挙動にするには`ChatScreen`を作り直さない設計へ変更が必要で、今回のスコープ外」と説明し、「離した時に確定」方式で合意を得た
- 画面が赤いまま何も表示されない不具合は、当初ユーザーから「端末再起動・再ビルドしても直らない」「アニメーションや拡大率が原因では」と報告を受け、ブラウザキャッシュ等ではなく実コードのバグだと判断。Explore サブエージェント2体を並行投入し、(1) アプリ起動時のエラーハンドリング・劇画UIの背景色解決経路、(2) 今日追加したアニメーション/独自CustomPaintコードのクラッシュ要因、を調べさせたところ、後者から`AnimatedIndexedStack`の`Offstage`デフォルト値の見落としを特定。ユーザーの「拡大率が原因では」という仮説は的外れだったが、ズーム関連コードが実際に存在しないことも調査で確認した上で否定できた

### 次にやること
- 今回追加したQRログイン確認ダイアログ・ファイル/画像/動画送信機能とも、実機・ブラウザでの動作確認はまだユーザー側で未実施
- アプリ内カメラ撮影画面（横スワイプでのモード切替・シャッター色分け・録画の開始/終了、前面/背面切替、モードアイコン）は特に実機でのジェスチャー・カメラ権限まわりの確認が必要
- 動画のインライン再生は`video_player`未導入のため対応せず外部アプリで開く形にとどめている。将来インライン再生が必要になれば別タスクとして検討
- Sudachi（P2P、2GB超のファイル転送）は今回のスコープ外のまま未実装
- 赤画面不具合の修正・劇画UI入力欄の調整は`flutter analyze`（既存の無関係な情報レベルの指摘のみ）は通過済みだが、ユーザー側での再確認はまだ完了していない（直前のスクリーンショットでは表示・入力自体は復旧を確認済み）
- `android/build/`が誤ってgit管理下に入りかけていたのに気づき、`.gitignore`に追加（今回のコミットには含めていない）

## 2026-08-09

### 実装内容
- スクリーンショット/削除用のメッセージ選択の当たり判定を、チェックボックス・吹き出しの上だけでなく行の横幅全体で反応するよう修正（`_MessageRow`のselecting-mode `GestureDetector`に`HitTestBehavior.opaque`を追加。`content`が`Align`でラップされ余白部分が空白扱いになっていたのが原因）
- メッセージの長押しメニューを、押したままメニュー上をスライドして指を離した項目が実行される方式に変更（`_MessageBubbleTapArea`を`StatefulWidget`化し、`OverlayEntry`+`ValueNotifier<int>`+`onLongPressMoveUpdate`による自前のドラッグ選択メニューを実装。右クリック時の`showMenu`はそのまま維持）
- TOTP方式の2段階認証を実装。Firebase Authentication標準のMulti-Factor Authentication APIを使用（自前の暗号実装はせず、CLAUDE.mdの「Don't roll your own crypto」方針に準拠）。`AuthRepository`にTOTP登録/確定/解除/再認証、`two_factor_setup_dialog.dart`（QRコード+シークレットキー+コード入力）、`settings_tab.dart`のセキュリティ欄に状態表示・登録・解除導線、`sign_in_screen.dart`にサインイン時のコード入力チャレンジを実装
- Firebase ConsoleではTOTP MFAがSMSと違い有効化トグルが無く、Identity Platformへのアップグレード（現状MAU数的に実質無料と確認の上ユーザーが実施）＋Admin SDK/REST API経由のプロジェクト設定変更が必要と判明。`functions/src/index.ts`に一度きりの管理用関数`enableTotpMfa`を追加・デプロイ・実行し、有効化を確認後にソースごと削除・再デプロイ
- 上記デプロイのため、開発環境にfirebase CLIを導入（`npx firebase-tools`）。キャッシュされたログインが個人アカウント（arataurusu@gmail.com）だったため、CLAUDE.mdの方針に従い`rhing.official@gmail.com`へ入れ直した（デバイスコードフローでユーザーに認証コードを取得してもらった）
- TOTP登録時に`requires-recent-login`エラーが発生する問題を修正。Web版`firebase_auth`プラグインでは一部のmultiFactor関連エラーが`FirebaseAuthException`型に正しくマッピングされず素の`Exception`として飛んでくることが判明したため、型チェックに加え`toString()`によるメッセージ判定も併用するよう`two_factor_setup_dialog.dart`・`settings_tab.dart`のエラーハンドリングを強化。`AuthRepository.reauthenticate()`（Google/Appleでの再認証）を追加し、エラー時に「再ログインする」ボタンから即座にリトライできるようにした
- 認証方式の今後の方針についてユーザーと相談し、Google/Apple OAuthのみを維持しRhing ID＋パスワードは新設しない、パスキーも（DaiDaiが認証情報を持たずGoogle/Appleに委任している以上）現時点では不要という結論に至り、CLAUDE.mdの「ログイン手段の方針」節を新設・更新（データモデルの`twoFactorEnabled`削除、開発フェーズ表のパスキーを対象外に変更）
- QRコードによるログイン機能を実装。読み取り方式は縁結び機能で既に使われていた`mobile_scanner`によるアプリ内スキャナーを再利用する方針とした（OS標準カメラ経由のUniversal Links方式は、新規のドメイン設定が必要な上Windows/Linux非対応のため不採用）。縁結びの`_QrScanScreen`を`lib/widgets/qr_scan_screen.dart`として共有ウィジェット化し、未ログイン端末側のQR表示・承認待ち・自動サインインを行う`lib/features/auth/qr_login_dialog.dart`を新規実装。`AuthRepository`にセッション作成・監視・承認・claimのメソッドを追加、`functions/src/index.ts`に`approveQrLoginSession`/`claimQrLoginSession`/`cleanupQrLoginSessions`を追加してデプロイ、`firestore.rules`に`qrLoginSessions`用ルールを追加してデプロイ。カスタムトークンはFirestoreに一切書き込まずCloud Functionsのレスポンスとしてのみ返す設計にし、セッションIDが漏れても認証情報自体は盗めないようにした

### 詰まったところ
- TOTP MFAがFirebase ConsoleのUI操作だけでは有効化できない（SMSのみUIから設定可能）ことに気づくまで調査に時間がかかり、実際にはAdmin SDK/REST API経由でのプロジェクト設定変更が必要だった
- `requires-recent-login`エラーが期待した型（`FirebaseAuthException`）で捕捉できず、当初の実装では生のエラーメッセージがそのままダイアログに表示されてしまった。Web版プラグインの型マッピングの不備が原因と判明し、メッセージ文字列ベースの判定を追加して解決
- この開発環境にfirebase CLIが入っておらず、かつキャッシュされたログインアカウントがRhing公式ではなく個人アカウントだったため、デプロイ作業に入る前にアカウントの入れ直しが必要だった

### 次にやること
- QRコードログイン機能の実機確認（Web側でQR表示→別端末/別ブラウザセッションでスキャンして承認、という一連の流れ）がまだ行われていない
- `flutter run -d web-server`のプロセスが会話の途中で終了しており、動作確認には再起動が必要
- CLAUDE.mdに追記した「将来の検討事項」（Google/Apple非依存が必要になった場合はRhing ID＋パスキーを軸に検討する）は未着手・未決定のまま

## 2026-08-06

### 実装内容
- リアクションを付けても一瞬で消えてしまう不具合を調査。原因は、前日にリアクションを「1人1絵文字の文字列」から「1人が複数絵文字を持てるリスト」に変更した際の`firestore.rules`側のバリデーション変更（ローカルファイル・git上には反映済み）を、本番Firebaseへ**デプロイし忘れていた**ことと判明。本番ルールが旧形式（単一絵文字文字列）のままだったため、クライアントがリスト形式で書き込むと毎回サーバー側で拒否され、Firestore SDKのローカル楽観的反映（書き込み即時にUIへ反映される仕組み）→サーバー拒否によるロールバック、という流れで「一瞬付いてすぐ消える」症状になっていた。ユーザーの許可を得て`firebase deploy --only firestore:rules --project daidai-rhing`（`rhing.official@gmail.com`アカウント）を実行して解消
- ユーザーの明示的な指示により、`flutter run -d web-server --web-port=8765`を再起動（動作確認環境の用意）
- 劇画UIのメッセージ吹き出しの形状を変更。ユーザー提示の参考画像（黒地白字、白い枠線、左辺が段差状にカクカクしたジグザグ、右辺中央にわずかな凹みを挟んだ矢羽根状の尖り）を目視でトレースし、正規化頂点リストとして起こした。`lib/theme/gekiga/gekiga_shapes.dart`に新規`speechBubbleVertices`（固定形状、他のモノクロボックス系と異なりseedによる頂点自体の乱数化はしない）・`mirrorHorizontal`（頂点のx座標のみを反転する汎用ヘルパー、テキスト等の中身は反転させない設計）・`seededThicknessScale`（枠線の太さだけをseedで0.7〜1.0倍にランダム化、`MonochromeBoxPainter`と同じ式を共通ヘルパー化）を追加。`_GekigaBubble`/`_GekigaBubblePainter`（`chat_screen.dart`）を旧来の`cutCorners`+`handDrawnPolygonPath`（手描き風ジグザグ）からこの新形状ベースの描画に置き換え、右寄せ表示（自分・sideBySideレイアウト）の時だけ形状を左右反転する`alignRight`引数を追加。内側paddingを新形状の凹みに合わせて拡大（horizontal 16→24、vertical 12→14）、線の結合を`round`→`miter`に変更し右辺中央の凹みの鋭さを保った
- 縦表示の寄合タブバー（`RoomTabBar`）に劇画UIの「モノクロボックス」意匠を適用。`lib/widgets/gekiga/gekiga_panel_box.dart`の`GekigaJointedTileList`に`axis`パラメータを追加（既定`Axis.vertical`、既存呼び出し元は無影響）し、`RoomTabBar`の劇画スタイル時だけ`ListView.separated`から`SingleChildScrollView`+`GekigaJointedTileList(axis: Axis.horizontal, ...)`に切り替え。選択中セルは白地黒文字・未選択は黒地白文字に反転。横スクロール・スライド切替・追加ボタンは維持し、シンプルスタイルは無変更
- 縦表示のチャット画面で、メッセージ吹き出しの上を右スワイプしても会話一覧に戻れない不具合を修正。原因は、返信/編集用の横ドラッグ検出（`_MessageInteractions`）が方向を問わず横ドラッグをジェスチャーアリーナ上で先に受理してしまい、外側の「戻る」ジェスチャー（`SwipeBackDetector`、吹き出しの無い余白では機能する）に一切伝播しないこと（受理は不可逆で事後的に譲れない）と判明。`_MessageInteractions`自身に右スワイプ検知（`kSwipeGestureVelocityThreshold`を`swipe_gestures.dart`から再利用）を直接組み込み、`onSwipeBack`を`ChatScreen`→`DmChatPane`/`GroupChatPane`→`app_router.dart`まで新規のnullableなコールバックとして配線。右寄せ表示（自分のメッセージ）は指定通り除外し、広い画面の分割表示（`talks_tab.dart`の埋め込み）には配線しない（会話一覧へ戻る概念が無いため、既存の`showRoomTabBar`と同じ判断基準を踏襲）
- トーク一覧の一対/広場カテゴリタブが、スワイプでは最初の1回しか切り替わらない不具合を修正。原因は、現在のカテゴリに応じて`SwipeBackDetector`の`onPrevious`/`onNext`のどちらか一方を常にnullにしていたため、同じ方向へ連続スワイプすると2回目以降無反応になる設計だったこと。一対/広場は2値しかないため、常に両方向を有効にし方向ごとに固定の相手側へ切り替える形に変更

### 詰まったところ
- リアクション即消え不具合の原因特定に、コードロジック（`_toggleMyReaction`・リポジトリの`setReaction`・UI側のヒットテスト等）を一通り再確認する時間がかかった。最終的には「firestore.rulesはローカル・gitには存在するが、実際にFirebaseへデプロイされたかどうかは別問題」という観点に気づいて特定できた。firebase-tools CLIには「現在デプロイ済みのルール内容を取得する」直接的なコマンドが無く、デプロイ済みかどうかの確認手段が限られていた
- `firebase deploy`はClaude Codeの自動モード分類器によって無許可実行がブロックされたため、ユーザーに状況を説明し明示的な許可を得てから実行した
- 右スワイプで戻れない不具合は、事前にExploreサブエージェント3体を並行調査に投入し「右スワイプで戻る機能自体がどこにあるか」「`_MessageInteractions`のジェスチャー処理」「カテゴリタブのスワイプ実装」を個別に特定させたことで、実装前にジェスチャーアリーナの根本原因（受理の不可逆性）を正確に把握できた。実装自体で手戻りは発生しなかった
- 開発用`flutter run -d web-server`のプロセスが会話の途中で消えており（環境再起動と思われる、PIDも変わっていた）、ホットリスタートのつもりが`kill`エラーになった。ログファイルも消えていたため、新規に立ち上げ直して対応した

### 次にやること
- 吹き出し形状の頂点座標は参考画像からの目視トレースによる暫定値のため、実機でユーザーが確認し、尖りの強さ・段差の位置等にズレがあれば座標を微調整する必要がある
- 自分/相手・sideBySide/allLeftの各レイアウトパターン、短文/長文/絵文字のみのメッセージでの吹き出し形状の網羅的な見た目確認はまだ実施していない（ホットリスタートでの反映のみ）
- 寄合タブバーのモノクロボックス化・右スワイプで戻る修正・カテゴリタブのスワイプ修正の3件は、`flutter analyze`/`flutter test`（既存の無関係な3件を除き）は通過済みだが、実機での目視・操作確認はまだユーザー側で行われていない

## 2026-08-05

### 実装内容
- 劇画UIを有効にした際の配色をアクセントカラー完全無効化・背景以外モノクロに変更（`GekigaTheme.build()`を`ColorScheme.dark()`から`ColorScheme.fromSeed(seedColor: Colors.grey, brightness: Brightness.dark)`ベースに変更し、`onSurfaceVariant`/`outline`/`outlineVariant`等の個別トークンも明示上書き。`main.dart`の呼び出しも引数無しに変更）
- メッセージ入力欄のヒント文言を英語ロケールでも英訳し、和英どちらも文末に「...」を追加（`strings.dart`に`chatInputHint`新設）
- 既読バッジ・リアクションチップを劇画UIでもモノクロ化（ギザギザ枠は使わず色のみ反転）
- 会話ごとのプロフィールカードセクションの「＋」ボタンをタイトルの左側に移動（`profile_tab.dart`）
- 身だしなみ（呼び名・一言・SNSリンク・保存/削除ボタン・イメージカラー入力欄・素材追加ボタン）に劇画ギザギザボックスUIを適用。プロフィールカードのグリッド・Hero遷移・素材サムネイルは対象外（ユーザーとの確認の上で安全な範囲に限定）
- ライト/ダークモードの外観設定を劇画UI選択中は無効化してグレーアウト（既存のアクセントカラー欄と同じ`Opacity`+`IgnorePointer`パターンを流用）
- 灰色になっていた仕切り線・説明文の色を劇画UIでも白に戻す
- メッセージ画面の日付セパレーターを劇画UIで黒地白字に戻す
- 広場名表示・ハンバーガーメニュー・音声通話/ビデオ通話ボタンに劇画ギザギザボックスを適用
- メッセージ画面上部に表示していた寄合名表示を削除
- 承認待ちの一対・広場をタップした際、Firestoreのルール上開けない本チャット画面の代わりに専用の`_PendingApprovalScreen`（`talks_tab.dart`）を表示するよう変更。一覧側の「相手の承認を待っています」テキストは削除。寄合一覧上の一対/広場名に付けていたアイコンも削除
- 既読バッジとリアクションチップの重なりを修正。試行錯誤の末、最終的に既読バッジと同じ`Positioned(bottom: -14, left/right: -10)`内で`Row`を使い横並びにする設計に収束（既読バッジの座標は吹き出しの高さ＝メッセージの長さに依存しないため、リアクション側の座標を独自に持たせず既読バッジの位置に相乗りさせることで、メッセージが短くても吹き出し外に突き抜けて重ならないことを構造的に保証）。ユーザー指定により、リアクションは既読バッジよりさらに外側（メッセージから見て外側）に配置する順序に調整
- リアクション選択ポップアップ（絵文字6個）が画面右端付近で開くと発生していたオーバーフローを、内容を`Row`から`Wrap`に変更して修正
- リアクションチップの人数表示を削除（絵文字のみ）。タップしても即座に自分のリアクションを取り消さず、新設した`_showReactionListPopup`（誰がどの絵文字を付けたかの一覧、既読ポップアップと同じ表示パターン）を開くよう変更。自分の行にのみゴミ箱ボタンを表示し、押すと取り消してポップアップを閉じる
- メッセージ画面の送信者アイコン背後に表示していた劇画UIの正方形バッジ（`GekigaBadgeFrame`）を削除し、丸いアイコンのみの表示に変更。使われなくなった`GekigaBadgeFrame`クラス自体も削除（`GekigaBadgePainter`/`GekigaBadgeShape`はホーム画面ナビチップで使用中のため維持）
- 「イメージカラー」機能を全レイヤーから削除（`AppUser.imageColor`、`UserRepository.setImageColor`、`profile_tab.dart`の`_ImageColorSection`、テストのフェイク実装、l10n文言、`GekigaTheme`/`GekigaBadgePainter`のコメント記述）。チャットアバター背景四角（`GekigaBadgeFrame`）削除により唯一の用途が無くなったため
- 身だしなみ＞工房＞会話ごとのプロフィールカードの「＋」ポップアップを改善: 一対はRhing IDではなく呼び名を表示、各行に左端アイコンを表示、チェックボックスによる複数選択に変更。複数選択時は選んだカードを全選択会話へ一括適用（`ProfileCardPicker`のみを載せた軽量ダイアログ＋`Future.wait`）
- 上記ポップアップをさらに調整: 「＋」ボタンをセクション見出しの右横（`Expanded`で右端に押し出してしまい後で修正）に配置、ポップアップ幅を`double.maxFinite`から固定360に絞って中央寄せ、名前検索ボックスを追加（`workshopConversationCardSearchHint`新設）、検索欄に入力時のみ表示される×クリアボタンを追加
- メッセージ画面の送信者アイコン（`GekigaPhotoFrame`）の角の形を複数回微調整（左下・右下・右上の角度バランス、左上の突き出し量）した上で、その枠形状（不規則四角形＋黒太枠・白細枠の二重枠）を「モノクロボックス」と命名し、`monochromeBoxVertices`/`MonochromeBoxPainter`として`gekiga_shapes.dart`/新規`monochrome_box.dart`に共通化。バッジ（`GekigaBadgeShape`）・アイコンボタン（`GekigaIconBadge`）・テキスト入力欄（`GekigaTextField`）・一対/広場/寄合一覧（`GekigaJointedTileList`/`GekigaJointedPair`）の手描き風ジグザグ枠を全て置き換え
- 一対/広場/寄合一覧側は当初「隣接する箱の共有辺を直角のまま揃え、cross軸サイズを強制的に一致させて接合する」設計で実装したが、実機確認で角の座学的な歪み・テキストのはみ出し・箱の意図しない引き伸ばしが発覚したため方針転換。接合機能自体を廃止し、各箱を独立サイズのまま8pxの間隔を空けて並べる設計に戻した。角も直角の矩形に統一し、代わりに`MonochromeBoxPainter`に`seed`を渡すことで枠の太さ（既定の0.7〜1.0倍、薄くなる方向のみ）を箱ごとに僅かに変える方式へ変更
- `GekigaTileContent`の横方向paddingを16→20に拡大（縦方向はシンプルスタイルの`ListTile`高さとの整合のため維持）
- メニューチップ・＋ボタン・ハンバーガーメニュー・通話ボタン（いずれも既に`GekigaBadgeShape`/`GekigaIconBadge`を経由）にも上記の直角モノクロボックス＋枠太さランダム化を適用（`GekigaBadgeShape`の共通化により自動的に反映）
- 「自分は右・相手は左」レイアウト選択時に一対でアイコンが表示されない件は、仕様として意図的に非表示にする決定済みの挙動であることをユーザーに確認し、対応不要と判断
- 寄合一覧・トーク一覧の名前表示に折返しが発生していた不具合を修正（`maxLines: 1`/`overflow: TextOverflow.ellipsis`を`talks_tab.dart`・`room_list_pane.dart`・`room_tab_bar.dart`の該当`Text`に追加）
- 寄合名編集ダイアログでEnterキー押下時に確定できるよう`onSubmitted`を追加（`chat_panes.dart`の`_showRenameRoomDialog`、`room_list_pane.dart`の`promptForRoomName`）
- 劇画UIにアクセントカラー相当の背景色設定を追加。シンプルUIのアクセントカラー（全体の`ColorScheme`のseed）とは異なり背景色のみを操作対象にし、標準値は現在の赤（`GekigaColors.background`）のまま後から編集可能、プリセットにも現在の赤を追加。新規`gekiga_background_color_provider.dart`（`accent_color_provider.dart`と同型のNotifier+SharedPreferences永続化+Firestore同期）、`GekigaTheme.build()`を`build(Color backgroundColor)`に変更、`AppUserPreferences.gekigaBackgroundColorArgb`追加、`settings_tab.dart`のデザイン設定を劇画/シンプルで対象プロバイダ・プリセットを切り替える形に変更
- 既読バッジ・リアクションチップの位置を再変更し、複数リアクション対応を実装。データモデルを`Message.reactions`が1ユーザー1絵文字（`Map<String,String>`）から、1ユーザーが複数の異なる絵文字を同時に持てる（`Map<String,List<String>>`）方式に変更。`DirectMessageRepository`/`GroupRepository`の`setReaction`系メソッド（`emoji`→`emojis`引数）、`firestore.rules`のリアクションバリデーション（`is list`/`size() > 0`/`hasOnly([...])`）も追随。既存データ（`reactions.$userId`が単一の絵文字文字列）は`Message.fromJson`側で後方互換読み込みし、Cloud Functionsでの移行は行わなかった
- 上記実装後の実機確認でユーザーから「既読バッジがボックスから離れすぎ」「複数リアクションを付けられない」「リアクション横の＋ボタンを押してもポップアップが出ない」「リアクションを付けると下のメッセージと重なる」という複数の不具合報告を受け、コードを再調査。原因はすべて、既読バッジ・リアクション行を`Stack`+`Positioned`で吹き出しの外へ「はみ出させる」実装にあったと判明: (1)`Positioned`のはみ出し分は`Stack`自身（ひいては`ListView`の行）のレイアウト計算上の高さに含まれないため下のメッセージと重なる、(2)はみ出し範囲は吹き出し本体の`GestureDetector`（`_MessageBubbleTapArea`）の当たり判定の外側になるため＋ボタン・リアクションチップのタップが物理的に一切届かない（`RenderBox.hitTest`が自身の`size.contains(position)`を満たさない座標では`hitTestChildren`自体を呼ばない仕様のため）。`Row`（既読バッジを吹き出しと横並びに、`crossAxisAlignment.end`で下端を揃える）＋`Column`（リアクション行を吹き出しの真下に実要素として配置）へ全面的に構成し直し、4件の不具合を一括で解消。リアクション一覧ポップアップの削除アイコンも指示通り「×」（`Icons.close`）に変更（縦並び・自分の行にのみ削除ボタンという構造自体は元から実装済みだった）

### 詰まったところ
- 既読バッジとリアクションの重なり修正で複数回の手戻りが発生した。(1) `SizedBox`挿入による重なり回避が`Edit`の`replace_all`のインデント不一致で片方の分岐にしか適用されておらず、ユーザーに「改善されていない」と再指摘された。(2) 既読バッジとリアクションを1つの`Column`にまとめてクラスタ全体の`Positioned.bottom`をより負の値にする形を試みたところ、既読バッジ自体が下（次のメッセージ側）に押し出されてしまい「既読を下に追いやってどうする」と指摘された。(3) リアクション単独の`Positioned`で「吹き出し下端から固定px上」に配置したところ、吹き出しの高さ（＝メッセージの長さ）に依存しない固定値だったため、短いメッセージ（1〜2文字）だと吹き出しの外（上のアバター・時刻表示側）に突き抜けて重なった。ユーザー自身から「座標を移動させても、メッセージの長さが変わると重なってしまうのでは」という的確な指摘を受け、既読バッジの座標に相乗りする`Row`方式へ方針転換して構造的に解決した
- リアクションのRow内での並び順を、既読バッジを常に外側に置く設計で実装したが、ユーザーの意図は逆（リアクションを既読より外側）だったため、並び順を反転する追加修正が必要だった
- 会話ごとのプロフィールカード追加ポップアップで「＋ボタンを見出しの右横に」という指示を、見出し`Text`を`Expanded`で囲む実装にしてしまい、結果的にボタンが行の右端まで押し出される誤りとなった。ユーザーから「文字の右横って言ったよね？何で右端まで移動させるの？」と指摘を受け、`Expanded`を外して単純に隣接させる形に修正
- `dart format lib/ test/`を対象ファイルを絞らず広く実行した際、無関係な約60ファイルがDart SDKのフォーマッタ差分で再フォーマットされてしまう事態が発生。`git status --short`で気付き、`git diff`で純粋に体裁差分のみと確認した上で`git checkout --`で対象外ファイルを個別に復元。以降はフォーマット/analyzeの対象を必ずその場で触ったファイルのみに絞る運用に変更
- モノクロボックスの「接合」実装（cross軸サイズ強制一致＋隣接しない角のみ座標をわずかにずらす）は、承認済みプランに基づき一度実装・テストまで完了させたが、実機のスクリーンショットで角の歪み・テキストのはみ出し・意図しない引き伸ばしが判明し、ユーザーから「接合機能は廃止」と方針を覆された。プラン承認後の実装がそのまま通用しなかった一例で、見た目に関わる細かな造形は実機確認前提で後から反転する可能性がある前提を再認識した
- メッセージ画面アイコンの左上角のy方向突き出しを半分に縮める調整では、ホットリロードは正常に成功していたにもかかわらず「変化なし」と指摘された。変化量（3.6px程度）が実際には見た目上ほとんど分からない小さすぎる調整だったためで、突き出しを完全に0にするまで強めて初めて視認できる変化になった
- 既読バッジ・リアクションの一連の不具合（ボックスから離れすぎ・＋ボタン無反応・複数リアクション付与不可・下のメッセージとの重なり）は、個別の不具合として1つずつ対処するのではなく、`_MessageBubbleTapArea`・`_reactionBar`・`Stack`周りのコードをまず読み込んで共通原因（`Positioned`オーバーフローがレイアウトサイズにも当たり判定にも含まれないというFlutterの仕様）を特定してから、`Row`/`Column`への構成変更一発で一括解消する方針にした。個別のオフセット値（`-48`/`-27`等）をその都度調整するその場しのぎではなく、構造自体を見直したことで再発しない形にできた

### 次にやること
- 今回の一連の劇画UI調整・リアクション配置変更・アバター背景四角の削除は、ホットリロードでの簡易確認のみで実機での網羅的な確認は未実施
- リアクション一覧ポップアップ（誰がどの絵文字を付けたか＋自分の行のみ削除ボタン）は新規実装のため、実際の操作感（ポップアップの開閉・削除後の見た目）のユーザー確認待ち
- 会話ごとのプロフィールカード追加ポップアップ（呼び名表示・複数選択・検索・×クリアボタン）も同様にホットリロードでの簡易確認のみで、ユーザーによる実際の操作確認待ち
- モノクロボックスの再設計（接合廃止・8px間隔・枠太さのランダム変化・`GekigaTileContent`横paddingの16→20拡大）は今回もホットリロードでの反映確認のみ。一対/広場/寄合一覧、設定・身だしなみのリスト、切り替えタブ、入力欄、メニューチップ・＋ボタン・ハンバーガーメニュー・通話ボタンでの実際の見た目をユーザーが確認する必要がある。間隔・枠太さの変化量は暫定値のため、確認結果次第で微調整の可能性がある
- 既読バッジ・リアクションのRow/Column再構成、劇画UIの背景色設定はホットリスタート実行時にブラウザタブが未接続（"No clients available"）だったため反映確認は未実施。ユーザーが`http://localhost:8765/`を開き直して実機確認する必要がある
- firestore.rulesの新しいリアクション用リスト検証（`is list`/`size()`/`hasOnly()`）はローカルにemulatorテスト環境が無いため、デプロイ前にFirebaseコンソールまたはemulatorでの動作確認が必要

## 2026-08-04

### 実装内容
- 劇画UIの一対一覧・広場一覧・寄合一覧・カテゴリタブ（一対/広場切替）を、単に隙間を詰めるだけでなく隣接する箱の接する辺を同じ折れ線として共有する「接合」実装に変更（新規`GekigaJointedTileList`/`GekigaJointedPair`、`lib/widgets/gekiga/gekiga_panel_box.dart`）。幅が異なる箱同士でも、重なる範囲だけ共有し、はみ出す部分は伸ばさず独自にジグザグを続ける設計
- Web版でのみ発生していた重大な描画不能バグを発見・修正: `lib/theme/gekiga/gekiga_shapes.dart`の`handDrawnPolygonPath`内`random.nextInt(1 << 32)`が、dart2js上ではJSの32bitビット演算の仕様により不正な値を返し例外になっていた（ネイティブ実行の`flutter test`では再現せず気付きにくい）。`dart compile js`+`node`での直接実行で原因を特定し、`0x7fffffff`に変更して解消。このバグにより、ナビチップのバッジ・メッセージ吹き出し等ジグザグ枠を使う全箇所がWeb版で描画されない状態になっていた
- ホーム画面のハーフトーン柄・ダイアゴナルアクセント背景装飾（`halftone_pattern.dart`/`diagonal_accent.dart`）を削除。リスト項目の隙間から模様が中途半端に透けて見える問題への対応
- シンプルUIとの見た目統一: ブロックのフォントサイズ（テーマの`titleMedium`/`bodyMedium`を参照）・内側padding・`_CategoryTab`の縦paddingをシンプル側に揃えた
- 一対一覧・広場一覧のブロックがヘッダー（一対/広場切り替えタブ）や区切り線に被る不具合を、ヘッダーと一覧の間の隙間追加・一覧左右paddingの追加で修正（`talks_tab.dart`）。寄合一覧サイドバー（`room_list_pane.dart`）でも区切り線との被りを上部padding追加で同様に修正
- 隣接ブロックの左下角が全ての箱で同じ形に欠けて見える不具合を修正。共有辺の生成方向を固定する設計上、4辺のうち必ず1箇所だけ両端とも頂点から離れた点同士がつながる構造的な欠陥と判明し、四隅の実座標を明示的にパスへ挿入することで解消。独立辺（左右辺）のジグザグ強度も引き上げ（jitter 2.4→3.0、segments 5→6）、箱ごとの個性を出した
- 一対一覧と広場一覧で左揃えの基準がズレる不具合を修正。原因は`talks_tab.dart`の一覧を包む外側`Column`の`crossAxisAlignment`が既定値`center`のままで、中身（`GekigaJointedTileList`）が自身の内容量に応じた幅だけを主張し、その幅で中央寄せされていたこと（一覧ごとに中身の最大幅が違うと中央寄せの基準もズレる）。`CrossAxisAlignment.start`を明示指定して解消

### 詰まったところ
- Web専用の描画不具合は`flutter test`では再現せず、原因特定にdart2jsコンパイル+node実行での直接検証が必要だった
- 一対一覧・広場一覧の左揃えのズレは、当初2つのビルドメソッド（`_buildDirectMessages`/`_buildGroups`）の構造が完全に同一であることまでは確認できたが、原因が個別コードの違いではなく外側`Column`のデフォルト中央寄せという間接的な要因だったため、特定に複数回のユーザー指摘・調査を要した

### 次にやること
- 実機・複数プラットフォームでの網羅的な動作確認は未実施（hot reloadでの簡易確認のみ）
- 左揃え修正が実際に解消したかのユーザーによる実機確認待ち

## 2026-08-03

### 実装内容
- CLAUDE.mdを更新: 実装完了時に動作確認用サーバー（`flutter run`）を自動で立ち上げる必要は無いと明記（ユーザーから明示的な指示があった場合のみ起動、既に起動済みの流れでの変更はホットリロード対応を継続）
- 会話ごとのプロフィールカード機能を身だしなみ＞工房へ移動。標準カードを使っている会話は表示せず、明示的に個別カードを割り当てている会話のみ一覧表示し、「＋」ボタンから未割り当ての会話を選んで即座に設定できるようにした（`profile_tab.dart`、`settings_tab.dart`からは同機能を削除）
- 各会話（広場の全体設定ポップアップ・単一モード広場のハンバーガーメニュー・一対のハンバーガーメニュー）からも直接「使うプロフィールカード」を選べる導線を追加（新規`conversation_profile_card_dialog.dart`）。従来の設定タブ＞語らいの一覧にも「相手・広場ごとに、自分が使うカードを選べる」旨の説明文を追加し、相手側のカードを選ぶ機能だと誤解されないようにした
- 広場の削除機能を追加。長のみ実行可（`GroupRepository.deleteGroup`、firestore.rulesも長のみ許可に変更）。寄合→メッセージ→ロール→広場本体の順に物理削除する、既存の「マーカーを立ててから物理削除」パターンを踏襲
- アカウント削除時に長を務める広場が残っている場合、譲渡を求めるガードダイアログ（`_OwnerGroupsGuardDialog`）を挟むよう変更。全ての広場で長を譲渡し終えるまで削除に進めない。Cloud Functions側（`deleteAccountImmediately`・`processAccountDeletions`）にも同じ制約の安全策を追加
- 縦表示（狭い画面）でのチャット画面のUXを変更: 寄合一覧のフルスクリーンドリルダウン画面（`room_list_screen.dart`）を廃止し、広場・一対をタップすると直接メッセージ画面に遷移。寄合の切り替えはメッセージ画面AppBar直下の横スクロールタブバー（新規`room_tab_bar.dart`）から行う。横表示（サイドバー表示）は変更なし
- 劇画UIスタイルのメニューボックス（`GekigaPanelBox`/`GekigaMenuTile`、一対・広場・寄合一覧・設定・身だしなみ共通）のサイズ調整。左詰めで端末からはみ出す不具合を、左に12px分の余白を入れて修正。シンプルスタイルとの縦幅の差についてユーザーからのフィードバックを複数回受け、内側paddingを段階的に拡大（vertical: 12→16→24）。横幅を親いっぱいに広げる`expand`オプションを一時追加したが、「横幅は可変のままが良い」というフィードバックを受けて削除し、可変幅のみに一本化した
- 劇画UIのギザギザ枠意匠を、これまで一対・広場・寄合一覧のリスト項目にしか適用していなかったところから、「＋」ボタン・ラジオボタン・テキストボックスへ拡張。新規共通ウィジェット`GekigaIconButton`（`lib/widgets/gekiga/gekiga_icon_badge.dart`、＋・歯車等の単体アイコンボタン用のギザギザ正方形バッジ）と`GekigaTextField`（`lib/widgets/gekiga/gekiga_text_field.dart`、ギザギザ枠のテキスト入力欄）を新設し、寄合一覧サイドバー・タブバーの＋/歯車ボタンと寄合名入力欄（`room_list_pane.dart`/`room_tab_bar.dart`）、一対・広場一覧の＋ボタン（`talks_tab.dart`）、友達検索・広場作成ダイアログの入力欄（`add_chat_dialog.dart`/`create_group_dialog.dart`）に適用。設定のUIスタイル選択（唯一存在するラジオボタン）も、既存の`GekigaMenuTile`の`selected`挙動（選択中=白地黒字/未選択=黒地白字）を流用したギザギザ枠に置き換え（`settings_tab.dart`）。いずれも劇画スタイル選択時のみ切り替わり、シンプルスタイルの見た目には影響しない
- 狭い画面の寄合タブバー（`room_tab_bar.dart`）に、ホーム画面のナビチップ（`_NavChip`）と同じ「指でなぞった先のタブへその場で切り替える」スライド操作を追加。ナビチップと異なり、なぞっている間セルを浮き上がらせる演出は付けない方針で実装。寄合数が多く横スクロールが必要な状態でこの操作を常時有効にすると、ドラッグが「タブ切り替え」と「一覧の横スクロール」で競合し本来のスクロールができなくなるため、全セルが画面幅に収まりきっている（`ScrollController.maxScrollExtent <= 0`）ときだけスライド切り替えを有効にする安全策を追加。ウィジェット自体をConsumerWidgetからConsumerStatefulWidgetに変更（GlobalKeyでのセル位置測定・ScrollController保持のため）
- CLAUDE.mdに「作業後に必ずホットリロードして、http://localhost:8765/ にアクセスして、動作確認をすること」を追記（ユーザーによる直接編集）
- Vercelの自動デプロイが「commit author did not have contributing access」でブロックされる不具合の原因を調査。このリポジトリのローカルgit設定（`user.name=arag616`/`user.email=arataurusu@proton.me`、個人アカウント）が、Vercelプロジェクト（`rhingofficial-3304`チーム、Hobbyプラン）と連携しているGitHubアカウント（`rhing-official`/`rhing.official@gmail.com`）と一致していないことが原因と判断し、このリポジトリのみ`git config --local user.name/user.email`を`rhing-official <rhing.official@gmail.com>`に変更（グローバル設定は変更していない）。今回の日記コミットが変更後初のコミットとなるため、次回以降Vercelデプロイがブロックされずに通るかは要確認
- 劇画UIのアイコン背後の「四角」バッジ（`GekigaBadgePainter`、ナビチップ・メッセージ画面アイコン共通）を、直線縁取りの非対称な旗/凧型四角形から、他の劇画パーツ（`GekigaIconBadge`/`GekigaPanelBox`/テキスト欄/吹き出し）と同じ`handDrawnPolygonPath`ベースの手描き風ジグザグ正方形に統一。黒枠→白縁取り→アイデンティティカラー塗りの3層構造（`insetPolygon`による同心図形）は維持しつつ、3層とも同じ`seed`でジグザグ化することで同心状に連動して見えるようにした。従来`paint()`内で未使用だった`seed`パラメータが初めて意味を持つようになった
- ホーム画面ナビチップ（語らい/身だしなみ/設定）の劇画スタイル用アイコンを、Higgsfield（AI画像生成）で生成した角ばったモノクロ画像に順次差し替える対応に着手。ユーザー提供のPersona/Metaphor系ゲームのメニューUI参考画像（特に設定の歯車アイコン）を元に、`recraft_v4_1`（要Basicプラン以上、このアカウントでは403で拒否）→`z_image`（写実的な質感になり不採用）→`nano_banana_pro`（`nano_banana_2`に自動ルーティング、狙い通りのフラットな太線ブロック状シルエットで採用）の順にモデルを試し、設定の歯車アイコンのみ生成・`remove_background`で背景透過化して`assets/icons/nav_settings.png`として組み込んだ（`pubspec.yaml`に`assets/icons/`追加、`home_screen.dart`に`_gekigaIconAssets`リストと`_NavChip`の`gekigaIconAsset`フィールドを追加、劇画スタイル分岐のみ`Image.asset`＋`colorBlendMode: BlendMode.srcIn`で選択状態に応じて白/白70%着色）。語らい・身だしなみの2アイコンは生成前にHiggsfieldのクレジット（freeプラン）が尽きたため保留とし、`_gekigaIconAssets`はnullのままフォールバックでMaterialアイコン表示を維持する設計にした
- SVGへの変換は行わず、PNGをそのまま高解像度で使う方針とした（このアイコンはナビチップ1箇所・固定56×56ボックスでしか使わないため、Flutterの解像度バリアント機構無しでも画質面で問題ないと判断）
- 実機で確認したユーザーから、ナビチップ設定アイコンのHiggsfield生成PNGは「デザイナーを雇って本格的に修正するまでは不要」との指示を受け、標準の`Icons.settings_outlined`（`GekigaBadgeShape`のジグザグ枠に載せる従来の形）に差し戻し。`_gekigaIconAssets`・`_NavChip`の`gekigaIconAsset`フィールド・`Image.asset`分岐を削除し、`assets/icons/nav_settings.png`と`pubspec.yaml`の`assets/icons/`エントリも削除した
- 劇画UIの「選択中=白地黒字/未選択=黒地白字のギザギザボックス」ルールが未適用だった箇所を一括対応。「一対/広場」カテゴリタブ（`talks_tab.dart`の`_CategoryTab`、`StatelessWidget`→`ConsumerWidget`化し`GekigaPanelBox`でラップ）、アクセントカラーのカラーコード入力欄（`GekigaTextField`に`suffixIcon`/`errorText`/`inputFormatters`/`textCapitalization`を追加した上で適用、「カラーコード」ラベルは削除、フォーカス時も黒地白字固定という指示は`GekigaTextField`の既存の固定スタイルでそのまま満たされた）に対応
- 設定タブの選択系UIのうち`ChoiceChip`/`SegmentedButton`で実装されていた4箇所（外観・用語スタイル・表示言語・時刻表示形式）を、まず`RadioGroup`+`RadioListTile`（シンプルスタイル側）に統一した上で劇画分岐（`GekigaMenuTile`）を追加。既に`RadioListTile`だった2箇所（語らい表示スタイル・送信キー）は劇画分岐の追加のみ。既存の`_UiStyleFolder`のパターンをそのまま踏襲した
- 「選択中=白地黒字、未選択=黒地白字のギザギザボックスで統一する」という設計ルールと、「ChoiceChip/SegmentedButtonはまずラジオボタンに統一してから劇画分岐を追加する」という方針を、ユーザーから「記憶しておいて」と明示指示されたためfeedbackメモリ（`feedback_gekiga_selection_ui.md`）として保存した

### 詰まったところ
- 劇画UIのサイズ調整で、1回目のpadding微増だけでは「大きくなっていない」と再度指摘を受けた。Simple版が`Card`/`ListTile`で親幅いっぱいに広がるのに対し劇画版は文字量に応じた可変幅のままだったことが視覚的な差の主因と判断し、`expand`オプションを追加したが、直後に「横幅は可変が良い」と方針が変わり撤回した。最終的には縦方向のpadding拡大のみで対応
- 寄合タブバーへのスライド切り替え追加では、対象を横スクロールタブバー（`room_tab_bar.dart`）とサイドバー一覧（`room_list_pane.dart`）のどちらにするか、後者はスクロール可能なリストでドラッグ操作が本来のスクロールと衝突しうるため、実装前にユーザーへ確認を取ってから横スクロールタブバーのみに絞った
- Vercelのデプロイブロックは、当初「push時にどのGitHubアカウントを選ぶか」の問題だと思い込んで案内したが、ユーザーから「rhingを選択したのに毎回失敗する」と指摘を受けて調査し直し、実際はコミット自体のauthor情報（ローカルgit設定）が原因と判明した。push時の認証アカウントとコミットのauthor情報は別物という点の切り分けに時間がかかった
- Higgsfieldでのアイコン生成で、指定した「フラット・モノクロ・角ばった」スタイルに対しモデルによって出力傾向が大きく異なった（`z_image`は写実的な金属テクスチャ・3Dの歯車になってしまい、プロンプトの「no gradients no shading」等の指示だけでは制御しきれなかった）。`nano_banana_pro`系に切り替えて解決したが、事前にどのモデルが適切かは試行錯誤が必要だった
- Higgsfieldの`recraft_v4_1`（ベクターアイコン生成に最適なモデル）がアカウントのプラン制限（Basicプラン以上必須）で使えず、次点のモデルで代替する必要があった
- アイコン生成の途中でHiggsfieldのクレジットが尽き（freeプラン、残り1.85→枯渇）、語らい・身だしなみの2アイコンが生成できなかった。クレジットが時間経過で回復するかは確認する手段がなく、ユーザー判断で今回は設定アイコンのみ組み込み、残り2つは保留することにした
- 今回のセッション自体は技術的な詰まりは特になし（実装前にAskUserQuestionで対象範囲を都度確認しながら進めた）

### 次にやること
- 今日実装した一連の機能（会話ごとのプロフィールカードの移動・会話設定からの直接選択、広場削除、アカウント削除の長ガード、縦表示の寄合タブバー化、劇画UIのサイズ調整、劇画UIの＋ボタン/ラジオボタン/テキストボックスへの拡張、寄合タブバーのスライド切り替え、劇画UIバッジ形状のジグザグ化、ナビチップアイコンのAI生成差し替え→差し戻し、一対/広場タブ・アクセントカラー欄・設定タブ選択系UI6箇所のギザギザボックス化）はホットリロードでの簡易確認のみで、実機・複数プラットフォームでの網羅的な動作確認は未実施
- 劇画UIのブロックサイズがユーザーの目視で十分かは要フォローアップ（縦padding拡大後の見た目はまだ未確認）
- 寄合一覧のサイドバー版（`room_list_pane.dart`）へのスライド切り替え適用は今回見送ったため、要望があれば別途スクロールとの共存方法を検討
- git設定変更後のVercel自動デプロイが実際にブロック解除されるか、次回push時に要確認
- 劇画UIバッジのジグザグ度合い（jitter: 2.2等の数値）はユーザーによるホットリロードでの目視調整が前提、まだ確定値ではない
- ナビチップの語らい・身だしなみ・設定アイコンのAI生成差し替えは、デザイナーを雇って本格対応するまで見送り（標準Materialアイコンのまま）。当面Higgsfieldでの再挑戦は予定なし

## 2026-07-30

### 実装内容
- ホーム画面タブのドラッグ選択チップ: 指がチップとチップの間にある瞬間は両方を浮き上がらせ、色は元々選択していたタブ側のみに付くよう修正。浮き上がりアニメーションを`RepaintBoundary`で囲んで、周囲の重いタブ内容の再描画に巻き込まれてカクつく問題を解消
- 広場に「寄合を単一にまとめる」機能を追加（`GroupRepository.setRoomsEnabled(enabled: false)`、寄合が1つしか無い場合のみ実行可）。全体設定ポップアップ（新規`group_settings_popup.dart`）から`manageRoles`権限保持者が操作できる。firestore.rulesの`roomsEnabled`書き込み制限（trueへの変更のみ許可）を両方向に緩和
- モバイルUIの寄合一覧画面（`room_list_screen.dart`）を、画面いっぱいに広がる表示からDiscordを参考にした固定幅（220px）のサイドバー表示に変更
- 新UIスタイル「劇画」を追加（`AppUiStyle`列挙型・`app_ui_style_provider.dart`、設定タブ＞アプリケーションから選択可、SharedPreferences保存＋Firestore経由で他端末に同期）。メッセージ画面（`chat_screen.dart`）に適用: 背景を赤に、吹き出しを手描き風ギザギザ・白黒反転（自分=白地黒文字、相手=黒地白文字）に、アイコン背後には身だしなみの「イメージカラー」（`AppUser.imageColor`、7/29実装済みだが未使用だった項目）で塗った四角いバッジ（黒枠→白縁取り→色塗りの3層）を追加
- バッジの四角形は、ユーザー提供の手描き参考スケッチに近づけるため頂点座標を何度も調整。最終的に、頂点を凹ませない凸四角形にした上で、縁取り計算を「頂点を単純に重心方向へ縮める」方式から「各辺を内側へ平行移動して交点を求める」方式に書き換え、縁の太さが均一になるよう修正。ユーザー自身が数値を調整できるよう、頂点配列の場所と意味を案内
- 一時的に実装した「吹き出しの下を縫うように、アイコンの左右位置に応じて這う黒い太線」機能は、ユーザー判断で実装後に完全に削除
- SO-53C実機の再接続、開発用Webサーバー（`flutter run -d web-server`）のクライアント未接続状態からの再起動を複数回実施

### 詰まったところ
- 劇画スタイルのアイコンバッジ形状の実装で、ユーザー提供の手描きスケッチをピクセル単位で解析しても「正方形になっている」「三角に見える」と複数回指摘を受けた。根本原因は、頂点の1つを凹ませた際に単純な重心方向への縮小処理では凹み部分で縁取りの太さが破綻し、白い縁取りがほぼ潰れて見えなくなっていたこと。辺オフセット＋交点計算の方式に直して解決した
- Flutterのホットリロードは、constクラスへの必須フィールドの追加・削除を反映できず（`Exception: Const class cannot remove fields`等）、ホットリスタートが必要になる場面に複数回遭遇した
- 開発用Webサーバーのプロセスが長時間セッション中に度々「クライアント未接続」で応答しなくなり、プロセスの再起動が必要だった
- Vivaldiがネイティブバイナリではなくimage Flatpak経由のインストールだったため、`flatpak run com.vivaldi.Vivaldi`での起動に切り替える必要があった
- Claude Codeの会話履歴自体をgit同期したいという要望があったが、セッションの`.jsonl`ファイル単体で296MBあり GitHubの100MB制限を超えるため、ユーザー判断で見送った

### 次にやること
- 劇画スタイルのアイコンバッジの正確な形状は、ユーザー自身が`_GekigaBadgePainter`の頂点座標を調整して詰める予定。まだ最終形は未確定
- 「寄合を単一にまとめる」機能・Discord風サイドバー・劇画UIスタイル全般について、実機・複数プラットフォームでの網羅的な動作確認は未実施
- Higgsfieldでの素材生成（AI画像生成でバッジ等のグラフィックを作る案）は見送りを提案済みだが、ユーザーの最終判断はまだ

## 2026-07-29

### 実装内容
- 友達追加・広場作成を、画面遷移（`add_chat_screen.dart`/`create_group_screen.dart`）から語らいタブ「＋」メニューに重ねて開くポップアップ（新規`add_chat_dialog.dart`の`AddChatDialogContent`・`create_group_dialog.dart`の`CreateGroupDialogContent`）に変更。`/add-chat`・`/create-group`ルートは廃止。友達追加ポップアップは検索→（未フレンドの場合）確認ステップの2段階UIに、広場作成ポップアップには「寄合を複数作成する」トグルを追加
- 会話ごとのプロフィールカード機能を新規実装（`AppUser.conversationProfileCardId: Map<String, String>`、未指定時は標準にフォールバック）。友達申請確認・招待経由の友達申請・広場参加リクエスト・設定タブ＞語らいの新セクションから、会話（一対/広場）ごとに使うカードを選べる。選択UIは新規共通ウィジェット`lib/widgets/profile_card_picker.dart`の`ProfileCardPicker`に切り出し、メッセージ画面・チャットペイン・プロフィールカードダイアログの送信者名/アイコン表示にも反映
- 寄合の単一/複数モード（`Group.roomsEnabled`/`DirectMessage.roomsEnabled`）を実装。広場作成時のトグルで初期モードを選べ、単一モードはハンバーガーメニューの「寄合を複数扱う」（広場・`manageRooms`権限要）／「寄合を増やす」（一対）でいつでも複数モードへ切り替え可能（逆方向はfirestore.rulesでも不可）。単一モードでは寄合一覧のドリルダウン・サイドバーを経由せず`defaultRoomId`を直接開く。寄合の名前変更機能（`renameRoom`）も追加
- 身だしなみに「イメージカラー」を追加（蔵に1つだけ登録・登録時に自動適用、`AppUser.imageColor`）。用途は別途説明予定のためUI実装のみ
- メッセージ画面の空白部分でミドルクリック/長押しすると、ブラウザのように上下へ自動スクロールする機能を追加（`chat_screen.dart`）。既存の「吹き出し全体の長押しでメニュー展開」機能と競合するため、ユーザー指示に基づき吹き出し部分は長押し/右クリックのみメニュー展開、余白部分は長押しで自動スクロールに役割分担
- プロフィールカードダイアログ（`user_profile_card_dialog.dart`）をユーザーからの複数回のフィードバックに沿って再設計: メンバー一覧から開いた場合のみ長の譲渡ボタン（プライマリー、ジャンプボタンの右横）を表示、IDの非表示、フッター（友達申請UI）を実質廃止し身だしなみで作ったカードそのままの見た目に統一、確認ダイアログの文言変更。カードのアスペクト比が身だしなみの実物（`height=width*1.25`）とズレて画像が意図と違う位置で切り取られる・画面をはみ出す不具合、下端だけ角丸にならない不具合も修正。2度目のズレ発生を受け、カードの実際の描画コードを共通ウィジェット`lib/widgets/profile_card_view.dart`の`ProfileCardView`に切り出し、身だしなみの工房カード一覧（`profile_tab.dart`）とダイアログの両方から使うよう統一
- プロフィールカードから一対へジャンプすると、左右分割表示（PC等の広い画面）でサイドバーが消えて全画面表示になってしまう不具合を修正。新規`lib/providers/chat_navigation_providers.dart`の`pendingDmSelectionProvider`（Riverpod 3の`Notifier`）経由で`TalksTab`自身に一覧クリック時と同じ処理をさせる方式に変更（狭い画面では従来通りの全画面ルートpushのまま）
- メンバー一覧のBOTTOM OVERFLOWEDエラーを修正（役職チップを`trailing`から`subtitle`へ移動）。長の表示もテキストチップからアイコンに変更
- 以下のバグを修正:
  - `GroupRepository.watchRooms`/`DirectMessageRepository.watchRooms`にユーザーIDでの絞り込み`where`句が無いと、Firestoreのlist操作が「返り得る全ドキュメントがルールを満たすと証明できない」として`permission-denied`になり寄合一覧が一切表示されない不具合（2026-07-28の複数寄合機能実装当初から混入、2026-07-29に発覚・修正）
  - 寄合一覧サイドバーが一瞬表示されて消える不具合。上位`StreamBuilder`の再構築のたびに`watchRooms(...)`を呼び直し`Stream`インスタンスが変わって購読し直されていたのが原因。選択中id変更時のみ作り直すキャッシュを`talks_tab.dart`に導入
  - ロール優先順位・長の譲渡ポップアップを開いたままにすると変更が反映されない不具合。`lib/providers/group_providers.dart`の`watchedGroupProvider`で`liveGroup`を都度解決するよう修正
  - メッセージ0件のまま`defaultRoomId`が未設定で一覧に出てこない一対を、`getOrCreateDirectMessage`で気付いた時点で自己修復するよう修正。`watchDirectMessages`の`orderBy('lastMessageAt')`も廃止しクライアント側ソートに変更（同種の除外バグ対策）

### 詰まったところ
- Riverpod 3系では旧来の`StateProvider`が廃止されており、既存コード（`accent_color_provider.dart`）に倣って`Notifier`/`NotifierProvider`で書く必要があると気付くまで一度コンパイルエラーになった
- Flutterの`Dialog`ウィジェットは`shape`で角丸を指定しても中身を自動でクリップしないため、フッターの有無によって下端だけ角丸にならない・画面をはみ出す不具合につながった。`ClipRRect`で明示的に囲む対応が必要だった
- 自動スクロール機能の実装で、既存の「吹き出し全体の長押しでメニュー展開」ジェスチャーと新機能が直接競合する設計上の対立に気付き、実装前にユーザーへ確認を取ってから対応方針を決めた
- プロフィールカードダイアログのデザインが、身だしなみ側の実装変更に追従できず2度ズレた（1度目はレイアウト・配色、2度目はアスペクト比）。個別に数値を合わせ直すのではなく、実際の描画コードを共通ウィジェット化する方針に切り替えた
- 開発サーバー（`flutter run -d web-server`）・Vivaldiのプロセスが会話の途中で落ちていることが複数回あり、その都度検知して再起動が必要だった

### 次にやること
- 今日実装した機能（会話ごとのプロフィールカード、寄合の単一/複数モード切替、イメージカラー、自動スクロール、プロフィールカードダイアログの一連の修正、一対ジャンプ時のサイドバー表示）はブラウザでの簡易確認のみで、実機・複数アカウントでの動作確認は未実施
- CLAUDE.mdの「寄合の単一/複数モード」節が広場作成画面を`create_group_screen.dart`と記載しているが、実際は`create_group_dialog.dart`にファイル名が変わっている（本文中の言及の更新漏れ）
- イメージカラーの実際の用途（どの場面で使うか）はユーザーから別途説明予定、未着手

## 2026-07-28

### 実装内容
- 広場・一対どちらでも1つの会話の中に複数の「寄合」（テキストチャンネル）を作れる機能を実装。友達一覧・広場一覧のサイドバーの右隣に、選択中の会話の寄合一覧サイドバー（新規`lib/features/chat/room_list_pane.dart`の`RoomListPane`）を追加し、寄合の追加（確認無し）・削除（「本当に削除しますか？」の確認あり、最後の1つは削除不可）ができるようにした
  - 一対側の複数寄合は元々CLAUDE.mdでフェーズ3想定だった「席」に近い機能だが、ユーザー確認の上「席」ではなく広場と同じ「寄合」の呼称のまま前倒しで実装（CLAUDE.mdの用語表・フェーズ表・データモデル節を更新）
  - 広場は既存の`rooms`サブコレクション・`defaultRoomId`という複数ルーム前提の設計を拡張（`GroupRepository.watchRooms`/`createRoom`/`deleteRoom`新設、`respondToJoinRequest`/`leaveGroup`/`setReadReceiptsEnabled`をdefaultRoomId決め打ちから全room走査に修正）。寄合の追加・削除は長・モデレーターのみ（firestore.rulesで強制、従来メンバーなら誰でもroom作成できてしまっていた未整備状態を締めた）
  - 一対側は`directMessages/{dmId}/messages`のフラット構造を、広場と同じ`directMessages/{dmId}/rooms/{roomId}/messages`構造に変更（`DirectMessage.defaultRoomId`追加、新規`lib/models/dm_room.dart`の`DmRoom`）。参加者2人はどちらも対等に寄合の追加・削除が可能。既存データはCloud Functionsの一度きりの移行処理（`functions/src/index.ts`、実行後にソースから削除済み）で新構造へ移行（本番の8件のDMを移行、2回目の実行で`migrated:0`となることを確認しべき等性を確認）
  - 寄合の削除は既存のseverance（絶縁）・既読オフと同じ「削除実行者を記録するマーカーフィールド（`roomDeletionRequestedBy`/`deletionRequestedBy`）を立ててからメッセージを物理削除し、最後に寄合自体を削除する」パターンを踏襲
  - 狭い画面では`/chat/dm-rooms`・`/chat/group-rooms`ルート（新規`lib/features/chat/room_list_screen.dart`）でフルスクリーンの寄合一覧を経由してから個々のチャット画面を開くドリルダウン構成、広い画面ではサイドバーを常設する分割表示
  - firestore.rulesをデプロイ済み。functionsも移行コード削除後の状態で再デプロイ済み（本番の関数は`deleteAccountImmediately`・`processAccountDeletions`のみに戻した）
- 一対のハンバーガーメニューに「一対の削除」項目を追加。相手がアカウントを削除した場合（`DirectMessage.accountDeletedUserId != null`）のみ表示され、通知に「いいえ」と答えた後・未応答のままでも、いつでもこの項目から削除できるようにした（確認ダイアログ→`deleteDmAfterAccountDeletion`→成功後ホームへ遷移）。表示条件はfirestore.rulesの既存の削除許可条件と同じため、ルール変更は不要だった
- サイドバーの寄合一覧ヘッダーが固定文言「寄合」だったのを、表示中の広場名／一対の相手の呼び名に変更（`RoomListPane`に`conversationName`を追加、分割表示・狭画面ドリルダウン両方に反映）
- 友達ステータス表示のバグを修正: プロフィールカードの「友達です」判定が`FriendRequest.status == accepted`（申請の履歴）を見ていたため、絶縁等で実際の`friends`サブコレクションが消えても古い申請ステータスが残っていると誤表示され続けていた。実データ（`FriendRepository.isFriend()`）を根拠にするよう修正。連動して`sendRequest`側にも同種のバグ（acceptedステータス頼りで再申請が無効化される）があり、こちらも`isFriend()`で確認してから再申請するよう修正
- 広場のカスタムロール機能を実装（CLAUDE.mdフェーズ3想定を前倒し）。ロールは名前＋色（`#RRGGBB`自由入力、アクセントカラー設定と同じ方式）のみを持つ見た目専用機能で、既存の長・モデレーター・メンバー権限（`memberRoles`）とは無関係。メッセージ画面の呼び名（アイコン横）のフォントカラーに反映される（※この設計は同日中にDiscordの権限UIを参考にした本格版へ全面置き換え、下記参照）
  - 新規`groups/{groupId}/roles/{roleId}`（`GroupRole`）、`Group.roleAssignments`（広場全体での付与）、`Room.roleAssignments`（寄合ごとの付与、広場全体より優先）を追加。1人のメンバーには1ロールまで
  - ハンバーガーメニュー「ロール管理」（長・モデレーターのみ有効）→ロールのCRUD用ポップアップ（新規`lib/features/chat/group_role_list_popup.dart`）。メンバー一覧ポップアップ（`group_member_list_screen.dart`）に「広場全体」／「この寄合のみ」の切り替えを追加し、メンバーごとに付与・解除する
  - `GroupChatPane`がロール一覧・寄合一覧をwatchし、送信者ごとに寄合限定→広場全体の順で付与ロールを解決して色を`ChatScreen.senderNameColorResolver`経由で`_SenderName`に渡す（一対には渡さない）
  - firestore.rulesに`groups/{groupId}/roles/{roleId}`のCRUDルール（長・モデレーターのみ書き込み、メンバー全員読み取り）と、`groups/{groupId}`更新ルールへの`roleAssignments`許可枝を追加してデプロイ済み
- 広場の寄合一覧サイドバー（`RoomListPane`）が空表示になる不具合を修正: Firestoreの`orderBy('createdAt')`が、そのフィールドを持たないドキュメント（`createdAt`未設定の既存の「メイン」寄合や、`FieldValue.serverTimestamp()`がクライアント側でまだ`null`のまま届く作成直後の新規寄合）をクエリ結果から除外してしまうのが原因。`GroupRepository`/`DirectMessageRepository`の`watchRooms`等から`orderBy`を外し、`null`を最古扱いにするクライアント側ソートに変更
- 広場のカスタムロール機能を、上記の「名前＋色のみ・1人1ロールまで・寄合ごと個別付与」という簡易版から、Discordのロール権限設定画面を参考にした本格版へ全面刷新（ユーザーからスクリーンショット付きで方針転換の指示を受け、Plan Mode経由で要件を確認した上で実装）
  - ロールに実際の権限を持たせた。DaiDaiに実在する5つの管理操作にスコープを絞り、Discordにあってダイダイに無い概念（ボイスチャンネル・AutoMod等）は含めない: 寄合の管理／ロールの管理／既読機能のオン・オフ／参加リクエストの承認・却下／招待リンクの作成（`GroupPermission`, `lib/models/group_role.dart`）
  - 「長・モデレーター・メンバー」階層は廃止し、長（`Group.ownerId`、常に全権限）のみをカスタムロールとは別の特別な存在として残した。長はメンバー一覧から他のメンバーへ譲渡できるようにした（新機能、`GroupRepository.transferOwnership`）。モデレーターという概念自体は無くなり、カスタムロールに統合
  - 全メンバーに自動適用される、削除・改名不可の基準ロール（Discordの`@everyone`相当、`GroupRole.isEveryone`）を広場作成時に自動生成するようにした
  - 寄合ごとの個別ロール付与（`Room.roleAssignments`）は廃止し、代わりに1人のメンバーに複数のロールを同時付与できるように変更（`Group.roleAssignments: Map<String, List<String>>`）
  - 呼び名の色は、複数ロールのうちどれを優先するかをドラッグ＆ドロップで並べ替える優先順位リストで決定（`Group.rolePriority`、新規`lib/features/chat/group_role_priority_dialog.dart`の`GroupRolePriorityDialog`）。寄合ごとに`Room.rolePriorityOverride`で上書きでき、設定されていれば広場全体の順序より優先される
  - firestore.rulesはロールを跨いだ動的な権限判定ができないため、`Group.memberPermissions`（userId→有効な権限文字列リスト、基準ロール込みで解決済み）という非正規化キャッシュを新設し、ロール・付与の変更のたびに`GroupRepository._recomputeMemberPermissions`が全メンバー分再計算して書き込む方式にした（`Room.memberIds`が`Group.memberIds`を非正規化して持つ既存パターンを踏襲）。ルール側もこのフィールドを信頼して判定する
  - サイドバー（寄合一覧ペインのヘッダー）に歯車アイコン「広場自体の設定」を新設し、`manageRoles`権限を持つメンバーのみロール管理画面を開けるようにした
  - 「変更権限の変更」（権限編集UIのカスタマイズ）はユーザーから「後ほど伝えるから実装は保留」と明示されたため未実装のまま
  - 本番の既存2広場は、Cloud Functionsの一度きりの移行処理（`functions/src/index.ts`、実行後にソースから削除済み）で新形式に移行（基準ロールの作成、`memberRoles`が`moderator`だったメンバーの相当ロールへの変換、`memberPermissions`の初期計算）。2回実行して`everyoneCreated`が`2→0`になることを確認しべき等性を確認
  - firestore.rulesをデプロイ済み。functionsも移行コード削除後の状態で再デプロイ済み

### 詰まったところ
- `DmRoom`に参加者リストを持たせずfirestore.rulesで親`directMessages`ドキュメントを`get()`参照する設計を最初に検討したが、DM作成と寄合作成を同一`WriteBatch`で行うと、ルールの`get()`がバッチ内の未コミットの親ドキュメントを見られず`permission-denied`になる問題（`groupInvites`で既知だった問題と同種）に気付き、`DmRoom.participants`を denormalize して持たせる設計に変更
- ローカルのAdmin SDKスクリプトで移行結果を直接検証しようとしたが、このサンドボックス環境に`gcloud`が無くADC認証情報が用意できず断念。移行エンドポイントを2回叩いて`migrated`件数の変化を見る形（べき等性の確認）で代替検証した
- `firebase deploy --only functions`が、移行コードをソースから削除した後の再デプロイで「ローカルに存在しない関数の削除は非対話モードでは実行できない」と拒否されたため、`firebase functions:delete migrateDirectMessagesToRoomsOnce --region asia-northeast1 --force`で個別に削除
- ロール付与UIの「解除」選択ダイアログで、最初`Navigator.pop()`（引数無し=null）を「解除」に割り当てていたところ、バリア外タップで閉じた場合も同じくnullが返るため「誤タップで意図せずロールが解除される」バグになりかけた。「解除」専用の番人値（sentinel）をpopで返し、素のnull（ダイアログを閉じただけ）とは区別するよう修正して回避
- ロール権限の本格版実装で、firestore.rulesの`groups/{groupId}`更新ルールを当初`hasOnly(['memberIds','memberRoles'])`のように狭く書いていたところ、`respondToJoinRequest`の後続の`memberPermissions`単体書き込みや、`leaveGroup`が`roleAssignments`/`memberPermissions`を`memberIds`/`memberRoles`と同一バッチで一緒に更新する実際の呼び出しパターンを壊すことに設計段階で気付き、実行前に該当ルール分岐の`hasOnly`集合を広げて対応
- 基準ロール（everyoneロール）の自動作成を`createGroup`と同一バッチに含めていたところ、`roles/{roleId}`の作成ルールが親`groups/{groupId}`を`get()`参照するため、同一バッチ内の未コミットの広場ドキュメントを見られず`permission-denied`になる問題（寄合機能実装時の`DmRoom`と同種）に気付き、バッチコミット後の別書き込みに分離して回避

### 次にやること
- 実機・複数アカウントでの動作確認（広場: 長/モデレーターと一般メンバーでの権限差、一対: 参加者どちらからでも追加・削除できること、最後の1つの寄合が削除できないこと、defaultRoomIdが指す寄合を削除した際の差し替え、狭い画面でのドリルダウン・スワイプ戻り、カスタムロールの色分け表示・広場全体/寄合ごとの付与の切り替え）はサンドボックス環境では実施できず未確認のまま
- 一対の「一対の削除」メニュー項目は、実際にもう一方のアカウントを削除する操作を伴わないと再現確認できておらず未検証
- ロール権限本格版の実機・複数アカウントでの動作確認（権限チェックボックスの効果、複数ロール付与時の色優先順位、寄合ごとの優先順位上書き・デフォルトに戻す動作、長の譲渡、各権限を持たないメンバーでUI要素が非表示・無効化されること）は未確認のまま
- 「変更権限の変更」機能はユーザーからの詳細指示待ちで、スコープ未確定のため着手しない

## 2026-07-27

### 実装内容
- 広場（グループ）通話の不具合3件を調査・修正:
  - 着信音が出ない件: 実際にはグループ通話に「着信（ringing）」の概念自体が無い（`GroupCallStatus`が`active`/`ended`のみで、進行中の通話に自分から参加しにいくプル型設計）ことが判明。ユーザー確認の上、バナー通知＋着信音を3コールだけ鳴らす方式を新規実装。新設の`GroupIncomingCallListener`（`lib/features/call/group_incoming_call_listener.dart`）が、自分がメンバーの広場を横断して新規に始まった通話を検知し、バナーをタップすると確認無しでそのまま参加する（`GroupCallRepository.watchActiveGroupCallsForMember`を新設、`firestore.indexes.json`に`groupCalls`用の複合インデックスを追加しデプロイ済み）。`CallSoundPlayer.playRingtoneTimes(3)`を追加
  - 映像がお互い真っ暗になる（時々）件: (1) ICE candidateが`setRemoteDescription`より先に届くと`addCandidate`が失敗し無言で失われる競合状態、(2) ICE接続状態を一切監視しておらずシグナリング完了だけで「通話中」表示になり接続失敗が無言の黒画面になる、という2つの実バグを特定。一対・広場両方のコントローラにICE候補のバッファリング＋フラッシュ、ICE接続状態監視＋UI表示（「接続が不安定です」）を追加。あわせてTURNサーバー配線（`lib/features/call/rtc_config.dart`に共通化）を、これまで広場通話にしか無かったものを一対通話にも拡張（coturn未構築のため現状はSTUNのみで動作、根本解消はサーバー構築後）
  - 広場通話のUIを一対と差別化: 人数によらず常にグリッド表示から始まり、任意の枠をタップするとその参加者を全画面表示、下スワイプでグリッドに戻る挙動に`group_call_screen.dart`を書き換え（旧・2人時のみの「フルスクリーン＋コーナーPIP」分岐を廃止し統一）
- 日本語・利便性重視の用語「ダイレクトメッセージ」（一対/DMの利便性重視表記）を「ダイレクト」に短縮（`lib/l10n/vocabulary.dart`、CLAUDE.mdの用語表も同期）

### 詰まったところ
- 特になし（調査は3件ともサブエージェントによる並行探索で比較的早く原因特定できた）

### 次にやること
- 広場通話の着信バナー・映像不通修正・グリッドUIの実機確認（複数アカウント・複数端末が必要なためユーザー側での確認待ち）
- coturn（TURN）サーバー自体の構築（VM・DNS・証明書）はまだ未着手。構築後に一対・広場双方のNAT越え問題が本格的に解消する見込み

## 2026-07-26

### 実装内容
- 既読マーク周りの表示を調整: バッジが文字に重なっていた問題を、右下オフセットの拡大＋バッジの左右をメッセージの送信者(`isMe`)ではなく実際の左右寄せ(`alignRight`)に連動させることで解消（`allLeft`表示スタイルで自分のメッセージが左寄せになる場合も正しく右下に付くようにした）。一対の自分のメッセージの既読バッジは人数表示無しの✓のみ・タップ無反応に簡略化。既読者一覧ポップアップは、画面下部で開くと下端で見切れる問題を、表示位置に応じてバッジの上に反転表示するよう修正
- 既読の追加仕様: 一対で「相手のメッセージを自分が読んだか」は相手には見えても自分には表示価値が無いため非表示に変更（既読の記録・トラッキング自体は維持、広場は変更なし）
- モバイルで身だしなみタブの上余白が設定タブとズレていたのを、設定タブと同じ`Align+ConstrainedBox(480)+Padding(top:56)`に統一
- モバイルのスワイプナビゲーションを再設計: ホームタブ（語らい/身だしなみ/設定/運営）切り替えのスワイプ受付範囲を、タブコンテンツ全体からチップ帯（下部の丸ボタン行／広い画面では左の縦並び）のみに限定。語らいタブ内で横スワイプすると一対⇄広場を切り替えられるようにした（`SwipeBackDetector`の既存の向き規則をそのまま流用、右スワイプ＝広場→一対、左スワイプ＝一対→広場）。身だしなみ/設定/運営タブの狭い画面ドリルダウンは既存の向きが要件と一致していたためコード変更不要だった
- 縁結び・広場招待リンクのUI微修正: リンクをコピーした際の「リンクをコピーしました」スナックバー表示を削除、招待リンクポップアップ（広場）の「キャンセル」ボタンを削除、縁結びページ全体を幅480pxに固定してリンク欄・QRコード・ボタンを左寄せに変更（以前はページ全体が画面幅いっぱいに間延びし、QRコード等が中央寄りに表示されていた）
- 通話機能を包括的に改善（ユーザー報告: iOSでスピーカー無効・Androidでマイク無効・Zorinは正常、ノイキャンがKrisp比で弱い、インカメラ固定、セルフプレビューが映らない、映像タップでの主従切替不可、着信音・着信画面が無い、広場通話の入退室音が欲しい）:
  - スピーカー切替ボタンを追加（`Helper.setSpeakerphoneOn`、接続直後と2秒後の2回適用でiOSの既知の音声ルート競合に対処）
  - カメラ切替ボタンを追加（`Helper.switchCamera`、Web版はカメラ列挙+deviceId指定、ネイティブ版はtrackのみ）
  - 音声制約を`webrtc_media_constraints.dart`に集約し、Androidのみ単純化した制約にフォールバック（詳細な制約マップがAndroidで正しく解釈されない既知issue対策）。ノイキャンはCLAUDE.md方針通りWebRTC標準設定の見直しに留め、Krisp相当のML統合（フェーズ3のRNNoise）は今回スコープ外とユーザーに確認済み
  - 1対1・広場通話（2人時）で、映像をタップすると全画面/コーナーの表示が入れ替わる機能を追加。広場通話は1人待機中も自分のプレビューを全画面表示するよう変更
  - 着信の自動応答バグを修正（着信画面を開いた瞬間にSDP応答が自動送信され、応答ボタンが見た目だけだった）。応答ボタンを実際に機能させ、着信中はループ着信音＋応答時にバイブレーションが鳴るようにした（`audioplayers`パッケージ追加、`assets/sounds/`新設）
  - 広場通話で参加者が入室/退室すると音が鳴るようにした（自分が入室した時点で既にいた人には鳴らない）。着信音・入退室音の素材はユーザー提供のFLACファイル2点をffmpegでmp3に変換して使用（着信音は専用素材が無いため入室音を暫定流用、ファイル差し替えのみで後から変更可能な構成にした）
  - フォアグラウンド限定（バックグラウンド/プッシュ通知経由の着信対応はユーザー確認の上で今回スコープ外）
- モバイルで送信ボタン（紙飛行機アイコン）が物理キーボード誤検知時に消えて送信できなくなる報告を受け、判定条件の修正はせず、テキスト入力中は常にボタンを表示するよう単純化（ユーザー指示通り）
- 身だしなみ/設定/運営タブのセクション詳細で、右スワイプは隣接セクションではなく常にカテゴリ一覧のトップへ戻るよう変更し、各セクション最上部の「←＋セクション名」の見出し行を削除（戻る導線がスワイプに一本化されたため）。対応する回帰テスト3件（profile_workshop_test.dart / settings_navigation_test.dart / support_navigation_test.dart）を新仕様に合わせて更新
- 語らいタブの左右分割表示（横幅の広いタブレット等）で、会話ペインを横スワイプで全画面表示に切り替えられるようにした（左スワイプで一覧を隠して全画面化、右スワイプで分割表示に戻す）。一覧ペイン側のスワイプ（一対⇄広場切替）とは独立
- ホーム画面のメニューチップ帯でスワイプしてもタブ切り替えが効かない不具合を修正: スワイプ検知用`GestureDetector`が`Center`の内側（チップ自体の見た目サイズ）にしか付いておらず、チップ間の余白でスワイプしても反応しない状態になっていた。`GestureDetector`を`Center`の外側（帯全体）に移動して解消
- 設定タブのデスクトップ分割表示で、内容ペイン上部にあったセクション名の見出し（例:「入力」）を削除。サイドバーで選択中カテゴリが既に分かるため重複表示だった
- 語らいのハンバーガーメニューボタンを、AppBarアクションの中で一番右（通話・ビデオ通話ボタンの後ろ）に配置し直した
- 広場のプロフィールカードを編集しても再読み込みするまで画面に反映されない不具合を修正: `talks_tab.dart`が選択中の広場を保持する`_selectedGroup`をタップ時点のスナップショットのまま使い続けており、Firestoreの最新スナップショットと同期していなかったのが原因。会話ペインに渡す広場データを、その都度最新の一覧から選択中IDで引き直すよう修正
- 広場の招待リンクが何時間経ってもDiscord等のプレビューに反映されない不具合を修正: 招待URLが`/join/{groupId}`という固定文字列で常に同一だったため、外部サービス側がURL単位でプレビューをキャッシュし続けていたのが原因（サーバー側の画像・HTMLキャッシュ自体は既に短時間設定で問題なかった）。招待ダイアログを開くたびにキャッシュ回避用のクエリ（`?v=タイムスタンプ`）をURLに付与し、貼るたびに新しいURLとして再取得されるようにした
- 縁結びページのQRコード・下のボタンを中央揃えに修正
- 招待リンクのOGP画像（`api/og-image.mjs`）のCache-Controlが、`ImageResponse`生成時のオプション指定では既定値（1年キャッシュ）に追記される形で上書きされておらず、カード編集後もキャッシュされた古い画像が返り続けていた不具合を修正: レスポンス生成後に`response.headers.set()`で明示的に上書きするよう変更
- 招待リンクのキャッシュ回避方式を、クエリパラメータ（`?v=`）からパスセグメント（`/join/:groupId/:cacheBust`）に変更: Discordがクエリ文字列を無視してキャッシュキーを正規化してしまい効果が無いことを、異なる`?v=`値を付けた複数の投稿が全て同一の古いプレビューを表示し続ける実機検証で確認した上での変更（`app_router.dart`・`vercel.json`・`api/link-preview.js`・`group_invite_dialog.dart`）
- アイコン・背景画像アップロード時、GIFは`flutter_image_compress`によるWebP圧縮をスキップし、アニメーションを保持したまま`image/gif`でアップロードするよう修正（`lib/utils/image_format.dart`新設、`user_repository.dart`/`group_repository.dart`)
- メッセージ本文中のURLをタップしてブラウザで開けるようにするリンク化機能を追加（`url_launcher`導入、`LinkifiedText`新設、`TapGestureRecognizer`をStatefulWidgetで管理・破棄）
- 語らい内のURLの下に、外部サイトのタイトル・説明・サムネイル画像を表示するリンクプレビューカードを追加（`api/url-preview.js`新設、SSRF対策としてローカル/内部ホストは拒否、`LinkPreviewCard`/`LinkPreviewRepository`新設）。画像は固定アスペクト比でクロップせず元画像の縦横比を保って表示するよう調整（縦長すぎる場合のみ最大360pxで安全策のクロップ）
- リンクプレビューAPI（`api/url-preview.js`）にCORSヘッダーが無く、本番サイト自身からの呼び出し（同一オリジン）は動く一方、ローカル開発サーバーからのクロスオリジン呼び出しがブラウザにブロックされていた不具合を修正（`Access-Control-Allow-Origin: *`を追加）
- YouTubeの動画URLのプレビューが、実行リージョンによってはGDPR同意の中間ページの内容（タイトル「 - YouTube」、定型文の説明、画像無し）になってしまう不具合を修正。YouTube公式のoEmbed API（`youtube.com/oembed`）を優先利用するよう変更し、動画タイトル・投稿者名・サムネイルを確実に取得できるようにした。あわせて汎用スクレイパー側にも同意済みCookie（`CONSENT=YES+`）を追加
- firestore.rulesの蔵素材（アイコン・ステメ・ニックネーム）保管上限のバグを発見・修正: アプリ側の上限定数（`kMaxIcons=5`等、`lib/models/profile_material.dart`）とルール側の上限値（`icons.size()<=2`等）がズレており、3個目のアイコン追加が`permission-denied`で保存失敗していた。ルールをアプリ側の定数と一致させて本番Firestoreにデプロイ
- 招待リンクのOGP画像生成の遅さを改善: 画像変換とフォント読み込みを`Promise.all`で並列化、警温状態（コンテナ再利用時）向けにPNG変換結果・フォントデータをモジュールスコープでメモリキャッシュ、Vercel関数のメモリ割当を2048MBに増加。加えて、真因だった`GroupInviteDialog`のキャッシュ回避値（`cacheBust`）が招待ダイアログを開くたびに`DateTime.now()`から生成されておりVercelのキャッシュを常に無効化していた問題を、プロフィールカードの実内容（名前・一言・アイコンURL・背景URL）から算出するMD5ハッシュ方式に変更し、内容が同じ限り本来のキャッシュが効くよう修正
- 個人の工房プロフィールカードが重複作成される（少しずつ異なる3枚が並び、招待リンクではさらに別の4枚目が表示される）バグを根本修正。Exploreエージェントの調査で複数の要因が重なっていたと判明: ①`_WorkshopView`が配列の先頭`kMaxProfileCards`(3)件のみを位置で表示し4件目以降を画面から隠していた ②`activeProfileCardId`等は配列全体をidで検索するため隠れた4件目以降が招待プレビューに表示されうる状態だった ③既存カード編集の保存が今も「arrayRemoveで古い値を消す→arrayUnionで新しい値を足す」という非原子的な2手順のままでエラーも握りつぶしており、ローカルデータが古いと重複を生みうる構造だった ④`_ProfileTabState`がログイン時の一回取得のみでFirestoreとリアルタイム同期していなかった。`UserRepository`に`saveProfileCard`/`deleteProfileCard`をFirestoreトランザクションベースで追加（保存直前にサーバー最新配列を読み直しidで置換）、`watchUser`による実時間同期を追加、`_WorkshopView`の表示枠数を`max(kMaxProfileCards, 実際の件数)`にして超過分も含め全件表示・削除可能にした（`AppUser.copyWith`の`activeProfileCardId: null`が`??`のせいで実際にはクリアされない潜在バグも合わせて`clearActiveProfileCardId`フラグで修正）
- 通話機能の追加不具合7件の報告を受け対応: ①カメラ前面/背面切替で真っ黒・激重になる→切替後に`localRenderer.srcObject`の再代入と`RTCRtpSender.replaceTrack()`が抜けていたのが原因で追加、多重実行防止ガードも追加 ②カメラオフ→オンで映像が戻らない→①の症状が波及していたもので①の修正で解消見込み ③メイン/コーナー入れ替えが左右反転にしか見えない→Web版`RTCVideoView`がStatefulWidgetで`key`が無いと入れ替え時に同じStateを使い回し実際の映像は入れ替わっていなかった。`key: ObjectKey(renderer)`を追加 ④スピーカー/受話口ボタンの違いが分かりにくい→実際は1つのボタンのアイコンが切り替わっているだけだったと判明、アイコンとツールチップで明示 ⑤広場通話でそれぞれ別の空間に入ってしまう→「進行中の通話を確認してから無ければ作成する」処理がFirestore側で原子的でなく、複数人がほぼ同時に開始すると別々の通話ができてしまうチェック・アクト競合が原因。`groupActiveCallPointers/{groupId}`という新規ポインタドキュメントをFirestoreトランザクションで扱う`getOrCreateActiveGroupCall`に置き換えて解消。`firestore.rules`に新規match blockを追加し本番デプロイ
- 広場の招待リンクで参加申請を送れない不具合を修正: `GroupRepository.requestToJoin`は却下後の再申請・退会後の再参加時に同一ドキュメントを再利用する設計だが、`firestore.rules`の`joinRequests`更新許可が長・モデレーターのみで、申請者本人による再申請が`permission-denied`になっていた。申請者本人がstatusをpendingに戻す更新のみ許可するよう修正し本番デプロイ。広場メンバー一覧の承認/却下ボタンがawait・エラーハンドリング無しの投げっぱなしだったのもあわせて修正（失敗時はスナックバー表示）
- ブロック機能を拡充: ブロック済みの相手を一対（DM）一覧から非表示にし、設定タブに新規カテゴリ「語らい」を追加してブロックしたユーザー一覧・ブロック解除ボタンを表示できるようにした
- 招待リンク画面（`JoinGroupScreen`）の`_resolve()`が例外を一切捕捉しておらず、失敗するとスピナーが回り続けるだけで何も表示されない不具合を修正（エラー画面＋再読み込みボタンを追加）。あわせて、語らいタブの広場一覧に自分が送った承認待ちの参加申請を「申請中・承認待ち」タイルとして表示し、タップすると承認待ちである旨のダイアログを出す機能を新設（`GroupRepository.watchMyPendingJoinRequests`をcollectionGroupクエリで実装、対応するFirestoreコンポジットインデックスをデプロイ）
- 招待リンクを新しいタブで直接開くと語らい（ホーム）画面が開くだけで何も起きない不具合を修正: Flutter Webが既定で使うハッシュURL方式（例: `/#/join/xxx`）では、ハッシュ無しの通常URLである招待リンクを新しいタブで直接開いた際にgo_routerがパスを読み取れず常にルートへフォールバックしていたのが原因。`usePathUrlStrategy()`でパスベースのURL方式に切り替えて解消（`flutter_web_plugins`を新規依存として追加）
- ブロックとは別に、双方の同意により友達関係を解消し会話履歴を完全に物理削除する「絶縁」機能を追加。`DirectMessage.severanceRequestedBy`（提案者のuserId）1フィールドで提案・取り消し・辞退の状態を表現し、相手が同意すると全メッセージ（500件ずつバッチ削除）→双方のfriends関係→friendRequests→一対自体の順に完全削除する（`DirectMessageRepository.acceptSeverance`、復元不可）。`firestore.rules`で、severanceRequestedByに設定できる値を「自分のuid」「null」のみに制限し、各コレクションの削除は「提案者本人ではない参加者（＝同意した側）」のみ実行できるよう`get()`で検証する条件を追加し本番デプロイ
- ユーザーより「モデレーターという言葉を削除して」と依頼があり、表示文言のみの変更（役割・権限自体は維持）で対応（`groupRoleModerator`の表示を通常メンバーと同一にし、招待・参加画面の「長・モデレーターの承認」→「長の承認」に変更）。ただしユーザーの判断でこの変更は今回pushせず保留中

### 詰まったところ
- 縁結びページでQRコード・ボタンを囲む`Center`を外した際、意図に反してコンテナが右まで間延びして表示され続けた。ListViewの子要素は`Center`で囲むと親の全幅で中央寄せされる一方、`Center`を外すだけだとListViewから渡されるtightな幅制約でコンテナ自体が引き伸ばされることを見落としていたのが原因。最終的にページ全体を`Align(topLeft)+ConstrainedBox(maxWidth:480)`で包む形に直して解消した
- 通話機能の改善依頼は音声・映像・着信・広場通話と多岐にわたる大規模な要望だったため、実装前にプランモードでノイキャンの本気度（Krisp相当を目指すか標準設定の見直しに留めるか）・着信通知の対応範囲（フォアグラウンドのみかバックグラウンド/CallKit等まで作るか）・着信音素材（提供された入退室音を流用するか）をユーザーに確認し、スコープを確定してから着手した
- iOSのスピーカー・Androidのマイクの不具合は実機を持たないため直接検証できず、flutter_webrtcの既知issueに基づくベストエフォートの対処に留まる（ユーザーに実機再検証を依頼）
- ホームのメニューチップ帯スワイプ不具合は、`GestureDetector`の当たり判定が`Center`の内側の子（チップの見た目サイズ）に閉じ込められ、周囲の余白まで広がっていなかったのが原因。レイアウト上「帯の中にある」ように見えても、実際のヒットテスト範囲は子ウィジェットの実サイズに限られる点を見落としていた
- 広場プロフィールカードの反映遅延は、一覧表示自体はStreamBuilderで最新化される一方、選択状態（`_selectedGroup`）だけが選択時点のスナップショットのまま保持され続けるという、状態管理上の「二重管理」に起因していた。招待リンクの反映遅延は当初サーバー側のキャッシュ設定を疑ったが、ユーザーへのヒアリング（「新たに発行したリンクを毎回Discordに送っている」）で、招待URL自体が常に同一の固定文字列であるためDiscord側がURL単位でキャッシュしていたと分かり、キャッシュ回避クエリの付与という対処に至った
- クエリパラメータ方式のキャッシュ回避を実装しCache-Controlバグも修正した後も、ユーザーから複数回「変化無し」と報告が続いた。og-image APIを直接`curl -D -`で検証してCache-Controlの上書き漏れ自体は特定・修正できたが、その後も改善しなかったため、異なる`?v=`値を付けた投稿を複数比較してもらったところ全て同一の古い内容だったことから、Discordがクエリ文字列自体を無視してキャッシュキーを正規化しているという、サーバー側の実装だけでは気付けない外部サービス仕様が真因と判明し、パスセグメント方式への変更に至った
- リンクプレビューの見た目修正（固定アスペクト比→可変）後も「変化無し」の報告があり、当初はブラウザのキャッシュ／Service Worker残留を疑って調査した（`flutter run -d web-server`はホットリロードをブラウザへ自動プッシュする仕組みが無く、実際に「No client connected」の状態が続いていた）。配信中のコンパイル済みJSを直接取得して最新コードが正しく配信されていることまで確認したが、ユーザーからの「本番サイトでは表示されるがローカルでは表示されない」という追加情報で、真因はリンクプレビューAPIにCORSヘッダーが無く、本番サイト自身からの同一オリジン呼び出しは通る一方ローカル開発サーバーからのクロスオリジン呼び出しだけがブラウザにブロックされていたことだと判明した。診断の途中経路（キャッシュ/Service Worker）は結果的に外れだった
- YouTubeリンクのプレビューがDiscordのように表示されない件は、当初コードのバグを疑ったが、ローカルの`curl`では正しいog:tagが取得できるのに本番デプロイ済みの関数経由では取得できないという違いから、YouTube側がリージョンによってGDPR同意ページを返す既知の挙動が原因と特定した
- GIFアイコンが保存失敗する件は、当初GIF固有の不具合を疑ったが、実際は`firestore.rules`の上限値がアプリ側の定数と一致していなかった既存バグで、たまたま3個目のアイコン（GIF）を追加した際に初めて上限に達して表面化したものだった
- og-image生成の遅さは、ローカルでの計測では1秒未満で完了するのに本番では毎回4〜5秒かかり続けており（`x-vercel-cache`が常にMISS）、当初はサーバー側の処理内容自体を疑っていた。実際はクライアント側（招待ダイアログ）が毎回`DateTime.now()`由来のキャッシュ回避値を発行しており、Vercelのキャッシュそのものを毎回無効化していたというクライアント/サーバー間にまたがる問題で、サーバー側だけを見ていても気付けなかった
- 工房プロフィールカードの重複は2026-07-25に一度「今後の重複発生防止」のみ対応済みとして一旦保留にしていたが、実際には保存経路自体がまだ非原子的な2手順のままで、かつ`_WorkshopView`が4件目以降を表示から隠す作りだったため「直したはずなのに直っていないように見える」状態が続いていた。Exploreエージェントによる包括調査で表示・保存・同期の3箇所にまたがる複合要因だと判明した
- 通話機能の5件はいずれもカメラ・マイクの実機が無いと直接検証できないため、コードレベルの原因特定（`replaceTrack`未呼び出し・`RTCVideoView`の`key`未指定・Firestoreのチェック・アクト競合等）を根拠にベストエフォートで修正し、ユーザーに実機での再検証を依頼する形になった
- 「プロフィールカードを削除しても復活してくる」という報告があり調査したが、`deleteProfileCard`のFirestoreトランザクション・`firestore.rules`・楽観的UI更新のロジックいずれにも明確な不具合が見当たらなかった。ユーザーに再現タイミング（リロード有無・エラー表示有無）を確認したところ「解決した」との回答で、コード変更をしないままクローズとなった
- 広場招待リンクの参加申請不具合はExploreエージェントの調査で、`GroupJoinRequest`のコメントに明記された「却下後の再申請は同じドキュメントを更新する」という設計意図と、`firestore.rules`側の`update`許可（長・モデレーターのみ）が食い違っていたと判明。初回の申請自体は問題なく通るため見過ごされやすい不具合だった
- 招待リンクを新しいタブで開いても何も起きない件は、当初`JoinGroupScreen`側の例外処理不備を疑い実際にそれも直したが、根本原因は別にあった。`usePathUrlStrategy()`が呼ばれておらずFlutter Webが既定のハッシュURL方式のままだったため、ハッシュ無しの招待リンクを新しいタブで直接開くとgo_routerがURLを認識できず常にホーム画面にフォールバックしていた。ユーザーからの「新しいタブで語らいが開かれるだけ」という追加報告で気付けた

### 次にやること
- iOS/Android/Linux実機でのスピーカー切替・マイク・カメラ切替・着信音の再検証（引き続き未実施）
- 着信音の本番音源ができ次第、`assets/sounds/call_ringtone.mp3`を差し替え
- 招待リンクのパスセグメント方式キャッシュ回避・GIFアイコン保存修正・YouTubeプレビュー修正について、ユーザーによる実機・実際のDiscord投稿での最終確認
- 工房プロフィールカードの重複修正後、ユーザーが実際に工房タブを開いて残留している重複カードを手動削除する必要がある（コード修正だけでは既存の重複データは消えない）
- 通話機能の5件（カメラ切替・カメラオンオフ復旧・メイン/コーナー入れ替え・スピーカーアイコン・広場通話の同一空間化）の実機・複数人での再検証
- 広場招待リンクの参加申請・パスベースURL方式への切り替えについて、実際のリンクでの再確認
- 絶縁機能について、実際に2アカウントでDMを開き「提案→バナー表示→同意→データが完全に削除される」までの一連の動作確認（未実施）
- 「モデレーター」表示文言の変更（`lib/features/chat/group_member_list_screen.dart`）はユーザーの判断でpushを保留中。次回、pushするか破棄するかの方針を確認する

## 2026-07-25

### 実装内容
- 設定＞アカウント＞プロフィール名（未実装のプレースホルダー行）を削除
- モバイルのスワイプナビゲーションを刷新: ホーム画面のタブ切り替え用スワイプジェスチャーを廃止し、`SwipeBackDetector`/`SwipeDownToDismiss`（`lib/widgets/swipe_gestures.dart`新設）を追加。設定・身だしなみの狭い画面ドリルダウン、語らい詳細等のpushed画面に右スワイプで戻る操作を実装し、トークタブの追加メニューポップアップに下スワイプで閉じる操作を追加。運営タブも設定・身だしなみと同じサイドバー＋ドリルダウン構成に統一
- 語らい（DM・広場）のメッセージバブルに送信時刻を表示する機能を追加。設定＞アプリケーションで24時間表記（00:00）／12時間表記（12:00 p.m.）を切り替え可能にした（`MessageTimeFormat`・`message_time_format_provider.dart`・`utils/message_time.dart`新設）
- 身だしなみ（蔵）にSNSのURL欄を追加（最大5件登録、工房のプロフィールカードに最大2件まで掲載可能）。カードのURL選択は他の素材選択と同じ`showMenu`ベースのポップアップ＋チェックボックス方式に統一（当初AlertDialogで実装したが、ユーザー要望で他ピッカーと揃える形に変更）
- ユーザー報告のバグ3件を修正: ①カードを閉じる時の「BOTTOM OVERFLOWED」エラー（`_WorkshopCardSlot`側のHero子要素が旧来の`ClipRect`のままで、`_CardZoomEditor`側だけ`FittedBox`化されていなかったのが残存原因。両方`FittedBox(scaleDown)`に統一） ②背景画像を一度設定すると以降タップしても反応しなくなる不具合（装飾用グラデーションの`DecoratedBox`がヒットテストを奪っていたのが原因。`IgnorePointer`で解消、ウィジェットテストで再現・回帰テスト追加） ③カード名の文字色が白で視認性が悪い（黒固定に変更）、SNS URLの選択肢表示から`https://www.`を除去、URL一覧を横並び（" / "区切り）から縦積み表示に変更
- 工房カードの重複作成バグ（ユーザーがリロードしても消えない実データの重複として報告）の真因を特定・修正: カードの更新保存は「古い値をarrayRemove→新しい値をarrayUnion」という2手順で、この2手順セット自体がFirestore側で原子的にまとまっていなかったため、URLのチェックボックスを連続でチェックする等で保存が重なると後発の`arrayRemove`が空振りし中間状態と最終状態の両方が配列に残ってしまっていた。`_CardZoomEditorState`内で保存呼び出しをFutureチェーンで直列化して解消し、`_FakeUserRepository`をFirestoreの実際の値比較（Dartの参照同一性ではなく深い値比較）に合わせて修正した上で、競合を確定的に再現する回帰テストを追加
- 工房の縮小表示（ズームアウト状態）のカードにもSNSのURLを表示するよう追加。カード名の色がHero飛行の瞬間だけ黒でなくなる不具合も、縮小表示側のテキストに色指定が無かったのが原因と特定し黒固定に修正
- 縁結び・広場招待リンクのOGP画像を、アイコン単体ではなく個人の工房カード／広場のプロフィールカードと同じ見た目でそのまま画像化する機能を追加: `UserInvitePreview`/`GroupInvitePreview`に背景画像URL・ステメ・SNS URL等のフィールドを追加し同期処理を拡張。Vercelの新Edge Function（`api/og-image.mjs`、`@vercel/og`使用、日本語表示のためNoto Sans JPをGoogle Fontsから実行時取得）でプロフィールカードの見た目（背景＋グラデーション＋アイコン＋太字名前＋説明＋URL）を1枚の画像として合成し、`api/link-preview.js`のog:imageがこれを指すよう変更
- 広場に背景画像を設定できるようにし（`GroupProfileCard.backgroundImageUrl`新設、`groupBackgrounds/{groupId}`ストレージパス追加）、広場のプロフィールカード編集画面（`group_profile_card_screen.dart`）を個人の工房カードと同じデザイン（背景＋グラデーション＋アイコン＋太字名前＋説明の縦積み）に全面刷新
- チャット画面（`chat_screen.dart`）のメッセージ表示をLINE風に刷新: 一対（DM）・広場ともに相手のアイコン＋呼び名を吹き出しの隣に表示し、送信時刻は呼び名の横に配置。日にちの区切りを画面中央に丸ピル型で表示（「昨日」等の相対表記ではなく常に`yyyy/mm/dd`絶対表記、その日最初のメッセージの直前にのみ挿入）。`ListView.builder(reverse: true)`向けに、日付区切りを挿入した配列を古い→新しい順で組み立ててから反転する方式にした（`formatMessageDate`/`isSameDay`を`utils/message_time.dart`に追加）
- 広場のハンバーガーメニューまわりを刷新: information（実装状況一覧）ボタンを削除し`plaza_detail_dialog.dart`ごと削除。ハンバーガーメニュー自体はボタン真下（`PopupMenuPosition.under`）に角丸で開くよう変更。メンバー一覧・プロフィールカードは全画面遷移をやめ、`GlobalKey`+`RenderBox.localToGlobal`でボタン位置を取得した上で`showGeneralDialog`によるポップアップ化（メンバー一覧はボタン左隣、プロフィールカードは画面中央）。メンバー一覧はメンバー本体を先・招待中（承認待ち）を最下部に順序変更。広場のプロフィールカードの「説明」ラベルは「一言」に変更
- メッセージ入力欄を角丸にし、送信ボタンをテキスト入力時のみ・かつ物理キーボード未接続時のみ表示するよう変更（`HardwareKeyboard.instance`のグローバルハンドラで物理キー入力を検知）
- 工房カードのモバイル表示改善: 3枚横並びだと最小幅を下回るモバイル幅で右端が画面外に切れていたのを、縦1列表示に切り替えて画面幅いっぱいまで大きく表示するよう修正。カード内の文字・アイコンの比率もズーム編集画面と完全に同じ計算式に統一
- 広場のプロフィールカードをクリック編集式・自動保存に刷新: 「背景をタップして変更」の案内文を削除、カード下部の余白を拡大、名前・一言をカード内で直接タップすると編集用の小さなポップアップが出る方式に変更（個人カードの操作感に統一）、変更の度に自動保存、保存ボタン・×ボタンを削除（外側タップで閉じる）。さらに「カードの名前」を実際の広場名（`Group.name`）と同一視するよう変更し、カード編集画面から名前を変更すると実際の広場名にも反映されるようにした（`firestore.rules`のgroups更新許可を拡張、`GroupRepository.updateProfileCard`がnameも同時書き込み）
- 語らいの日付区切りに曜日を追加（日本語は1文字略称「(土)」、英語は3文字略称「(Sat)」）
- 身だしなみ/設定/運営タブのサイドバー幅・上余白のずれ（身だしなみだけ220px/16pxで他は240px/24px）を統一
- ホーム画面のタブ切り替えスワイプを再実装（ポップアップ表示中は反応しない`ModalRoute.isCurrent`ガード、Endイベント速度のみで判定）。さらに身だしなみ/設定/運営タブの狭い画面ドリルダウン中に、隣接カテゴリへ左右スワイプで切り替えられる機能を追加（`SwipeBackDetector`に`onPrevious`/`onNext`を追加、右スワイプは前カテゴリ→先頭なら一覧へ戻る、左スワイプは次カテゴリ）
- 招待リンクのOGP画像生成（`api/og-image.mjs`）が実機で3段階に壊れていたのを特定・修正: ①画像保存形式が全てWebPなのに`@vercel/og`内部のSatoriレンダラーがWebPデコードに未対応で空画像になっていた（`sharp`でPNGのdata URIに変換、Edge→Node.js Runtimeへ変更） ②Node.js Runtimeでは`request.url`が絶対URLでなくパス+クエリのみになり`new URL()`が例外（ダミーbase指定で解消） ③Node.js Runtimeで`export default`は`(req,res)=>void`専用シグネチャのため`Response`の戻り値が無視され300秒のハードタイムアウトまでハング（`export function GET(request)`という名前付きexportに変更）。あわせて`ImageResponse`の既定Cache-Control（1年間キャッシュ）がカード編集後も古い画像を返し続ける原因になっていたため、5分に短縮
- Storage rules（`groupIcons`/`groupBackgrounds`）が前回セッションで書いただけで本番未デプロイだったことが判明し、`firebase deploy --only storage`を実行して反映（広場のプロフィールカードのアイコン・背景アップロードが`unauthorized`で失敗していた）。同様に`firestore.rules`（`groups`のprofileCard更新許可）も未デプロイだったため`firebase deploy --only firestore:rules`を実行
- 語らいの一対（DM）にもハンバーガーメニューを新設（広場と同じ見た目・ボタン直下に角丸展開）。中身は通知オフ・ブロック。ブロックは`users/{userId}/blockedUsers/{blockedUserId}`という個人専用リストで管理し（`BlockRepository`新設）、ブロック中は相手のメッセージを非表示・自分からの送信も禁止（クライアント側の抑制、サーバー側の送信拒否ではない）。広場のハンバーガーメニューにも「通知オフ」を追加（既存の`ConversationPrefsRepository`を流用）
- 自分が送信したメッセージのアイコン・時刻を、相手のメッセージと同じ配置（アイコン＋時刻の見出し行、自分は右寄せ・アイコンは吹き出しの右側）で表示するよう変更
- 既読機能を実装: `Message.readBy`（`MessageReadReceipt`のリスト、Firestoreの技術仕様書通り`{userId, readAt}`形式）を追加し、語らいを開いている間に自分が未読のメッセージを自動で既読化。誰か（送信者以外）が既読にしたメッセージは吹き出しの角にチェックマーク＋既読人数（例:「✓4」）を表示し（自分のメッセージは左下、相手のメッセージは右下）、タップすると既読者（自分を含む）のアイコン・呼び名一覧がチェックマーク直下から伸びる形でポップアップ表示される（画面を暗転させないよう`barrierColor`は透明化、「完了」ボタンは無し）。既読機能の有無は一対・広場のハンバーガーメニューから会話ごとに個人設定として切り替え可能（`ConversationPrefs.readReceiptsEnabled`、既定true）。DM・広場それぞれのメッセージ`readBy`更新を許可する`firestore.rules`を追加しデプロイ
- モバイルで改行できなくなる不具合を修正: Androidのソフトウェアキーボードは`textInputAction.newline`指定時、改行キー押下で専用エディタアクションではなく生のEnterキーイベントを送出する仕様があり、これが「キー入力を受け取ったらハードウェアキーボード接続済みとみなす」という既存の判定ロジックに誤検知され、物理キーボード未接続のスマホでも送信ボタンが消え、かつEnterキー押下が独自の送信判定に横取りされて改行が一切できなくなっていた。Enter/NumpadEnterキーをハードウェアキーボード判定の材料から除外し、判定ロジック自体もハードウェアキーボード未検出時は一切介入しないよう修正
- 「語らいのUI」設定を追加: 設定＞アプリケーションから、(1)自分は右・相手は左でアイコンを最小限にする表示（自分のアイコンは非表示、一対では相手のアイコン・呼び名も非表示。広場では引き続き表示）と、(2)自他ともに左寄せでアイコン・呼び名を常に表示する表示、の2スタイルを切り替え可能にした（`ChatLayoutStyle`、端末のSharedPreferencesに保存する個人設定で相手の見え方には影響しない）
- 既読機能で「既読が全く積み上がらない（誰が送ったどのメッセージも既読1のまま）」という不具合を修正: 既読を付けるFirestoreへの書き込みが`await`もエラーハンドリングも無い投げっぱなし処理になっており、失敗時（通信不安定・タブを閉じる等）でも重複防止用の`_markedReadIds`に処理済みとして永久登録されてしまい二度とリトライされない実装になっていたのが原因。書き込みを`await`しエラー時は再試行可能なようリストから外すよう修正。あわせて、将来同一Widgetインスタンスが別ユーザーに使い回された場合の潜在バグとして、`currentUserId`変更時に重複防止セットをリセットする処理も追加

### 詰まったところ
- 背景画像タップ不能バグの原因特定に時間がかかった。Hero遷移やジェスチャー競合など複数の仮説を検証してはいずれも外れ、最終的に`WidgetsBinding.instance.renderViews.first.hitTest()`で実際のヒットテスト結果を直接確認する方法で、装飾用`DecoratedBox`が背後のタップを奪っていることを突き止めた
- 工房カード重複作成バグは当初「ダブルタップによる多重ダイアログ表示」と誤診断し、再入防止ガードを追加したが実際には直らず（ユーザー報告では2枚→3枚に悪化）。ユーザーへの追加ヒアリング（「勝手に3つ作られる」「リロードしても再現する」）でリロード後も再現する＝本物のサーバー側データ重複だと判明し、Firestoreのarray操作の競合レースという真因にたどり着いた。さらに再現テスト作成時、フェイクリポジトリの`mapEquals`がList値を参照同一性で比較してしまい実際のFirestoreの挙動と異なる誤判定をしていたことにも気づき、深い値比較に修正した
- OG画像生成（`api/og-image.mjs`）は前回セッションで「未検証」のまま実装していたが、今回実際にデプロイしてVercel実行時ログ（`vercel logs`、CLIログイン含めて実施）を直接確認しながらデバッグする形で、WebP非対応・URL解析・Response無視という3段階のバグを順に特定・修正した。ローカルで検証できないEdge/Node.js Functionsの挙動差は、結局本番ログを見るのが一番早いと分かった
- 「メニューをスライドしても切り替わらない」という報告が何を指すか曖昧で、確認ツールが一度応答不能になったため最有力候補（ホームタブのスワイプ切り替え）で先に実装したが、後でユーザーに確認したところ実際は身だしなみ/設定/運営タブの狭い画面カテゴリ切り替えのことだった。プランモードで改めて設計し直して対応
- 広場のプロフィールカードの「背景と一言を設定しても保存されない」報告は、コード自体のバグではなく前回セッションで書いた`storage.rules`/`firestore.rules`がそもそも本番デプロイされていなかったことが原因だった。ルールをコードに書いただけではデプロイ済みという思い込みをせず、実機で本当に反映されているか確認する必要があると再認識した
- 既読機能について、ユーザーから「自分が相手を既読したかを判定する機能ではない」という指摘を受け設計意図を再確認した。当初の実装は既に「送信者以外の既読者全員（自分・相手問わず）」という設計になっていたが、5人の広場での実機テストで全メッセージが「既読1」のまま増えないという具体的な不具合報告を受け、Exploreサブエージェントに調査を依頼。シードスクリプトの類は無く実際に複数アカウントでアプリを操作したデータと確認した上で、既読書き込みが投げっぱなしで失敗時に永久にリトライされないコード上の欠陥を特定した。ユーザーへの初期の質問（設計の確認か具体的な不具合か）で「具体的な不具合がある」との回答を得て初めて、画像から読み取れる送信順の矛盾という具体的根拠を引き出せた
- 「メニューをスライドしても切り替わらない」の件と同様、曖昧な不具合報告に対してAskUserQuestionで早めに具体化を促すことで、無駄な仮説実装を避けられた

### 次にやること
- 今回のセッションで変更したコード一式をgit push（本エントリと同時に実施）
- 既読の書き込み失敗の根本原因である「タブを閉じるタイミングでネットワーク到達前に書き込みが失われる」こと自体は今回の修正では解決していない（次回開いた際にリトライされるようにしただけ）。実機でネットワーク到達を待たずに離脱するケースが多いようなら、ページ離脱時の同期送信（`sendBeacon`相当）等のさらなる対策を検討する余地がある
- 工房カード重複バグの修正は今後の重複発生を防ぐのみで、既にFirestore上に残っている重複カードのクリーンアップはできていない（該当ユーザーに手動削除してもらう必要あり）
- ブロック機能は現状「自分が送信できない・相手のメッセージが見えない」というクライアント側の抑制のみで、相手側から見た表示（自分がブロックされていることが相手に分かるか等）は未検討・未実装

## 2026-07-24

### 実装内容
- ニックネーム・ステメが登録できない不具合の真因を特定・修正: `Random().nextInt(1 << 32)`がDart VM（`flutter test`）では`4294967296`を返すのに対し、Web（dart2js）ではビット演算が32bit符号付き整数に切り詰められ`1 << 32`が`0`になり`RangeError`が発生していた（`dart compile js`＋`node`で実際に検証して特定）。`profile_tab.dart`のID生成をリテラル`4294967296`を使う形に修正して解消
- ニックネーム・ステメの登録行にごみ箱アイコンを文字に近づけたレイアウトへ変更し、編集ボタンを追加（追加済みの内容を後から直せなかった不足を解消）
- 工房（プロフィールカード作成画面）のカードを画面いっぱいに拡大表示。背景画像を`Stack(fit: StackFit.expand)`でカード全面に敷く構造に変更
- アクセントカラーのプリセット3種（`F08300CC`/`3D2EE0CC`/`88B04Bdd`）を設定＞アプリケーション＞色に追加
- `docs/マップ.md`のサイトマップ通りにアプリ全体をマッピング: 未実装機能はすべて「準備中」ページとして配置し、広場の詳細ダイアログ（`plaza_detail_dialog.dart`）で表広場/裏広場・表組/裏組・寄合/密談・談論の実装状況を可視化。バックアップコード・メールアドレスの設定項目は、CLAUDE.mdの既存不採用方針に基づきユーザー確認の上で追加しない判断をした
- 用語切り替え機能を実装: `docs/マップ.md`の対訳表（世界観重視／利便性重視 × 日本語／English）をそのまま`Vocabulary`クラス（`lib/l10n/vocabulary.dart`）として実装し、表示言語とは独立した軸`TerminologyStyle`をSharedPreferencesに永続化。設定＞アプリケーション＞言語から切り替え可能にし、蔵/工房・一対/広場等の呼称に反映
- 運営タブを`docs/マップ.md`通り3項目（ホームページのURL／お知らせ／質問フォーム）に再構成。正式なホームページURLが未確定のため、実在しないURLを決め打ちで埋め込まず、いずれも準備中ダイアログにした
- ホーム画面のメニューアイコン（語らい/身だしなみ/設定/運営の丸いチップ）のホバー時ツールチップ表示を削除
- 身だしなみタブの蔵/工房トグル直下に残っていた、選択中ステメのプレビュー表示（重複表示になっていた）を削除
- 設定タブを全面刷新し、macOS Finderのカラム表示のようなマルチカラムUIに変更: 階層を選ぶたびに右へ新しい列（サイドバー）が追加され、末端項目を選ぶと一番右の内容ペインに表示する構成にした。ルートフォルダ（アカウント/アプリケーション/入力/通知）とサブ項目を1つの`_Node`木構造・汎用コンポーネントに統一。列の合計幅が画面に収まらない場合はサイドバー部分のみ横スクロールし、内容ペインの幅は常に確保する
- 蔵/工房の切り替えボタンで、用語や画面幅によってはラベルの後半（「工房」の「房」など）が見えなくなる不具合を恒久修正: `softWrap: false`だけでは幅不足時に文字が黙ってクリップされるだけだったため、`FittedBox(fit: BoxFit.scaleDown)`で包み、入り切らない場合は縮小して必ず全体を表示するようにした
- 市場調査用のカスタムサブエージェント`market-research`（`.claude/agents/market-research.md`）を新設。競合調査の結果を蓄積する`競合調査.md`をリポジトリ直下に新設し、調査のたびに先頭へ追記する運用にした。CLAUDE.mdに「プロダクトの方向性に関わる指示を受けたときは競合調査を踏まえて差別化提案を添える」運用方針を追記
- 上記エージェントを使い「LINE以外の国内メッセージアプリの動向」を調査（+メッセージ、Discord、Yay!、Chatwork等）し、`競合調査.md`に結果を記録
- CLAUDE.mdに「コード編集が終わる度に確認なしでVivaldiで動作確認環境を立ち上げる／再起動指示には即座に応じる」運用ルールを追記
- 工房のプロフィールカード編集画面を全面刷新: カードをタップするとHeroアニメーションでカードが画面中央にズームインし、アイコン・背景画像・ニックネーム・ステメの各要素をタップすると蔵の登録済み素材一覧（同じ種類のみに絞ったポップアップ、`showMenu`ベース）が出て選択即差し替えできる直接編集UIに変更（`_CardZoomEditor`。旧・ダイアログ形式の`_ProfileCardEditorDialog`を置き換え）
- プロフィールカードの表示内容を整理: カード名（管理用ラベル）をカード内から外してカード下に表示し、カード内はニックネームを強調・その下にステメを表示する構成に変更
- 蔵/工房切り替えボタンの文字サイズ・幅を固定化: 環境（用語スタイル・画面幅）依存で縮小される`FittedBox(scaleDown)`をやめ、固定フォントサイズ＋固定幅（当初100px→「マテリアルボックス」等の長い用語が省略されたため140pxに拡大）の`_WorkshopToggleLabel`に変更
- 設定タブで表示言語を切り替えると強制的に設定のトップ階層まで戻されてしまう不具合を修正: ナビゲーションのパス追跡（`_SettingsTabState._path`等）が翻訳済みタイトル文字列に依存していたのが原因。全ての`_Node`に翻訳非依存の安定した`id`を追加し、パス保持・選択状態の判定をidベースに切り替えた
- 工房のカード名Heroモーション: ズームイン時にカード名がその場に残る問題を修正し、カードとカード名を1つのHeroとして一緒に移動するモーションに変更。カード名のテキストボックスも編集ボタン不要で最初から常時表示に変更
- ニックネーム・ステメの「使うものを選ぶ」ラジオ選択機能を廃止（表示に使う1件は登録順で自動決定）。あわせて用語を日本語のみ再編: 利便性重視でステメ→ステータスメッセージ、世界観重視でステメ→一言・ニックネーム→呼び名に変更。`Vocabulary`クラスに`nickname`/`statusMessage`フィールドを追加して対応
- 蔵の素材最大登録数を変更: アイコン5・背景3・呼び名10・一言10（`kMaxIcons`等の定数を更新）
- 設定タブをDiscordのスクショを参考に全面刷新: 前日実装したFinder風マルチカラムのドリルダウンをやめ、サイドバーは最上位カテゴリ（アカウント/アプリケーション/入力/通知）のみに留め、残りは各カテゴリ1ページ内にまとめてスクロールで見せる構成に変更
- 設定の内容ペインが画面幅いっぱいに広がる不具合を修正: `ConstrainedBox(maxWidth: 640)`だけでは`Expanded`由来のtight制約下で無効化される（`BoxConstraints.enforce`の仕様）ため、`Align`で緩い制約に変換してから`ConstrainedBox`を適用する形に修正
- カードズームイン時に一瞬「No Material widget found」が出る不具合を修正: Hero飛行中に子ウィジェット（TextField）がOverlayへ一時的に再親化されMaterial祖先を失うことが原因。Hero子要素を直接`Material(type: transparency)`で包んで解消
- 作成済みカードを閉じる時のみ「BOTTOM OVERFLOWED」警告が出る不具合を修正: Flutter SDKの`MaterialRectArcTween`が矩形の対角2点を独立して円弧補間するため、Hero飛行中に一時的に両端点よりも小さいサイズを取りうることが原因と特定。Hero子要素のColumnを`ClipRect`で包む対応で解消
- モバイルのスワイプナビゲーションを修正: スワイプ判定領域がホーム下部の小さいナビチップ帯に限定されていたのを、タブコンテンツ全体に拡張
- 1対1のビデオ通話を実装: `Call.isVideo`フラグを追加、`WebrtcCallController`でカメラ映像取得（720p ideal）・ローカルプレビュー（`RTCVideoRenderer`）・カメラON/OFF切り替えを実装。`CallScreen`に相手映像全画面＋自分プレビューのビデオ専用レイアウトを追加。チャット画面AppBarにビデオ通話ボタンを追加し、Android/iOS/macOSにカメラ権限設定を追加。グループ通話はメッシュP2Pの人数上限・TURN未導入という制約があり、ユーザー確認の上で今回は見送り、1対1のみ先行実装
- 身だしなみタブにサイドバー構造を追加し、蔵/工房をそこへ移動。新たに「縁結び」項目を新設し、招待リンク（`/invite/:rhingId`）とQRコード（`qr_flutter`/`mobile_scanner`）によるフレンド追加を実装。未ログイン状態で招待リンクを開いてもサインイン→Rhing ID設定→招待確認画面まで導線を保てるよう、`AppGate`から`AuthGate`（`lib/features/auth/auth_gate.dart`）を共通部品として切り出した
- 工房のカード整列不具合を修正（ユーザーがスクショで報告）: 作成済みカード（カード＋名前でその分背が高い）と未作成カードで高さが異なるのに、並べる`Row`が`CrossAxisAlignment.center`のままだったため、カード自体の縦位置がずれていた。`CrossAxisAlignment.start`に変更して解消
- 広場（グループ）にカスタマイズ機能を追加: 通話ボタン右のハンバーガーメニューから「プロフィールカード（1枚のみ・メンバー全員編集可）」「メンバー一覧（承認待ちの参加リクエスト表示・長/モデレーターが承認/却下）」「招待リンク作成（QRコード付き）」「退会（長は不可）」を実装。招待リンク経由の参加は即時参加ではなく長/モデレーターの承認制（`GroupJoinRequest`）にした。招待リンクをコピーして他SNSに送った際もカード内容がプレビュー表示されるよう、Vercelサーバーレス関数（`api/link-preview.js`）で実URLのHTMLに`og:title`等のメタタグを動的注入する仕組みを追加し、同じ仕組みを既存の縁結び（個人）招待リンクにも適用した（`userInvites`/`groupInvites`という公開読み取り専用のFirestoreコレクションを新設）
- グループ通話（広場通話）を実装: メッシュ型P2P・参加上限5人（`kMaxGroupCallParticipants`）。`GroupCall`/`CallParticipant`/`CallPeerLink`モデル、`GroupCallRepository`、複数の`RTCPeerConnection`を管理する`WebrtcGroupCallController`（辞書順で小さいuserIdが常にオファー役という決定則でglareを回避、15秒間隔のプレゼンスハートビートで在室検知）、グリッド/アバター表示の`GroupCallScreen`を新設。1対1通話のコード（`Call`/`CallRepository`/`WebrtcCallController`/`CallScreen`）は変更せず並行実装し、共通の丸型コントロールボタンのみ`call_controls.dart`に切り出して共用。TURNサーバーは自前coturn運用の方針に決定し、`String.fromEnvironment`経由の設定読み込みフックはコード側に用意したが、VM構築等のインフラ自体はまだ先の作業として`実装内容.md`に明記した
- 保留していた`firestore.rules`/`storage.rules`の本番反映をユーザーに実行してもらい完了（`firebase deploy --only firestore:rules,storage`）
- 縁結びの招待リンクを送っても相手にプロフィールカードが表示されない不具合（ユーザー報告）を調査・修正: FirestoreへのCurl直叩きで`userInvites/{rhingId}`が403（ルール未反映）→ルールデプロイ後は404（ドキュメント自体が存在しない）と切り分け、この機能追加より前から使っているユーザーは`nicknames`/`icons`更新時にしか走らない`_syncInvitePreview`が一度も実行されていないと特定。`UserRepository`に公開メソッド`syncInvitePreview`を追加し、縁結びページ（`EnmusubiPage`、`ConsumerStatefulWidget`化）を開くたびに呼んでバックフィルする形に修正
- （別チャットで対応）広場作成時に`permission-denied`になる不具合を修正: `GroupRepository.createGroup`が`groups`/`rooms`/`groupInvites`の3ドキュメントを同一`WriteBatch`でコミットしており、`groupInvites`書き込みルールの`get()`が同バッチ内未コミットの`groups`ドキュメントを「存在しない」ものとして評価していたのが原因
- （別チャットで対応）広場作成画面のメンバー選択をRhing ID手打ちから友達一覧のAutocompleteに変更（呼び名優先表示・選択済み除外・友達0人時の案内表示）
- （別チャットで対応）`AppUser.activeProfileCardId`を追加し、工房のカードに適用中バッジ（◉/○）を新設。招待プレビュー（`syncInvitePreview`）は適用中カードがあればそのカードが指すニックネーム・アイコンを優先するよう変更。カード削除時、それが適用中だった場合は適用状態を解除する処理も追加
- 一対・広場一覧でアイコンが表示されない不具合（ユーザー報告）を修正: 一対・広場一覧はもともとアイコン表示自体が未実装で固定プレースホルダーのままだったこと、広場のメンバー一覧は`activeIcon`しか見ておらず工房のプロフィールカード経由でアイコンを設定している人には反映されないことを確認。`AppUser`に`effectiveIcon`/`effectiveNickname`（適用中プロフィールカードがあればそちらを優先するgetter）を新設し、`syncInvitePreview`内に重複していた同種のロジックもこちらに統一。一対リスト・広場リスト・広場メンバー一覧すべてをこのgetter経由の表示に変更し、メンバー一覧はユーザー要望通り`@rhingId`ではなく呼び名（未設定時は`@rhingId`にフォールバック）表示に変更

### 詰まったところ
- ニックネーム/ステメ登録不具合は、画像アップロードは成功するのに文字だけ失敗するという非対称な症状から原因の当たりがつけにくく、`dart compile js`でコンパイルして`node`で実行し`1 << 32`の挙動を実際に検証することで初めてVM/Web差異と特定できた
- 設定タブのマルチカラム化で、列の間に挟む`VerticalDivider`（幅1px）の分を横幅計算に含め忘れ、`RenderFlex`が列数×1pxだけオーバーフローするバグを作り込んだ。ウィジェットテストの実行で検出し修正
- 新設した`market-research`サブエージェントは、`.claude/agents/`にファイルを作成した直後の同一セッション内では認識されず（利用可能なエージェント一覧はセッション開始時点のもの）、その場はメインエージェントが直接WebSearch/WebFetchで代替調査した。次回セッション以降は反映される見込み
- 設定タブの言語切り替えナビゲーションバグは、症状（設定トップに戻される）だけでは原因箇所の見当がつきにくく、パス追跡ロジックが翻訳済み`title`をキーにしていたという設計上の見落としだと気づくまで、コードを読み込んで初めて特定できた
- カード名とHeroの組み合わせで、子要素にMaterial/ClipRectの層を追加するたびに括弧の対応がずれるミスを繰り返した。`flutter analyze`で都度検出し修正
- 設定内容ペインの横幅制限が効かない原因は、`ConstrainedBox`単体では`enforce()`がtight制約下で小さいmaxWidthを上書きしてしまう仕様によるもので、`Align`で緩い制約に変換する必要があると気づくまで原因調査に時間がかかった
- カードを閉じる時だけ発生するオーバーフロー警告は、Hero飛行のfrom/toどちら向きかという勘違いから複数の誤った仮説を経て、最終的にFlutter SDK本体（`heroes.dart`/`arc.dart`）を読んで`MaterialRectArcTween`の角ごと独立補間という仕様に行き着くまで特定できなかった
- `firebase deploy --only firestore:rules,storage:rules`をBashで直接実行しようとしたところ、「Claude Code auto modeの分類器」に本番インフラ変更として拒否された。バイパスはせず、ユーザーに状況を説明し許可または手動実行を依頼する対応にとどめた
- `_RoundButton`を`CallRoundButton`に公開クラス化して切り出す際、`replace_all`付きの一括置換が2箇所を取りこぼし、`flutter analyze`の「メソッド未定義」で発覚してから個別修正した
- 現行バージョンのriverpodには`AsyncValue.valueOrNull`が存在せず、`flutter analyze`で指摘されてから既存コードと同じ`.value`（nullable getter）に修正した
- `firebase deploy --only storage:rules`が「Could not find rules for the following storage targets: rules」で失敗。`storage`には`firestore:rules`のような`:rules`サブセレクタが存在せず、"rules"という名前のデプロイターゲットを探しにいってしまうのが原因と特定し、`--only storage`（ターゲット指定なし）に変更して解消
- 縁結びのプロフィールカード不具合は、Vercel側のOGP関数自体は正常に動いているのにプレビューが既定値のままという紛らわしい症状で、FirestoreへのREST API直叩き（`curl`）で403→404の切り分けをしなければ原因（ルール未デプロイ→デプロイ後はバックフィル未実施）に辿り着けなかった
- 動作確認のためVivaldiを起動しようとしたところ`vivaldi`コマンドがPATHに存在せず失敗。Flatpakアプリ（`com.vivaldi.Vivaldi`）としてインストールされていると特定し`flatpak run com.vivaldi.Vivaldi`で起動。Flatpakのサンドボックス内プロセスは通常の`ps aux`からは見えず、`flatpak ps`で確認する必要があった

### 次にやること
- 新設した`market-research`サブエージェントが次回セッションで実際に呼び出せるか確認する
- 表示言語切り替えの全画面対応（現状はナビ・語らい・設定・プロフィール等の主要文言のみ）
- 通知タイミング・再通知間隔の設定（FCM通知基盤自体が未実装のため後続タスク）
- 実装したビデオ通話の実機・複数ブラウザでの実通話動作確認（カメラ権限ダイアログ・実際のP2P接続）が未実施
- 自前coturn（TURNサーバー）のインフラ構築（VM/DNS/TLS証明書）。Firebase以外の環境構築のため後回しと決定し、現時点では未着手
- Vercelサーバーレス関数（`api/link-preview.js`）によるOGPリンクプレビューの実機検証（Discord/LINE/X等での実際のリンク展開確認、ローカルでは検証不可）。縁結びの`userInvites`バックフィル修正後、実際に正しいプレビューが表示されるか再確認が必要
- グループ通話の実機・複数人・複数ネットワークでの動作確認（NAT越え・入退室・満員時の挙動などプラン記載のQAチェックリスト一式）
- 一対・広場一覧・メンバー一覧のアイコン/呼び名表示修正について、Vivaldiでの見た目のユーザー確認待ち（本セッションでは`flutter analyze`/`flutter test`のみ確認済み）
- `profile_workshop_test.dart`に残る、旧ダイアログ形式（保存ボタン）を前提とした3件の失敗テストの整理（本セッションでは対象外として保留）

## 2026-07-23

### 実装内容
- Vercelへのデプロイがすべて「Blocked」になっていた問題を診断・解決: GitHubのVercelチェックが「Git author arag616 must have access to the project on Vercel」で失敗していた。原因はVercel Hobbyプランがprivateリポジトリでの複数コントリビューターでのデプロイを許可していないため。ローカルリポジトリのgit設定（`git config --local`、グローバル設定は変更せず）を`rhing-official <rhing.official@gmail.com>`に変更し、この名義で新しくcommit・pushして解消
- デプロイ成功後、本番URL（`dai-dai-phi.vercel.app`）でGoogleログインが`firebase_auth/unauthorized-domain`エラーになったため、Firebase ConsoleのAuthentication設定にこのVercelドメインを認証済みドメインとして追加する手順を案内
- モバイル端末で`daidai-phi.vercel.app`（ハイフンなし・実際には存在しないドメイン）にアクセスして404になっていた件は、単純なURLの打ち間違いと判明（バグではない）
- 語らいタブに「一対」「広場」それぞれの件数チップを追加。＋ボタンを右下のFABから「広場」タブの右横（ヘッダー行内）に移動
- 設定タブのアクセントカラー入力欄の「#」をテキストフィールドの`prefixText`に変更し、誤って消せないように修正
- ブラウザ・マウスの「戻る」「進む」ボタンがアプリ内の画面遷移と対応しない問題を修正: これまで`Navigator.push`のみでWeb上のブラウザ履歴に画面遷移が乗っておらず、「戻る」を押すとアプリ内ではなくサイト自体から離脱してしまっていた。`go_router`を導入し（`lib/router/app_router.dart`新設）、チャット画面・相手を追加・広場を作る・通話画面をそれぞれURL付きのルートに変更。画面間のデータ受け渡しは`extra`パラメータ経由（URLへの直接エンコードはせず、ブラウザの直接URL入力やリロードからの復元は現状未対応）。あわせて、一対作成直後の画面にだけ発信ボタンが付いていなかった既存の抜け漏れも、ルーター側でChatScreen組み立てロジックを一本化したことで解消
- CLAUDE.mdの「開発時の動作確認」節に、デバッグ中の変更ごとに確認を挟まずホットリロード（`r`）／ホットリスタート（`R`）を実行する運用と、こちらがバックグラウンドで`flutter run`を起動している場合はユーザーの端末からキー入力を送れないため`kill -SIGUSR1`（リロード）／`SIGUSR2`（リスタート）で代替する方法を追記
- リポジトリルートに`実装内容.md`を新設。実装済み/未実装をチェックボックスで管理し、バグで再修正が必要になった項目は着手時にチェックを外し直す運用ルールを明記
- 表示言語切り替え（日本語／British English）を実装。`flutter_localizations`を追加し、`lib/l10n/app_locale.dart`・`lib/l10n/strings.dart`・`lib/providers/app_locale_provider.dart`で対訳とSharedPreferences永続化を用意。設定タブをフォルダ階層UI（アカウント／デザイン／表示・言語／通知）に再構成し、その中に言語切り替えを設置
- モバイル下部ナビ上での横スワイプによるタブ切り替えを実装
- 語らいタブの＋ポップアップが「外側タップで閉じない」バグを特定・修正: `Center > Material(color: transparent) > Dialog`という二重ラップが原因で、外側の`Material`がタップを吸収していた。`showGeneralDialog`の構造を段階的に切り分けるFlutterウィジェットテストで再現・特定し、`Dialog`を`Center`の直接の子に戻して解消（`test/add_menu_dismiss_test.dart`に回帰テストを追加）
- 友達申請フローを実装（CLAUDE.md記載の「仲間承認制」スパム対策の実体化）: `FriendRequest`/`Friend`モデル・`FriendRepository`を新設し、Rhing ID検索→申請送信→相手が承認→一対で会話可能、という流れに。相互申請時は自動承認。Firestoreルールで一対（DM）の作成を「対応するfriendRequestsがaccepted済み」の場合に限定し、IDを知っているだけでは会話を開始できないようサーバー側でも強制
- 届いている／送った友達申請を、専用画面ではなく一対リストの最上部にインライン表示する形に変更（「相手から申請が届いています」「相手の承認を待っています」）。承認して会話が始まると通常の一対リストへ合流する。当初作った`FriendRequestsScreen`と専用アイコン・バッジは削除し、＋ボタンは元の位置（広場タブの右横）に戻した
- ニックネーム機能を実装し直し: 当初「友達ごとに相手を自分だけリネームする」方式（LINE風）で作ったが、ユーザーの意図は「自分の身だしなみタブでニックネームを最大3件登録し、その中から選んだものが友達に表示される」方式（アイコン・背景画像・ステメと同じ蔵システムのパターン）だったため作り直し。`AppUser`に`nicknames[]`/`activeNicknameId`を追加、`profile_tab.dart`にステメと同型の追加/選択/削除UIを追加、`UserRepository.watchUser()`で相手のアクティブニックネームをリアルタイム購読し、一対リスト・チャット画面タイトルでRhing IDの代わりに表示
- 一対・広場のピン留め・通知オフを実装。`users/{userId}/conversationPrefs/{conversationId}`に個人設定として保存（相手には非公開）し、右クリック（デスクトップ）／長押し（モバイル）のコンテキストメニューから切り替え。ピン留めした項目はリスト上部に固定
- 身だしなみタブでニックネーム・ステメを追加しても反映されない不具合を調査・修正: 直前にデプロイしたFirestoreルールが`nicknames`フィールドの存在を前提にしており、クライアントとルールのバージョンずれで無関係な更新まで巻き込まれるリスクがあったため、フィールド未存在時はチェックをスキップする防御的な書き方に修正。あわせて`flutter run`プロセスを完全に再起動して古いビルドの可能性を排除し、`profile_tab.dart`の保存処理に失敗時のスナックバー表示を追加（これまで保存失敗が無言だった）
- 身だしなみタブ上部のプレビューからアイコン（アバター）と`@RhingID`表示を削除（ニックネーム・ステメのみ表示）
- Discord風の左右分割ビュー（760px以上の画面）を実装: `talks_tab.dart`を書き換え、広い画面では一対・広場の一覧をタップしても画面遷移せず右隣に会話ペインが開く形に変更。`chat_panes.dart`を新設し`DmChatPane`/`GroupChatPane`としてChatScreen組み立てロジックを共通化（go_routerのフルスクリーン遷移・分割ビュー埋め込みの両方から使う）。＋ボタンを広場タブの右横にチップとして配置し直し、設定タブの横幅を480pxに制限して余白を調整
- 和合（プロフィールカード作成画面）を新設: `ProfileCard`モデル（アイコン・背景画像・ニックネーム・ステメのIDを参照する形。蔵の素材データを複製しない）と`profile_creator_screen.dart`を追加し、最大3枚までカードを作成・編集・削除できるUIを実装。身だしなみタブから和合への導線を追加。カードの実際の利用場面（プロフィールURL共有時の表示、友達バーのホバーポップアップ）は依存する仕組み自体が未実装のため、カード作成機能のみ先行実装
- メッセージ送信キーの切り替えを実装: 「Enterで送信・Shift+Enterで改行」／「Enterで改行・Ctrl+Enterで送信」を設定タブ（新設の入力フォルダ）から選択可能にした。`SendKeyMode`モデル・プロバイダーを新設し、`chat_screen.dart`の`Focus.onKeyEvent`でハードウェアキーイベントを判定・処理
- 相手に通知せず送信（サイレント送信）を実装: Enterで送信モードは`Ctrl+Enter`、Ctrl+Enterで送信モードは`Ctrl+Shift+Enter`、モバイルは送信ボタンの長押しで、それぞれ通知なし送信になる。`Message`に`silent`フラグを追加してFirestoreに保存し、送信済みメッセージには🔕アイコンを表示。実際の通知抑制はFCM基盤が未実装のため、将来の接続ポイントとして先行実装
- 送信キー・サイレント送信の全パターン（キー組み合わせ6通り＋長押し）を`test/chat_send_key_test.dart`で実際にキーイベント・タップ操作をシミュレートして検証
- 設定タブのアクセントカラー入力を最大8桁（`#RRGGBB`／`#RRGGBBAA`）まで受け付けるようにし、16進数以外の文字は入力できないよう制限。標準アクセントカラーを`#F08300`に変更
- 8桁カラーコード（`RRGGBBAA`）の透明度（末尾2桁）を`Color()`のARGB整数へ誤った桁位置のまま渡していたバグを修正（`f08300`と`f08300ff`が別の色になっていた）。`test/color_hex_test.dart`に回帰テストを追加
- Apple認証を実装（`AuthRepository.signInWithApple()`、Webは`signInWithPopup`、Android/Windows/Linuxは`signInWithProvider`の汎用OAuthフロー）。Firebase ConsoleでApple providerを設定する際、Apple Developer Program（有料、Services ID/Team ID/Key ID/秘密鍵の登録が必要）だと判明し、ユーザーの意向（「有料なら諦める」）でログイン画面のボタンをUIから非表示に変更。メソッド自体は残してあるので、将来登録すればボタンを戻すだけでよい
- 身だしなみタブを「蔵」（素材の登録・管理）と「工房」（旧・和合）にボタン（`SegmentedButton`）で切り替えられるよう再構成。工房は常に3枠を表示し、未使用の枠は「+」付きの白紙カードとして表示、どの枠をタップしても選択画面（作成・編集ダイアログ）が開く。独立画面だった旧`/profile-creator`ルートは廃止・統合
- 身だしなみタブ全体（蔵・工房双方）の表示文言を`Strings`経由にし、日本語／British Englishの切り替えに対応
- ニックネーム最大20字・ステメ最大40字の文字数上限は既に`TextField.maxLength`で実装済みだったと判明。ウィジェットテストで実際に上限を超えて入力できないことを確認
- 「蔵に素材を登録できない」の再調査で、ユーザーから具体的な症状（アイコンが真っ白、ニックネーム/ステメの追加・工房の保存ボタンを押しても反応がない）を確認できたことで、複数の実バグを特定・修正:
  - ニックネーム・ステメ・工房カードの必須項目が未入力のまま追加/保存を押すと、内部で無言でreturnするだけでダイアログが閉じず何も起きないように見えていた不具合を修正（エラーメッセージ表示に変更）
  - アイコン・背景画像のサムネイルが`DecorationImage`で、読み込み中・失敗時に何も表示されず「真っ白」に見えていたのを、読み込み中はスピナー・失敗時は壊れた画像アイコンを表示する`Image.network`ベースの表示に変更
  - Firebase Storageの中身を直接確認（`firebase login`済みのOAuthトークンでGCS JSON APIを叩いて調査）したところ、image_picker由来の未圧縮フルサイズ画像（3〜10MB超）がそのままアップロードされていたと判明。`flutter_image_compress`を導入し、アップロード時にWebPへ圧縮・リサイズ（アイコン512px/背景1080px）するよう修正。Windows/Linuxは同パッケージ未対応のためその場合のみ生JPEGにフォールバック
  - `ThemeData.fontFamily: 'monospace'`がCJK文字の字幅計算とレイアウトエンジンの推定字幅をズレさせ、「工房」など複数文字の日本語ラベルだけ折り返される不具合の原因になっていたため削除（プラットフォーム既定フォントへ戻した。CLAUDE.mdが意図する等幅フォントBIZ UDPMincho/BIZ UDGothicはまだアセット未取得）
- ログイン画面のGoogleボタンが画面幅いっぱいに広がっていた問題を修正。`ElevatedButtonThemeData.minimumSize`が`Size.fromHeight`（＝横幅無制限）を指定しており、外側に幅の制約がない画面ではそのまま画面幅まで広がっていたため、ログイン画面を最大400pxに制限
- 通話のノイズキャンセリングについて調査: flutter_webrtc/libwebrtcはマイク→エンコーダ間の生音声サンプルに介入するフックをDart層に公開しておらず、RNNoise本格統合にはプラットフォームごと（Web=AudioWorklet+WASM、Android/iOS/Windows/Linux/macOS=ネイティブAPMカスタムプロセッサ）のネイティブ音声パイプライン改造が必要な大規模な別プロジェクトだと判明。ユーザー確認の上、まずはgetUserMediaの標準ノイズ抑制（echoCancellation/noiseSuppression/autoGainControl）を有効化し、RNNoise本格対応はフェーズ3で別途着手する方針に決定

### 詰まったところ
- Vercelの「Blocked」ステータスが最初はビルド失敗（コード側の問題）に見えたが、実際はプラットフォーム側の権限制限だった。デプロイ詳細画面を開いてもらい「Deployment Blocked」の具体的な理由テキストを確認するまで特定できなかった
- git commitのauthorを変更しても、GitHub側でそのメールアドレスが対象アカウントの認証済みメールと一致していないと正しく紐付かない点に注意が必要だった
- Flutter Web＋素朴な`Navigator.push`だけではブラウザの戻る操作がアプリ内履歴に乗らないという設計上の制約に、ユーザーから「戻るとサイトへアクセスする前に戻される」という実際の症状を報告されるまで気づけなかった
- ＋ポップアップの外側タップ非表示バグは、`showGeneralDialog`の標準挙動を信じて「コード上は問題ないはず」と最初は誤診断した。ユーザーから2度目の報告を受けて実際にウィジェットテストで再現するまで、原因（余分な`Material`ラップ）に気づけなかった
- ニックネーム機能を最初「友達ごとの個別リネーム」として実装したが、ユーザーの追加指示（「身だしなみから変更できるように」「最大3つまで登録可能」）で「自分の蔵素材としてのニックネーム」だったと判明し、作り直しが発生した。要件の初期解釈がずれていた
- Firestoreルールに新フィールドの存在を前提とした検証を追加した際、クライアント側の反映漏れ（またはバージョンずれ）で無関係な既存機能（ステメ追加）まで巻き込んで失敗する可能性に気づかず、ユーザー報告で初めて対策した
- 送信ボタンの長押し検出を最初`GestureDetector(onLongPress:)`で`IconButton`をラップする形で実装したが、`IconButton`内部の`InkWell`がジェスチャーアリーナを先に獲得してしまい長押しが一切発火しない不具合があった。ウィジェットテストで送信0件という結果から気づき、`Material > InkWell`にonTap/onLongPress両方を直接指定する形に書き直して解消
- Enterキーのハンドリングで、送信しない場合に`KeyEventResult.ignored`を返しTextField既定の改行挿入に任せる実装を最初試したが、テストの生キーイベントシミュレーションでは改行が自動挿入されないことが判明。常に`handled`を返し`_insertNewline()`で手動挿入する形に修正
- カラーコードの8桁対応バグは、`RRGGBBAA`形式の整数をそのまま`Color(value)`に渡すとAARRGGBBとして誤読され、透明度のはずの末尾2桁が最上位（アルファ）バイトの位置とズレて全く別の色になるという、ユーザー報告で初めて気づいた不具合だった
- 「蔵に素材を登録できない」は今回で3回目の調査依頼だった。1・2回目はウィジェットテストでUIロジック自体は正常と確認できていたため、コードレビューだけで新たな原因を特定するのは困難だった。ユーザーから実際の症状（真っ白／ボタンが反応しない）を具体的に教えてもらえたことで初めて、フォント・画像サイズ・必須項目バリデーションという3つの独立した実バグにたどり着けた
- CJK文字の字幅ズレはFlutterのウィジェットテスト（テスト用フォントを使うため）では再現できず、コードとFirebase Storageの実データを直接調査する形でしか原因を特定できなかった

### 次にやること
- go_router移行後、実機・複数ブラウザでの戻る/進むボタンの動作確認がまだ未実施
- ブラウザの直接URL入力・リロードからの画面復元は現状未対応（`extra`でのデータ受け渡しに依存しているため）。分割ビューの選択状態もURLには載らない
- Vercel上でのPC・モバイル間の音声通話の実際の動作確認がまだ未実施
- CLAUDE.mdに残る3つの未解決の食い違い（談論/バックアップコード/メールアドレス）は引き続き未確認
- 身だしなみタブの蔵・工房まわり（フォント修正・WebP圧縮・バリデーション修正）が実際に直っているか、ユーザー側での再確認待ち
- 表示言語切り替えは現状ナビ・語らいタブ・設定タブ・身だしなみタブなど主要文言のみ対応。アプリ全体の完全な多言語化は未実施
- 友達申請の新着通知（現状は一対リストへのインライン表示のみ）・友達解除（アンフレンド）機能は未実装
- 通知オフ・サイレント送信は表示上のみで、実際の通知抑制はFCM通知基盤自体が未実装のため後続タスク
- 工房カードの実際の利用場面（プロフィールURL共有時の表示、友達バーのホバーポップアップ）は未実装。カード作成機能のみ先行実装した
- 用語・表示設定（`preferences.terminology`）の再設計は未着手（世界観重視／利便性重視 × 日本語／Englishの2軸への対応方針は`docs/マップ.md`側で継続検討中）
- CLAUDE.mdが意図する等幅フォント（BIZ UDPMincho/BIZ UDGothic）のアセット取得・導入（日本語グリフの字幅要確認）
- RNNoise本格統合（フェーズ3）: Web=AudioWorklet+WASM、Android/iOS/Windows/Linux/macOS=ネイティブAPMカスタムプロセッサという、プラットフォームごとの別プロジェクト規模のネイティブ実装が必要
- Apple認証: Apple Developer Program登録後、Firebase ConsoleでのServices ID/Team ID/Key ID設定とログイン画面のボタン復活が必要

## 2026-07-22

### 実装内容
- UIスタイルを「DaiDai（標準）/シンプル」の2択からシンプル1本化に統合。`lib/models/ui_style.dart`・`lib/providers/ui_style_provider.dart`を削除し、代わりに`lib/providers/accent_color_provider.dart`（アクセントカラーをSharedPreferencesに永続化）・`lib/utils/color_hex.dart`（`#RRGGBB`⇔Color変換）を新設。`AppTheme.themeFor(UiStyle)`だった`app_theme.dart`を`AppTheme.theme(Color accentColor)`に変更し、`ColorScheme.fromSeed`で配色全体を導出する形にした
- 設定タブにカラーコード入力欄を追加。ユーザーが`#RRGGBB`形式で好きな色を入力するとアクセントカラーが即時反映される
- ホーム画面のナビゲーションを下部ナビ/サイドレールから、独立した丸いチップ（`_NavChip`）を浮かせる形に変更。選択中のチップはアクセントカラーで塗りつぶし
- 身だしなみタブに「蔵」機能を実装。`ProfileMaterial`（アイコン・背景画像）・`StatusMessage`（ステメ）モデルを追加し、上限をアイコン最大2件・背景画像最大3件・ステメ最大2件に設定。`AppUser`に`icons[]`/`backgroundImages[]`/`statusMessages[]`とそれぞれの`activeXxxId`を追加。`UserRepository`に`updateUser`・`uploadIcon`・`uploadBackgroundImage`（Firebase Storageへアップロードしてダウンロード URL取得）・`deleteProfileMaterial`を追加。`profile_tab.dart`をプレースホルダーから、サムネ一覧＋追加/選択/削除ボタンを持つ実装に書き換え
- 依存パッケージに`firebase_storage`・`image_picker`を追加
- `storage.rules`を新規作成（本人のみアップロード・削除可）、`firestore.rules`のusers更新ルールに蔵の件数上限のサーバー側バリデーションを追加、`firebase.json`にstorage設定を追加してデプロイ
- Firebase Storageをこのプロジェクトで初めて有効化（Blazeプランへアップグレード、リージョンは東京=asia-northeast1）。Firestore・Storage双方のルールをデプロイ
- このマシンにNode.js・Firebase CLI（npm版）を新規インストール
- 学校プロキシ（`proxy.kasd.ac.jp:8080`）が原因でローカルのFlutterデバッグ接続が503エラーになる問題を`NO_PROXY=localhost,127.0.0.1,::1`のユーザー環境変数追加で解消
- CLAUDE.mdに「開発時の動作確認」節を追加：コード編集後は確認を挟まずVivaldiで動作確認環境を起動する運用ルールを明文化

### 詰まったところ
- 学校プロキシ設定（`HTTP_PROXY`はあるが`NO_PROXY`が空）により、ChromeDevToolsProtocol経由のローカルデバッグ接続がSquidプロキシに横取りされ503エラーでクラッシュしていた。原因特定に時間がかかった
- `flutter run -d edge`で接続自体は成功したように見えたが、実際にはmsedge.exeプロセスが起動していることを確認できず、最終的に`web-server`モード＋手動ブラウザ起動に切り替えた
- Firebase CLIのWindowsスタンドアロン版（firepit、235MB）が初回起動時に内部の`welcome.js`で`JSON.parse`エラーを起こし全く動かなかった。原因はfirepit独自のnode/npmエミュレーションの制約と推測されるが根本解明はできず、Node.js＋npm版のfirebase-toolsに切り替えることで回避
- `firebase login`・`firebase login:ci`はどちらも非対話環境（stdin未接続）だと明示的に拒否される仕様で、ユーザー自身のターミナルでログインしてもらう必要があった
- `firebase deploy --only storage:rules`が「Could not find rules for the following storage targets: rules」で失敗。Firestoreの`--only firestore:rules`と違い、Storageの`--only`はカスタムターゲット名を指定する文法のため`storage:rules`だと存在しないターゲット`rules`を探しにいってしまう。正しくは`--only storage`とだけ指定する
- Firebase Storageがプロジェクトで未セットアップで、有効化にはBlazeプラン（従量課金制）への請求先アカウント登録が必要だった。決済を伴う判断のため代行せずユーザーに確認・操作してもらった

### 次にやること
- Vivaldiで蔵機能（アイコン/背景画像のアップロード・切り替え・削除、ステメの追加・選択）の実際の動作確認
- 設定タブ（用語・表示設定）・運営タブなど残りのプレースホルダー画面の実装
- 画像圧縮（WebP変換）は未実装。現状アイコン・背景画像はJPEG生バイトのままアップロードしており、フェーズ1仕様の「WebP圧縮」とは未整合
- CLAUDE.mdと`docs/マップ.md`の食い違い3点（談論/バックアップコード/メールアドレス）は依然未解決

## 2026-07-21

### 実装内容
- 一対の初回作成時に`cloud_firestore/permission-denied`になるバグを修正: `getOrCreateDirectMessage()`が作成前に存在確認の`get()`を行うが、Firestoreルールが`resource == null`（未作成）のケースを考慮していなかったため。ルールに`resource == null`の許可条件を追加してデプロイ
- `messages`コレクション（一対・広場共通）の`deletedAt`+`sentAt`クエリに複合インデックスが必要だったため`firestore.indexes.json`に追加してデプロイ
- UIスタイル切り替え機能を追加（`lib/models/ui_style.dart`, `lib/theme/`, `lib/providers/ui_style_provider.dart`）。「DaiDai（標準）」と、mobbin.com「one year」を参考にした「シンプル」の2種類。設定タブから切り替え、端末のSharedPreferencesに永続化
  - 当初シンプルUIが配色変更のみに見えるという指摘を受け、根本原因（ナビゲーションバーやチャット吹き出しなど多くのウィジェットが橙色をハードコードしテーマを見ていなかった）を修正し、`Theme.of(context).colorScheme`を参照するよう統一
  - `AppThemeExtras`というThemeExtensionを追加し、シンプルUIではボタン・カード・チャット吹き出しに強めのelevationと藍色シャドウで「浮いている」見た目を付与。画面遷移にスライド＋ポップのアニメーション、＋ボタンのポップアップにバウンドするポップアニメーションを追加
- ホーム画面・語らいタブのUI再構成: サイドバーを独自Column実装に変更しアイコンを縦に均等分散、AppBarのタブ名表示を撤去、＋ボタンをホーム画面から語らいタブ自身に移動しポップアップを中央表示に変更、一対/広場の切り替えをプルダウンから横並びタブに変更（一対が左）、各画面の戻る矢印を削除しブラウザ/マウスの戻る操作に任せる形に変更、メニューアイコン下のラベルテキストを削除
- 音声通話機能を実装（`flutter_webrtc`、CLAUDE.mdの通話仕様通りSTUNのみ・TURN未導入）: `Call`モデル、`CallRepository`（`calls/{callId}`＋`callerCandidates`/`calleeCandidates`サブコレクションでシグナリング）、`WebrtcCallController`（RTCPeerConnectionのライフサイクル管理）、`CallScreen`（発信中・着信中・通話中を1画面で表現）、`IncomingCallListener`（アプリ全体で着信を監視し自動でCallScreenを開く）。一対のチャット画面に発信ボタンを追加（広場の通話はフェーズ1スコープ外のため未対応）。Firestoreルールに`calls`用のアクセス制御を追加してデプロイ。Android（`RECORD_AUDIO`等）・iOS/macOS（`NSMicrophoneUsageDescription`、entitlements）にマイク権限設定を追加
- デバッグ運用方法を見直し: `flutter run -d chrome`（毎回使い捨てブラウザプロファイル）をやめ、`flutter run -d web-server --web-port=8765`を起動して複数のブラウザ/プロファイルで同じURLを開く方式に変更。ログインセッションが毎回消える問題と、複数アカウント同時テストの両方を解決
- Vercelへのデプロイ準備として`vercel.json`を作成（ビルド時にFlutter SDKをcloneして`flutter build web --release`する構成）。ホームページ（rhing.jp）との統合はDaiDai完成後に先送りする方針

### 詰まったところ
- Firestoreルールで「ドキュメントが存在しない場合のget」を考慮しておらず、一対の初回作成が必ずpermission-deniedになっていた（`resource == null`の分岐漏れ）
- シンプルUIの見た目が変わらない件、原因調査したところ多数のウィジェットが橙色を直接ハードコードしていて、テーマ切り替えのRiverpod provider自体は正しく動いていたのに反映される箇所がごく一部だった
- ローカルdevサーバー上で「クリックもマウスも効かない」状態が発生。原因はデバッグ環境固有の可能性が高いと判断し、実配信（Vercel）での確認に切り替える方針にした。根本原因は未特定のまま
- `shared_preferences`など新規パッケージ追加直後はホットリスタート（`R`）でも反映されず、`flutter run`プロセスの完全な再起動が必要だった

### 次にやること
- `vercel.json`のコミット・push（今回の日記pushに含める）
- Vercel側のプロジェクト作成・GitHub連携はユーザーのブラウザ操作待ち（`rhing.official@gmail.com`でログイン）
- 音声通話の実機・複数ブラウザでの実動作確認がまだ未実施
- TURNサーバー未導入のため、双方が厳しいNAT環境だと接続できないケースは未対応（フェーズ2以降の課題）
- ローカルdevサーバーでクリックが効かなくなった件の根本原因調査（再現すれば）
- CLAUDE.mdに残る3つの未解決の食い違い（談論/バックアップコード/メールアドレス）は引き続き未確認
- 広場（グループ）の音声通話は未対応。必要になった時点で別タスクとして着手

## 2026-07-20

### 実装内容
- `docs/`配下にObsidianノート（企画書・技術仕様書・マップ）へのシンボリックリンクを作成し、`.gitignore`に追加（個人環境の絶対パス依存のためgit管理外）。CLAUDE.mdに「実装前に必ずdocs/を参照する」旨と、「CLAUDE.mdとdocs/が食い違う場合はユーザーに確認してから進める」旨を明記
- `docs/マップ.md`に合わせてCLAUDE.mdの独自用語表を全面更新（縁側→一対、お部屋→寄合、節→表組、など）。`docs/マップ.md`と既存CLAUDE.md本文の間で未解決の食い違いが3点（談論=フォーラム機能の是非、バックアップコードの採否、メールアドレス欄の扱い）あることを警告ブロックとして明記し、無断で決め打ちしないよう記録
- プラットフォーム実装・リリース順序（①ブラウザ→②Android→③Windows→④Linux→⑤iOS→⑥macOS）をCLAUDE.mdに記録
- ホーム画面をレスポンシブ化: 画面幅600dp（Material 3のmedium windowクラス）を境に、広い画面では`NavigationRail`（サイドバー）、狭い画面では`NavigationBar`（下部ナビ）に自動切り替え
- 語らいタブ（`TalksTab`）を「広場」「一対」のポップアップ切り替え式に再設計。従来の2セクション同時表示から、選択中のカテゴリのみ一覧表示する方式に変更
- コード内コメント・UI文言の「縁側」表記を「一対」に置換（`chat_screen.dart`, `direct_message.dart`, `direct_message_repository.dart`, `home_screen.dart`）
- 相手検索の「@」プレフィックス不整合バグを修正: UI表示は常に`@rhingId`形式だが、Firestore側の`rhingId`フィールドには「@」が含まれないため、「@aaaa」のように入力すると検索がヒットしなかった。`add_chat_screen.dart`・`create_group_screen.dart`の両方で、検索前に先頭の「@」を`replaceFirst(RegExp(r'^@+'), '')`で除去するよう修正し、入力欄に`prefixText: '@'`を追加してユーザーが「@」を入力しなくて済むようにした
- rhing.jpホームページ（Next.js）へのFlutter Web組み込み方法を調査し、`/home/arata/Obsidian-Personal/コマンド/FlutterWebをNext.jsに同梱.md`にまとめた（推奨方式: `flutter build web --base-href /app/`の成果物をNext.jsの`public/app/`にコピー）
- ローカル開発環境: `flutter run -d chrome`用に`CHROME_EXECUTABLE`をFlatpak版Google Chromeに設定し`~/.bashrc`に永続化（Vivaldi・snap版Chromiumも試した上でChromeに確定）

### 詰まったところ
- `flutter run -d chrome`が「no supported devices」で失敗する問題は、Chrome系ブラウザ（DevTools Protocol対応）が入っていなかったことが原因。FloorpはFirefox系のため非対応と判明し、最終的にFlatpak版Google Chromeを`CHROME_EXECUTABLE`に指定する形で解決
- マルチアカウントでの同時ログインテスト中、`flutter run`が出力するDart VM Service（デバッグ用URL、DDS経由）と実際のアプリURLを混同し、デバッグURLをブラウザに直接開いてしまう場面があった。デバッグ用エンドポイントと実アプリのURLは別物である旨を確認
- 上記の「@」検索バグは、UIの表示規則（常に@付きで見せる）と、Firestoreに保存する実データ（@なし）の対応関係を実装時に明示的に統一していなかったのが原因

### 次にやること
- 「@」検索バグの修正がユーザーの実行環境（`flutter run -d chrome`セッション、ホットリロード）でまだ再検証されていない
- CLAUDE.mdに残した3つの食い違い（談論/バックアップコード/メールアドレス）をユーザーと確認して解消する
- Persona 5風のビジュアル・配色の作り込みを今進めるか、デザイナー雇用後に回すか未決定（ユーザーに確認中）
- mobbin.comのTelegram UI参考リンクが403で読み込めなかったため、代替の参照先が必要なら再取得する
- rhing.jpリポジトリのPAT平文埋め込みをcredential-store方式に移行するか（提案済み・未回答）
- `sudo snap remove chromium`をユーザー自身が実行済みか未確認
- 複数アカウントでの広場・一対の送受信テストを再開（「@」バグ調査で中断していた）
- コード内の旧用語（`DirectMessage`クラス・`directMessages`コレクションなど）を新用語にリネームする作業は未着手（別タスクとして分離済み）
- rhing.jp/Flutter Web統合の実装（ドキュメント化は完了、コード側の実行はこれから）
- Android実機/エミュレータ、iOS/macOSでの動作確認は引き続き保留中

## 2026-07-19

### 実装内容
- Flutterプロジェクトを新規作成（`jp.rhing.daidai`、Android/iOS/macOS/Windows/Linux/Web対応）
- Firebaseプロジェクト`daidai-rhing`（rhing.official@gmail.com管理）と接続（`flutterfire configure`）
- Firestoreデータベースを作成（asia-northeast1）、本番用セキュリティルールをデプロイ（縁側の参加者のみ読み書き可能）
- Firebase Authenticationで Google ログインを有効化
- Repositoryパターンで実装（CLAUDE.mdの方針通り）:
  - `AuthRepository`（Google Sign-In、Web/モバイルで実装を分岐）
  - `UserRepository`（Rhing ID検索・重複チェック）
  - `DirectMessageRepository`（縁側の作成・一覧取得・メッセージ送受信）
- モデル: `AppUser`, `DirectMessage`（相手のRhing IDを非正規化して保持）, `Message`
- 画面: サインイン画面、Rhing ID登録画面、ホーム画面（語らい・身だしなみ・設定・運営の4タブ構成）、相手追加画面、1対1チャット画面（リアルタイム送受信）
- Android向けにSHA-1フィンガープリントをFirebase CLIで登録し、`google-services.json`を更新。iOS/macOS向けに`GoogleService-Info.plist`を取得
- Firestore複合インデックス（`participants` array-contains + `lastMessageAt`）をデプロイ
- Web（`flutter build web`を静的サーバーで配信）で、Googleログイン〜Rhing ID登録〜ホーム画面〜相手追加〜チャット送受信までの一連の動作を実機ブラウザで確認済み
- ホーム画面を語らい・身だしなみ・設定・運営の4タブ構成に再編（CLAUDE.mdのメインメニュー定義に準拠）。ログアウトはAppBarから設定タブへ移動
- 複数人数での語らい（広場・お部屋）機能を実装:
  - `Group`/`Room`モデル、`GroupRepository`（広場作成・一覧取得・お部屋メッセージ送受信）を追加
  - 広場作成画面（Rhing IDでメンバーを複数追加、自分＋2人以上＝3人以上を必須化）
  - `ChatScreen`をDM・広場共通で使える汎用コンポーネントに再設計（stream/送信コールバックを外から注入する形に変更）
  - `TalksTab`を「広場」「縁側」の2セクション表示に拡張
  - Firestoreセキュリティルールに`groups`/`rooms`/`messages`のパスを追加してデプロイ（メンバーのみ読み書き可）
- `/diary`スキルを修正: `日記.md`だけでなくFlutterプロジェクト一式もまとめて`git add -A`でpushするよう変更（CLAUDE.mdのGit管理セクションにも反映）

### 詰まったところ
- `google_sign_in` v7の`authenticate()`はWebでは未対応（GIS SDKのボタンUI経由のみ）という仕様に気づかず一度実装。Web版は`firebase_auth`の`signInWithPopup`を使う形にkIsWeb分岐で修正
- `flutter run -d web-server`の開発サーバーが、ソース修正後も古いコンパイル結果を配信し続ける現象に遭遇。`pkill -f "flutter run -d web-server"`が実際のdartvmプロセスのargvにマッチせず無効だったのが一因。最終的にPID直指定でプロセスをkillし、`flutter build web` + Python簡易サーバーという毎回フルビルドする確実な方式に切り替えて解決
- Firestore APIの有効化・Google認証プロバイダの有効化は、gcloud CLI未導入のためCLIから完結できず、Firebase Consoleでの手動操作（ユーザー側の作業）が必要だった
- Web向けdart2js出力は日本語文字列が`\uXXXX`形式でエスケープされており、素のgrepで「新機能の文字列が見当たらない→ビルドが古いのでは」と一瞬誤認しかけた。実際はビルドは最新で、検証方法（grep対象）が誤っていただけだった

### 次にやること
- 複数アカウント（3つ以上のGoogleアカウント、別ブラウザプロフィール）での同時ログインによる、広場・縁側の送受信テストがまだ未実施。テスト方針をユーザーと相談したところで会話が終了しており、実際の動作確認はこれから
- Android実機/エミュレータでの動作確認（Android SDK cmdline-toolsが未導入のため保留中）
- iOS/macOSはXcode環境がなく未検証
- 仲間承認制などのスパム対策（現状は誰でもRhing ID検索で縁側・広場を開始できる）
- 身だしなみ（プロフィール・蔵システム）、設定、運営タブは現在プレースホルダーのみ。中身の実装
- 広場のメンバー管理（招待・脱退・権限変更）は未実装（作成時のメンバー構成で固定）
