> 2026-09-18更新: コード（`lib/`, `functions/src/`, `firestore.rules`等）を直接確認し、`課題.md`・`日記.md`（直近2週間分）と突き合わせて全面更新した。2026-09-08時点の優先度整理セクションはそのまま残し、それ以降のセクションを最新状態に合わせて書き換えている。
> **運用ルール再確認**: `[x]`は「実装済みかつ動作確認済み」のみに付ける。コードは存在するが本番デプロイ未確認・実機未検証のものは`[ ]`のまま本文に状況を注記する（2026-09-16の日記で「パスキー関連Cloud Functions16個中12個が本番未デプロイのまま放置されていた」教訓があり、コードの存在だけでは`[x]`にしない）。

# 実装内容チェックリスト

完成した処理と未実装の処理を分けるためのチェックリスト。

**運用ルール**: チェック済み（`[x]`）の項目は「実装済みで動作確認も取れている」ことを意味する。
その処理に関連するエラー・バグが発生し、コードを再び書き直すことになった場合は、
**着手した時点でチェックを外す**（`[x]` → `[ ]`）こと。直ったら再度チェックする。

**4区分タグ（2026-09-23追加）**: `[ ]`の項目のうち「本当に未着手の機能」には、実装するかどうかの見通しを示す以下のタグをチェックボックス直後に付ける。
- `**[近日]**` これから実装する機能（直近で着手する想定）
- `**[将来]**` いずれ実装する機能（フェーズ3以降・急ぐ理由なし等）
- `**[検討中]**` 実装するか迷っている機能（方針未確定）
- `**[不採用]**` 絶対実装しない機能（CLAUDE.mdの「実装しない機能」表と同期。末尾セクション参照）

一方、`[ ]`の中には「実装済みだが本番デプロイ・実機検証が済んでいないだけの項目」（パスキー本体等）や「バグ・不具合の再調査項目」も混在しており、これらは「実装するかどうか」の意思決定の対象ではないため4区分タグを付けない（既存の注記文のまま）。

---

## 2026-09-08 優先度整理（極みプラン・E2E暗号化着手前の残タスク）

有料機能（極みプラン）はStripe決済に関わる法務手続きが終わるまで着手できない。E2E暗号化はSignal Protocolの実装自体に時間がかかる重い作業のため、その間に法務と無関係に進められるタスクを整理したもの。2026-09-18時点で再確認したが、いずれも状況は変わっていない。

### 法務と無関係にすぐ着手できるもの
- [ ] **[近日]** 友達解除（アンフレンド）— 単純な一方的アンフレンドは無く、双方合意制で会話ごと完全削除する「絶縁（severance）」（`lib/repositories/direct_message_repository.dart`）のみ実装済み。カジュアルなアンフレンド機能は依然未実装
- [ ] **鍵付きの寄合の最終会話日時が非参加者にも見えてしまうバグ**（`課題.md`記載の未検証バグ）— 2026-09-14に`defaultRoomId`概念が全面撤去され最終会話日時の算出ロジック自体が変わっているため、旧バグがまだ再現するか要再調査
- [ ] 語らいの数・文字数が増えた際に選択ボタンがピクセル不足で途切れるバグ（`課題.md`記載の未検証バグ、日記.mdに修正記録なし）
- [ ] **[近日]** OSごとの通知対応（FCMは現状Android/Web限定、`lib/repositories/push_notification_repository.dart`の`_supportedPlatform`参照。iOS/Windows/Linux/macOSは未対応）
- [ ] **[近日]** 工房カードの実利用箇所（プロフィール共有時の表示等、作成機能のみ先行実装のまま放置）
- [ ] **[近日]** Sudachi（P2Pファイル転送）— CLAUDE.mdの必須機能だがまだ手つかず（`lib/utils/file_extension_policy.dart`にコメントのみ、2GB上限の送信拒否だけ実装済み）

### フェーズ3以降でよい（急ぐ理由なし）
- [ ] **[将来]** 表広場・表組裏組・密談・談論（`lib/l10n/vocabulary.dart`に用語定義はあるが機能は未実装）
- [ ] **[将来]** 安否確認
- [ ] **[将来]** RNNoise本格統合
- [ ] **[将来]** TURNサーバー導入（`競合調査.md`2026-07-28の調査でも「少人数利用なら急ぐ必要なし」と結論済み。アプリ側は環境変数注入のフックのみ実装済み、coturn本体のインフラ構築は未着手）

### 方針未確定（判断待ち）
- [x] メッセージ検索 — 実装済み（`lib/features/chat/talks_search.dart`/`talks_search_screen.dart`）。2026-09-13/14に不具合修正・検索対象拡大（全寄合を対象に）を実施。`課題.md`側に「実装するか迷っている機能」として残っているのは記載が古いだけなので要更新
- [ ] ぺったんの有料販売（`functions/src/index.ts`の`PAID_PACKS_ENABLED = false`で意図的に凍結中。機能自体は実装済み、商取引の手続き待ち）

---

## 認証・アカウント

- [x] Google認証（Web/モバイルでフロー分岐）
- [ ] **[近日]** Apple認証の実装（`AuthRepository.signInWithApple()`実装済みだが、Apple Developer Program未契約のためログイン画面のボタンは非表示のまま。CLAUDE.md記載通り変化なし）
- [x] Rhing Seed登録・重複チェック・小文字統一
- [x] 2段階認証（TOTP）※フェーズ2計画だったが2026-08-09前倒し実装済み（`AuthRepository`のTOTP関連メソッド、`two_factor_setup_dialog.dart`）
- [ ] パスキー（Rhing Seed＋パスキー）— コードは新規登録・ログイン・複数端末管理・秘密の質問復旧・Conditional UI対応まで実装済み（`functions/src/index.ts`のパスキー関連16関数、`lib/repositories/auth_repository.dart`、`passkey_management_dialog.dart`）だが、**2026-09-16に「関連Cloud Functions16個中12個が一度も本番デプロイされていなかった」ことが判明し当日デプロイ**したばかりで、本番環境での実際のWebAuthn認証（登録・ログイン・Conditional UI・復旧）は未検証。本番ドメイン未確定のため`PASSKEY_RP_ID`/`PASSKEY_ALLOWED_ORIGINS`も`localhost`のまま、Android/iOS/macOSのネイティブ設定（assetlinks.json等）も未着手
- [x] QRコードログイン ※フェーズ2計画だったが2026-08-09前倒し実装済み
- [ ] 秘密の質問によるパスワード復旧（コード実装済み、パスキー本体と同じく本番デプロイ直後で実機未検証）
- [x] アカウント削除（2026-07-28実装、旧「3時間以内完全削除」から変更）: 通常削除（30日猶予＋復元）・即時削除の両方を実装済み（`UserRepository.requestAccountDeletion`/`deleteAccountImmediately`、`functions/src/index.ts`の`processAccountDeletions`/`deleteAccountImmediately`）。長を務める広場が残っている間は削除不可のガードも実装済み。ただし削除完了時のDM通知（`notifyDirectMessages`等）は2026-09-15時点でデプロイ済みだが実機確認は未了

## 一対（DM）

- [x] 友達申請フロー（IDで検索→申請を送る→相手が承認する→一対で会話できるようになる）
- [x] 届いている申請／送った申請を一対リストの最上部にインライン表示
- [x] 届いている申請の承認・拒否をその場のボタンで操作
- [x] 相互申請時の即時承認
- [x] Firestoreルールで一対作成をfriendRequests承認済みに限定
- [x] 一対一覧（件数チップ表示）
- [x] 一対・広場のピン留め（右クリック／長押しメニュー）
- [x] 一対・広場の通知オフ（表示上のみ、実際の抑制はFCM基盤側の対応次第）
- [x] メッセージ送受信（Firestoreリアルタイム購読）
- [ ] **[近日]** 友達申請の新着通知（現状は一対リストへのインライン表示のみ。プッシュ通知は後続）
- [ ] **[近日]** 友達解除（アンフレンド）— 上記「2026-09-08優先度整理」参照。合意制の「絶縁」機能はあるが単純なアンフレンドは無い
- [x] 招待リンク・QRコードでの仲間追加（縁結び、2026-07-24追加、`lib/features/profile/enmusubi_page.dart`）
- [x] 一対の複数会話（寄合）機能・単一/複数モード切り替え（2026-07-28前倒し実装、`roomsEnabled`）: 2026-09-13〜14にフィールド設計を再整理（3択→ON/OFFトグル1つに統合、`defaultRoomId`概念を全面撤去）。firestore.rulesは本番デプロイ済みだが実機での動作確認は日記TODOに残ったまま
- [x] 一対専用の設定画面「一対の設定」（2026-09-13新設、ミュート・ブロック・既読提案・絶縁提案・会話削除・寄合モード選択を集約）

## 広場（グループ）

- [x] 広場の作成（自分＋2人以上）
- [x] 広場一覧（件数チップ表示）
- [x] メッセージ送受信（Firestoreリアルタイム購読、複合インデックス対応済み）
- [x] メッセージへの送信者アイコン表示
- [x] 広場のピン留め・通知オフ
- [x] 広場のカスタマイズ機能（プロフィールカード・メンバー一覧・招待リンク作成・退会、2026-07-24追加）
- [x] 広場のカスタムロール機能（2026-07-28実装、`GroupRole`/`GroupPermission`: manageRooms/manageRoles/manageReadReceipts/manageJoinRequests/createInvite の5種、複数付与対応、`group_role_list_popup.dart`/`group_role_priority_dialog.dart`）
- [x] 広場の複数寄合機能・単一/複数モード切り替え（一対と同じ`roomsEnabled`設計、2026-09-13〜14に再設計済み）
- [ ] **[将来]** 表組・裏組（カテゴリ機能） ※フェーズ3、未実装（用語定義のみ`vocabulary.dart`に存在）
- [ ] **[将来]** 表広場（公開広場） ※フェーズ3、未実装
- [ ] **[将来]** 密談（非公開チャンネル）・談論（プロジェクトボード） ※フェーズ3、未実装
- [x] 広場詳細ダイアログ（`PlazaDetailDialog`）: マップ.mdのサイトマップを一覧表示し、実装済み/準備中を区別表示
- [ ] 実機・本番環境での複数寄合／単一モード切替の最終動作確認（コード・rules共にデプロイ済みだが未検証）

## 語らいタブ（ホーム画面UI）

- [x] 一対／広場の横並びタブ切り替え
- [x] ＋ボタンからの中央ポップアップ（一対を始める／広場を作る）
- [x] サイドバー／下部ナビを丸いチップ形式に統一（レスポンシブ切り替え）
- [x] ブラウザ・マウスの「戻る」「進む」ボタン対応（go_router導入）
- [x] モバイル下部ナビ上でのスワイプによるタブ切り替え
- [x] 広い画面（760px以上）では一対・広場一覧と会話を左右分割で同時表示
- [ ] **[検討中]** ブラウザ直接URL入力・リロードからの画面復元（現状`extra`渡しのため未対応）

## 身だしなみ（蔵システム）

- [x] アイコンのアップロード・切り替え・削除（最大2件）
- [x] 背景画像のアップロード・切り替え・削除（最大3件）
- [x] ステメ（ひとこと）の追加・切り替え・削除（最大2件）
- [x] ニックネームの追加・切り替え・削除（最大3件、最大20字）
- [x] ステメの文字数上限（最大40字）
- [x] Firebase Storageルール（本人のみアップロード・削除可）
- [x] 蔵／工房のタブ分割・素材管理
- [x] 画像のWebP圧縮（`flutter_image_compress`、Windows/Linuxはフォールバック）
- [x] 工房（プロフィールカード作成、最大3枚）
- [ ] **[近日]** 工房カードの実際の利用場面（プロフィールURL共有時の表示、友達バーのホバーポップアップなど）は未実装。カードの作成機能のみ先行実装
- [ ] **[検討中]** アイコンにgifを設定

## 音声通話・ビデオ通話

- [x] WebRTCによる1対1音声通話（STUNのみ、TURN未導入）
- [x] Firestoreシグナリング（offer/answer/ICE candidate交換）
- [x] 着信の自動検知・着信画面表示
- [x] ミュート・切断
- [x] Android/iOS/macOSのマイク権限設定
- [x] ビデオ通話（720p、1対1、`webrtc_media_constraints.dart`で`width.ideal=1280`/`height.ideal=720`固定）
- [x] 標準ノイズ抑制（`echoCancellation`/`noiseSuppression`/`autoGainControl`）
- [ ] 実機・複数ブラウザでの実通話の最終動作確認
- [x] 広場（グループ）通話（2026-07-24追加、メッシュ型P2P・参加上限5人、`webrtc_group_call_controller.dart`）: アプリ側の実装は完了
- [ ] **[将来]** TURNサーバー導入（自前coturn運用で2026-07-24方針決定）: アプリ側は`String.fromEnvironment`経由の環境変数注入フックのみ実装済み。**coturn自体のVM構築・DNS・証明書等のインフラは未着手**のためSTUNのみで動作中（対称NAT/CGNAT環境同士は接続失敗しうる）
- [ ] **[将来]** RNNoise本格統合（フェーズ3・保留、標準ノイズ抑制で当面代替する方針決定済み）
- [ ] **[検討中]** 画面共有（未着手、該当コードなし）

## メッセージ機能

- [x] メッセージ削除（2026-07-27実装、範囲選択削除・全参加者が削除し終えた時点でサーバーからも物理削除、`hideMessagesForMe`/`hideRoomMessagesForMe`）
- [x] メッセージ検索（`talks_search.dart`。2026-09-13不具合修正、2026-09-14に検索対象を会話の全寄合へ拡大）
- [x] ぺったん（スタンプ）機能 ※フェーズ2予定だったが前倒しで大部分実装済み: `Sticker`/`StickerPack`（`lib/models/sticker.dart`）、購入済みパック管理（`owned_sticker_packs_popup.dart`）
- [x] メッセージ内容に応じたぺったん提案（2026-09-05実装、`StickerRole`＋キーワード中間層、`sticker_suggestion_strip.dart`/`suggestStickers`）
- [ ] ぺったんパックの有料販売（`PAID_PACKS_ENABLED = false`で意図的に凍結中、商取引の手続き待ち）
- [ ] ぺったんの送信導線の再確認（2026-09-15の並行作業で入力欄「＋」添付メニューから「ぺったん」項目が削除されており、代替導線が見当たらず要確認との日記記載あり）
- [x] 既読管理（オン/オフ切り替えのみ、`readReceiptsEnabled`、双方合意制の提案フロー）
- [ ] **[将来]** 既読の「一部」非表示（CLAUDE.md記載の粒度。現状は全体オン/オフのみで、極みプランとの連携も`subscriptionPlan`自体が未実装のため無し）

## 運営・管理画面

- [x] `/admin`管理画面（2026-08-12実装、`admin_panel_screen.dart`）: 住人一覧・アカウント停止/解除
- [x] 便り（公式アカウントからのお知らせ配信、`broadcastAnnouncement`、`announcement_screen.dart`）
- [x] 初回管理者登録（`grantFirstAdminOnce`、早い者勝ちブートストラップ）

## 極みプラン

- [ ] **[近日]** 極みプラン本体（未着手。2026-09-14の方針決定「機能はフェーズ1に前倒しし当面無料開放、課金ゲートのみ後日有効化」はまだ実装に反映されておらず、`subscriptionPlan`/`kiwami`関連コードはリポジトリ全体で0件のまま。`font_design.dart`の`isKiwamiExclusive`フラグ2件のみ存在するが、極みプラン本体の判定ロジックが無いため実質誰でも選択可能）
- [ ] **[将来]** 既読の一部/完全非表示の極みプラン連携
- [ ] **[将来]** 1080pビデオ通話の極みプラン連携

## UIスタイル・デザイン

- [x] アクセントカラーのカラーコード自由入力（`#RRGGBB`/`#RRGGBBAA`）
- [x] 標準アクセントカラーを`#F08300`に設定
- [x] アクセントカラープリセットの統合・ユーザー登録色との一本化（2026-09-16）
- [x] ダークモード（無料版で提供、2026-07-27実装、中立ダークグレー固定）
- [x] UIスタイル3種（フラット／劇画／ガラス、`AppUiStyle`）
- [x] フォントデザイン（25書体実装済み、CLAUDE.md記載と一致。`lib/models/font_design.dart`の`FontDesign`enumで確認）
- [x] 用語スタイル切り替え機能の廃止（2026-08-13、`TerminologyStyle`は完全削除済み、`vocabulary.dart`は表示言語のみの1軸に整理済み）
- [ ] **[近日]** 表示言語の全画面対応（`lib/features/`配下に日本語ハードコードが16ファイル程度残存。うち`admin_gate.dart`/`admin_panel_screen.dart`は運営専用画面のため意図的に対象外の可能性）
- [x] マテリアルに影を一切使わない方針の徹底（2026-09-06、`GlassIconBadge`の`shadow`例外も撤回済み）

## 通知基盤

- [x] FCM基盤自体は実装済みだが対応OSが限定的（`push_notification_repository.dart`の`_supportedPlatform`が`kIsWeb || Platform.isAndroid`のみ）
- [ ] **[将来]** iOS/Windows/Linux/macOSへの通知対応拡大（未着手）
- [ ] **[検討中]** 通知タイミング・再通知間隔の設定UI

## インフラ・デプロイ

- [x] Firestore/Storageセキュリティルールのデプロイ
- [x] Vercelへのデプロイ
- [x] Firebase Storageバケットのcors設定
- [x] Firebase Authの認証済みドメインにVercel本番ドメインを追加
- [ ] **[将来]** Sudachi（P2Pファイル転送、2GB超）— 別アプリとして実装。現状は2GB上限を超えるファイルの送信を拒否するだけ（`file_extension_policy.dart`）
- [ ] **[近日]** rhing.jp（ホームページ）との統合（DaiDai完成後に着手予定）
- [ ] パスキー関連本番デプロイの実機検証（Web/Android/iOS/macOS、Conditional UI含む）
- [ ] **[近日]** パスキーのネイティブ設定（Android/iOS/macOSのassetlinks.json等、本番ドメイン確定後に着手）

## スパム対策

- [x] 仲間承認制（友達申請フローで実装済み）
- [x] レート制限（`functions/src/index.ts`のサーバー側時間窓レート制限、`spam_config_repository.dart`/`spam_check.dart`）
- [ ] **[将来]** メタデータ分析の高度化（基本的なレート制限のみ実装、より踏み込んだ分析は未着手）

## その他・未確認事項

- [ ] **[将来]** E2E暗号化（Signal Protocol） ※フェーズ2、専門家レビュー必須、未着手（「まず何から始めるか」という検討課題のまま`課題.md`に残存）
- [ ] **[将来]** 安否確認・ロール管理（安否確認は未着手。ロール管理は広場のカスタムロールとして実装済みのため実質解消、要`課題.md`更新）
- [ ] CLAUDE.mdと`docs/マップ.md`の未解決の食い違いは2026-07-24に3点とも解消済み（談論／バックアップコード／メールアドレス、CLAUDE.md「独自用語」節参照）。本項目は解消済みとして削除可
- [ ] 入力中テキストの共有ノート即時同期（2026-09-09時点で「実機では反映されず未解決」のまま、その後の調査記録なし）
- [ ] 新規実装機能の実機未検証まとめ（2026-09-14〜16に並行実装され動作未確認のもの）: スクリーンショットのOS標準保存先直接保存（`gal`パッケージ導入）、カメラ撮影の送信前プレビュー方式、GlassAvatarのガラスUI各所への横展開、モバイル分割表示判定（`classifyDevice`）まわり

## 課題.md記載の未解決事項（2026-09-18時点、参考転記）

- [ ] **[将来]** 高性能ノイズキャンセリング（RNNoise本格統合）
- [ ] **[近日]** OSごとに対応した通知（FCMがAndroid/Web限定）
- [ ] ぺったんの有料販売（`PAID_PACKS_ENABLED=false`で凍結中）
- [ ] **[将来]** E2E暗号化（何から着手するか含め未検討）
- [ ] **[近日]** 極みプランの実装
- [ ] 入力中テキストの同期方法の即時同期化
- [ ] 語らいの数・文字数増加時のボタン欠けバグ
- [ ] 鍵付きの寄合の最終会話日時が非参加者にも見えるバグ（`defaultRoomId`撤去後の再現性は要再調査）

## 実装しない機能（不採用）

CLAUDE.mdの「実装しない機能」表と同期（2026-09-23追加、一覧性のためこちらにも転記。理由の一次情報はCLAUDE.md側を参照）。

- [ ] **[不採用]** 広告表示 — ユーザー体験を損なう
- [ ] **[不採用]** 個人情報の収集・売却 — プライバシーポリシーに反する
- [ ] **[不採用]** メールアドレス・電話番号登録 — プライバシー重視
- [ ] **[不採用]** DaiDai独自ポイント — 複雑化を避ける
- [ ] **[不採用]** マイナンバー連携 — 必要性がない
- [ ] **[不採用]** 着せ替え — DaiDai本来のUIを崩す恐れ（2026-09-23: LINE等の競合調査の結果、実装・審査体制の運用負荷も大きいと判断し見送り方針を維持。ユーザーから強い希望があれば再検討する）
- [ ] **[不採用]** メーラー — 機能複雑化を避ける。作るなら別アプリ
- [ ] **[不採用]** Discordフォーラム機能 — 会話が細分化されすぎる懸念（※`docs/マップ.md`の「談論」構想と重複。方針要確認）
